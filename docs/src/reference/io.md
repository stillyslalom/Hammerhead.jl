```@meta
CurrentModule = Hammerhead
```

# I/O and batch processing

Image and mask loading (FileIO/ImageIO), JLD2 result serialization, and the
batch sequence driver. See the [batch-processing how-to](../howto/batch.md)
for workflow recipes and [Run PIV on a GPU](../howto/gpu.md) for persistent
device-workspace behavior.

```@index
Pages = ["io.md"]
```

```@autodocs
Modules = [Hammerhead]
Pages = ["io.jl"]
Private = false
```
