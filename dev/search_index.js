var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#Bibliography","page":"Bibliography","text":"Hammerhead's correlation, validation, uncertainty, and stereo-calibration algorithms are implemented from the primary literature. Pages throughout the documentation cite the entries below.\n\nDuncan, J.; Dabiri, D.; Hove, J. and Gharib, M. (2010). Universal outlier detection for particle image velocimetry (PIV) and particle tracking velocimetry (PTV) data. Measurement Science and Technology 21, 057002.\n\n\n\nGarcia, D. (2010). Robust smoothing of gridded data in one and higher dimensions with missing values. Computational Statistics & Data Analysis 54, 1167–1178.\n\n\n\nKeane, R. D.; Adrian, R. J. and Zhang, Y. (1995). Super-resolution particle imaging velocimetry. Measurement Science and Technology 6, 754–768.\n\n\n\nKähler, C. J.; Astarita, T.; Vlachos, P. P.; Sakakibara, J.; Hain, R.; Discetti, S.; La Foy, R. and Cierpka, C. (2016). Main results of the 4th International PIV Challenge. Experiments in Fluids 57, 97.\n\n\n\nMeinhart, C. D.; Wereley, S. T. and Santiago, J. G. (2000). A PIV algorithm for estimating time-averaged velocity fields. Journal of Fluids Engineering 122, 285–289.\n\n\n\nNobach, H. and Honkanen, M. (2005). Two-dimensional Gaussian regression for sub-pixel displacement estimation in particle image velocimetry or particle position estimation in particle tracking velocimetry. Experiments in Fluids 38, 511–515.\n\n\n\nScarano, F. (2002). Iterative image deformation methods in PIV. Measurement Science and Technology 13, R1–R19.\n\n\n\nShavit, U.; Lowe, R. J. and Steinbuck, J. V. (2007). Intensity capping: a simple method to improve cross-correlation PIV results. Experiments in Fluids 42, 225–240.\n\n\n\nSoloff, S. M.; Adrian, R. J. and Liu, Z.-C. (1997). Distortion compensation for generalized stereoscopic particle image velocimetry. Measurement Science and Technology 8, 1441–1454.\n\n\n\nStanislas, M.; Okamoto, K. and Kähler, C. J. (2003). Main results of the First International PIV Challenge. Measurement Science and Technology 14, R63–R89.\n\n\n\nWesterweel, J. and Scarano, F. (2005). Universal outlier detection for PIV data. Experiments in Fluids 39, 1096–1100.\n\n\n\nWieneke, B. (2005). Stereo-PIV using self-calibration on particle images. Experiments in Fluids 39, 267–280.\n\n\n\nWieneke, B. (2015). PIV uncertainty quantification from correlation statistics. Measurement Science and Technology 26, 074002.\n\n\n\n","title":"Bibliography"},{"category":"section","location":"howto/preprocessing/#Build-a-preprocessing-chain","page":"Build a preprocessing chain","text":"Goal: condition raw recordings — static glare, uneven illumination, overexposed particles, low contrast — before correlation. Applied well, preprocessing is often the difference between an unusable and a clean vector field on real data.","title":"Build a preprocessing chain"},{"category":"section","location":"howto/preprocessing/#The-building-blocks","page":"Build a preprocessing chain","text":"Function Removes Typical use\nsubtract_background static background (walls, glare) first, with a compute_background image\nhighpass_filter low-frequency illumination gradients sheet inhomogeneity; sigma a few × particle diameter\nintensity_cap overexposed particles and reflections (Shavit et al., 2007) before correlation, cheap and safe\nclahe (contrast-limited adaptive histogram equalization, CLAHE) poor local contrast dim regions next to bright ones\n\nEach has a mutating form (subtract_background!, highpass_filter!, intensity_cap!, clahe!) that operates in place on a floating-point image — use those in hot loops so chains don't allocate per step.","title":"The building blocks"},{"category":"section","location":"howto/preprocessing/#Estimate-a-background-from-the-sequence","page":"Build a preprocessing chain","text":"For sparse particles over static background, the pixel-wise minimum over many frames is a robust background estimate:\n\nusing Hammerhead\n\nfiles = readdir(\"run_042\"; join = true)\nbg = compute_background(load_image(f) for f in files)   # method = :min","title":"Estimate a background from the sequence"},{"category":"section","location":"howto/preprocessing/#Chain-in-place-transforms","page":"Build a preprocessing chain","text":"The mutating forms return the image, so they nest naturally:\n\npreprocess(img) = clahe!(highpass_filter!(subtract_background!(img, bg); sigma = 8))\n\nOrder matters: remove the background first (so the high-pass filter doesn't smear glare into halos), cap or equalize last.","title":"Chain in-place transforms"},{"category":"section","location":"howto/preprocessing/#Hook-into-the-batch-drivers","page":"Build a preprocessing chain","text":"run_piv_sequence, run_piv_ensemble, and self_calibrate accept a preprocess function applied to each frame after loading:\n\npairs = image_pairs(files; mode = :chained)\nresults = run_piv_sequence(pairs, passes; preprocess)\n\nFrames loaded from file paths arrive as fresh buffers, so mutating preprocessors are safe there. If you pass in-memory matrices, they are handed to preprocess as-is — use the allocating forms in that case to leave your arrays untouched.\n\nPreprocessing currently runs on the central processing unit (CPU) even when a particle image velocimetry (PIV) driver uses a graphics processing unit (GPU) backend. The processed pair is uploaded afterward; keep mutating chains allocation-light and reuse batch-driver workspaces as described in Run PIV on a GPU.","title":"Hook into the batch drivers"},{"category":"section","location":"howto/preprocessing/#Check-the-effect-before-committing","page":"Build a preprocessing chain","text":"Preprocessing that helps correlation can also destroy information (an aggressive high-pass erases large particles). Sanity-check on one pair before running a batch: process with and without the chain and compare result.peak_ratio distributions — the chain should raise it. The real-data tutorial works this comparison on a real wind-tunnel recording, where the textbook chain turns out to hurt.","title":"Check the effect before committing"},{"category":"section","location":"howto/scaling/#Scale-results-to-physical-units","page":"Scale results to physical units","text":"Goal: turn pixel displacements into physical velocities — without giving up the pixel-native diagnostics.\n\nHammerhead measures in pixels per frame interval and keeps every stored array in those measured units. Physical calibration is metadata: a PhysicalScale records the pixel size, the frame interval dt, and the unit names, and physical applies it on demand.","title":"Scale results to physical units"},{"category":"section","location":"howto/scaling/#Attach-a-scale","page":"Scale results to physical units","text":"Pass scale to any driver — run_piv, run_piv_sequence, run_piv_ensemble, run_piv_stereo, run_ptv, run_ptv_sequence, or track_particles:\n\nscale = PhysicalScale(pixel_size = 0.02,   # 0.02 mm per pixel\n                      dt = 1e-3,           # 1 ms between frames\n                      length_unit = \"mm\", time_unit = \"s\")\nresult = run_piv(imgA, imgB; effort = :medium, scale)\n\nThe numbers carry the conversion and the unit names are display-only labels, so results come out in whatever units go in — millimeters and seconds here, so velocities will be mm/s. result itself is unchanged by the attachment: result.u is still pixels, bitwise identical to a run without scale.\n\nFor a result you already have (e.g. loaded from a file saved without one), attach after the fact:\n\nresult = with_scale(load_results(\"run.jld2\")[1], scale)","title":"Attach a scale"},{"category":"section","location":"howto/scaling/#Convert-with-physical","page":"Scale results to physical units","text":"p = physical(result)\np.u          # velocities, mm/s\np.x, p.y     # positions, mm\n\nphysical returns the same result type with positions multiplied by pixel_size and displacements plus their uncertainties by pixel_size / dt. The converted result carries an identity scale with the same unit labels, so physical(p) === p — you cannot double-convert.\n\nConvert last. Validation thresholds, peak_locking, the 0.1 px noise floor used by universal outlier detection (UOD), and the correlation diagnostics all speak pixels, and physical deliberately leaves peak_ratio/correlation_moment untouched. Run the full pixel-side analysis first and convert at the end, for plotting and physics.","title":"Convert with physical"},{"category":"section","location":"howto/scaling/#Construct-from-Unitful-quantities","page":"Scale results to physical units","text":"Load Unitful to activate a quantity-based constructor (a package extension):\n\nusing Unitful\nscale = PhysicalScale(20.0u\"µm\", 0.5u\"ms\")   # pixel size, dt\n\nThe values are stripped in their own units and the unit names become the labels — this scale produces µm and µm/ms. Convert with uconvert before construction if you want different output units.","title":"Construct from Unitful quantities"},{"category":"section","location":"howto/scaling/#Plotting","page":"Scale results to physical units","text":"plot_vector_field plots a scaled result in physical units automatically, labeling the axes with the length unit. To overlay arrows on the source image in pixel coordinates instead, strip the scale:\n\nplot_vector_field(result)                      # physical axes (mm)\nplot_vector_field!(ax, with_scale(result, nothing))  # pixel axes, e.g. over the image","title":"Plotting"},{"category":"section","location":"howto/scaling/#Stereo:-dt-only","page":"Scale results to physical units","text":"A StereoPIVResult is already spatially calibrated — its fields are in the dewarp grid's world units (typically mm). Attach a scale with dt and labels only, leaving pixel_size = 1:\n\nstereo = run_piv_stereo(A1, B1, A2, B2, dw1, dw2;\n                        scale = PhysicalScale(dt = 1e-3,\n                                              length_unit = \"mm\", time_unit = \"s\"))\nphysical(stereo).w    # out-of-plane velocity, mm/s\n\nThe embedded per-camera results (stereo.cam1, stereo.cam2) always stay in dewarped pixels — they are diagnostics.","title":"Stereo: dt only"},{"category":"section","location":"howto/scaling/#Particle-tracking-velocimetry-(PTV)-and-trajectories","page":"Scale results to physical units","text":"run_ptv results convert like particle image velocimetry (PIV) results (the match_residual is a frame-A distance, so it scales with pixel_size), and ptv_to_grid carries the scale onto the binned grid — bin the raw result, then convert.\n\nA TrackingResult stores only positions; velocities are derived by differencing, so pass the scale to trajectory_velocities:\n\ntracks = track_particles(frames; scale)\nu, v = trajectory_velocities(tracks.trajectories[1], tracks.scale)   # mm/s\n\nThis works identically on a raw or a physical-converted tracking result: the converted result's scale keeps dt (its positions are already lengths).","title":"Particle tracking velocimetry (PTV) and trajectories"},{"category":"section","location":"explanation/uncertainty/#Uncertainty-quantification","page":"Uncertainty quantification","text":"Uncertainty quantification (UQ) asks how much random error is likely in each particle image velocimetry (PIV) vector; it is not a claim that the true value is known. With uncertainty = true in PIVParameters, Hammerhead estimates a per-vector measurement uncertainty — one standard deviation, in pixels — into the uncertainty_u / uncertainty_v fields of the result. The estimator is the correlation statistics method of Wieneke (2015), implemented from the paper.","title":"Uncertainty quantification"},{"category":"section","location":"explanation/uncertainty/#How-it-works","page":"Uncertainty quantification","text":"After image deformation has converged, the two deformed windows should show the same particle pattern; any residual asymmetry between them is caused by noise, out-of-plane loss, and local gradients — exactly the effects that perturb the correlation peak. The method measures, pixel by pixel, the asymmetry statistics of the correlation-difference terms and propagates them to the displacement estimate, yielding a per-window standard deviation for u and v separately.","title":"How it works"},{"category":"section","location":"explanation/uncertainty/#What-it-needs","page":"Uncertainty quantification","text":"A converged multi-pass schedule. The derivation assumes the correlation peak sits at nearly zero residual displacement, so the estimate is computed on the final pass only and is meaningful only after the predictor–corrector iteration has settled. Repeat the final window size, e.g. multipass_parameters([32, 16, 16]; uncertainty = true, ...), or iterate the final pass to convergence with final = (max_iterations = 3,) (see iterative passes) — an iterating final pass estimates the uncertainty once, from the deformed windows of its last sweep.\nModerate noise. The method is accurate for uncertainties up to about 0.3 px; beyond that (or when the window has no usable correlation signal) the fields hold NaN.","title":"What it needs"},{"category":"section","location":"explanation/uncertainty/#What-the-numbers-mean","page":"Uncertainty quantification","text":"The estimate describes the random error of the correlation measurement at that window — nothing more:\n\nSystematic errors (peak locking, calibration bias) are invisible to it; diagnose those with peak_locking and, for ground-truthed cases, error_statistics.\nThe estimate is not updated when validation replaces or substitutes a vector: it describes the original correlation, not the replacement.\nWindows that are nearly outliers legitimately report very large σ. When comparing uncertainty against a reference error, use medians over non-outlier vectors rather than means — a handful of near-outlier windows otherwise dominates.\n\nOn synthetic noise sweeps, the median estimate tracks the measured root-mean-square (RMS) error within about ±25% up to 20% image noise.","title":"What the numbers mean"},{"category":"section","location":"explanation/uncertainty/#Ensemble-pooling","page":"Uncertainty quantification","text":"In run_piv_ensemble, the per-window statistics are summed across all pairs — the ensemble correlation plane is itself such a sum — so the reported uncertainty describes the ensemble-mean vector and shrinks as pairs are added. Like ensemble correlation itself, this assumes the same displacement in every pair; genuine pair-to-pair flow fluctuation is not captured. Quantify fluctuation with field_statistics over single-pair results instead.","title":"Ensemble pooling"},{"category":"section","location":"explanation/uncertainty/#Execution-precision-on-GPU-backends","page":"Uncertainty quantification","text":"The KernelAbstractions (KA) family of backends computes the same additive statistics on its execution device and always accumulates them in Float64, including for Float32 images. An iterative pass runs one uncertainty-quantification sweep over the final device-resident warped windows; an ensemble keeps the pooled statistics on the device until final analysis. The CPU finalizer remains the correctness reference. Consumer GPUs with weak Float64 throughput may spend more time on UQ than on correlation; see Run PIV on a GPU for benchmarking guidance.","title":"Execution precision on GPU backends"},{"category":"section","location":"explanation/uncertainty/#Stereo-propagation","page":"Uncertainty quantification","text":"run_piv_stereo propagates the two cameras' per-window estimates through the same least-squares operator used for the (u, v, w) reconstruction, assuming independent per-camera errors, into uncertainty_u / uncertainty_v / uncertainty_w in world units.","title":"Stereo propagation"},{"category":"section","location":"explanation/uncertainty/#Cheap-proxies","page":"Uncertainty quantification","text":"Every result also carries two always-on quality indicators: peak_ratio (primary-to-secondary correlation peak ratio; higher is more reliable) and correlation_moment (peak second moment; lower is sharper). They rank vectors well but are not calibrated uncertainties.","title":"Cheap proxies"},{"category":"section","location":"reference/preprocessing/#Preprocessing","page":"Preprocessing","text":"Image conditioning before correlation: background removal, intensity capping, high-pass filtering, and contrast equalization, plus affine registration utilities. The mutating forms (f!) are the implementations and operate in place on floating-point buffers; the allocating names accept any real-valued matrix. See the preprocessing how-to for chaining recipes.\n\nPages = [\"preprocessing.md\"]","title":"Preprocessing"},{"category":"section","location":"reference/preprocessing/#Image-conditioning","page":"Preprocessing","text":"","title":"Image conditioning"},{"category":"section","location":"reference/preprocessing/#Registration-and-warping","page":"Preprocessing","text":"","title":"Registration and warping"},{"category":"method","location":"reference/preprocessing/#Hammerhead.clahe!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"clahe(img; tiles=(8, 8), clip_limit=2.0, nbins=256) -> Matrix{Float64}\nclahe!(img; tiles=(8, 8), clip_limit=2.0, nbins=256) -> img\n\nContrast-limited adaptive histogram equalization. The image is divided into tiles, each tile's histogram is clipped at clip_limit times the uniform bin count (excess redistributed) and converted to a CDF mapping; pixel values are remapped with bilinear interpolation between the four surrounding tile mappings. Output is in [0, 1]. Standard preprocessing for unevenly illuminated PIV recordings. The mutating version remaps the floating-point img in place.\n\n\n\n\n\n","title":"Hammerhead.clahe!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.clahe-Tuple{AbstractMatrix{<:Real}}","page":"Preprocessing","text":"clahe(img; tiles=(8, 8), clip_limit=2.0, nbins=256) -> Matrix\n\nAllocating form of clahe!: returns a new floating-point matrix and leaves img untouched.\n\n\n\n\n\n","title":"Hammerhead.clahe"},{"category":"method","location":"reference/preprocessing/#Hammerhead.compute_background-Tuple{Any}","page":"Preprocessing","text":"compute_background(images; method=:min) -> Matrix{Float64}\n\nEstimate a static background from an iterable of equally sized images: :min (pixel-wise minimum — robust for sparse particles over a static background) or :mean.\n\n\n\n\n\n","title":"Hammerhead.compute_background"},{"category":"method","location":"reference/preprocessing/#Hammerhead.highpass_filter!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"highpass_filter(img; sigma=3) -> Matrix{Float64}\nhighpass_filter!(img; sigma=3) -> img\n\nRemove low-frequency background (sheet inhomogeneity, glare) by subtracting a Gaussian blur of scale sigma pixels, clamping at zero. sigma should be a few times the particle image diameter so particles survive the filter. The mutating version overwrites the floating-point img; the blur itself requires one internal buffer.\n\n\n\n\n\n","title":"Hammerhead.highpass_filter!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.highpass_filter-Tuple{AbstractMatrix{<:Real}}","page":"Preprocessing","text":"highpass_filter(img; sigma=3) -> Matrix\n\nAllocating form of highpass_filter!: returns a new floating-point matrix and leaves img untouched.\n\n\n\n\n\n","title":"Hammerhead.highpass_filter"},{"category":"method","location":"reference/preprocessing/#Hammerhead.intensity_cap!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"intensity_cap(img; n_sigma=2) -> Matrix{Float64}\nintensity_cap!(img; n_sigma=2) -> img\n\nCap pixel intensities at median + n_sigma * std (Shavit et al. 2007), limiting the influence of overexposed particles and reflections on the correlation. The mutating version overwrites the floating-point img.\n\n\n\n\n\n","title":"Hammerhead.intensity_cap!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.intensity_cap-Tuple{AbstractMatrix{<:Real}}","page":"Preprocessing","text":"intensity_cap(img; n_sigma=2) -> Matrix\n\nAllocating form of intensity_cap!: returns a new floating-point matrix and leaves img untouched.\n\n\n\n\n\n","title":"Hammerhead.intensity_cap"},{"category":"method","location":"reference/preprocessing/#Hammerhead.invert_image!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"Invert intensities about their finite range (lo + hi - value).\n\n\n\n\n\n","title":"Hammerhead.invert_image!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.local_variance_normalize!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"local_variance_normalize!(img; sigma=3, epsilon=1e-3)\n\nSubtract a Gaussian local mean and divide by the local standard deviation. This is useful for multiplicative illumination/contrast variation; unlike CLAHE it preserves a linear standardized response, but it can amplify noise in flat areas, controlled by epsilon.\n\n\n\n\n\n","title":"Hammerhead.local_variance_normalize!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.percentile_stretch!-Tuple{AbstractMatrix{<:AbstractFloat}}","page":"Preprocessing","text":"Stretch the low and high intensity percentiles to [0,1], clipping outside.\n\n\n\n\n\n","title":"Hammerhead.percentile_stretch!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.subtract_background!-Tuple{AbstractMatrix{<:AbstractFloat}, AbstractMatrix{<:Real}}","page":"Preprocessing","text":"subtract_background(img, background) -> Matrix{Float64}\nsubtract_background!(img, background) -> img\n\nSubtract a background image (see compute_background), clamping the result at zero. The mutating version overwrites the floating-point img without allocating.\n\n\n\n\n\n","title":"Hammerhead.subtract_background!"},{"category":"method","location":"reference/preprocessing/#Hammerhead.subtract_background-Tuple{AbstractMatrix{<:Real}, AbstractMatrix{<:Real}}","page":"Preprocessing","text":"subtract_background(img, background) -> Matrix\n\nAllocating form of subtract_background!: returns a new floating-point matrix and leaves img untouched.\n\n\n\n\n\n","title":"Hammerhead.subtract_background"},{"category":"type","location":"reference/preprocessing/#Hammerhead.AffineTransform","page":"Preprocessing","text":"AffineTransform(A::AbstractMatrix, b::AbstractVector)\nAffineTransform()  # identity\n\n2D affine transform x' = A*x + b acting on [x, y] coordinates (x along columns, y along rows). A must be 2×2 and b of length 2.\n\n\n\n\n\n","title":"Hammerhead.AffineTransform"},{"category":"method","location":"reference/preprocessing/#Hammerhead.calculate_manual_registration-Tuple{AbstractVector, AbstractVector}","page":"Preprocessing","text":"calculate_manual_registration(points_image, points_reference) -> AffineTransform\n\nLeast-squares affine transform mapping image coordinates to reference coordinates (x_ref = A*x_img + b). Both arguments are vectors of corresponding 2D points (tuples or 2-vectors, as (x, y)); at least 3 non-collinear pairs are required.\n\n\n\n\n\n","title":"Hammerhead.calculate_manual_registration"},{"category":"method","location":"reference/preprocessing/#Hammerhead.transform_vector_field-Union{Tuple{S}, Tuple{AbstractArray{<:Real}, AbstractArray{<:Real}, AbstractArray{<:Real}, AbstractArray{<:Real}, AffineTransform{S}}} where S<:Real","page":"Preprocessing","text":"transform_vector_field(x, y, u, v, tform::AffineTransform)\n\nTransform grid-point locations (x' = A*x + b) and vector components (u' = A*u) by tform. All four arrays must share the same shape, which is preserved. Returns (new_x, new_y, new_u, new_v).\n\n\n\n\n\n","title":"Hammerhead.transform_vector_field"},{"category":"method","location":"reference/preprocessing/#Hammerhead.warp_image-Union{Tuple{S}, Tuple{T}, Tuple{AbstractMatrix{T}, AffineTransform{S}}} where {T, S<:Real}","page":"Preprocessing","text":"warp_image(image, tform::AffineTransform) -> Matrix\n\nApply tform to image: the output at (row, col) samples the input at the inverse-transformed coordinate, using bilinear interpolation. Coordinates outside the input are filled with zero.\n\n\n\n\n\n","title":"Hammerhead.warp_image"},{"category":"section","location":"howto/validation/#Tune-validation","page":"Tune validation","text":"Goal: adjust outlier detection when the defaults flag too much (smooth fields peppered with false positives) or too little (spurious vectors surviving into your statistics).","title":"Tune validation"},{"category":"section","location":"howto/validation/#What-runs-by-default","page":"Tune validation","text":"Every run_piv pass applies, in order:\n\nUniversal outlier detection (UOD) (normalized median test, (Westerweel and Scarano, 2005)) — uod_threshold = 2.0, uod_neighborhood = 2 (a 5×5 neighborhood), noise floor epsilon = 0.1 px.\nPeak-ratio check — disabled by default (min_peak_ratio = 1.0).\nPeak substitution — flagged vectors are re-tested against their secondary/tertiary correlation peaks (n_peaks = 3); a locally consistent alternative is accepted as measured data and unflagged.\nLocal-median replacement — remaining flagged vectors are replaced (replace_outliers = true; intermediate multi-pass passes always replace). A pass with max_iterations > 1 then re-measures replaced vectors, re-deforming by the corrected field and re-correlating until it converges — see iterative passes.","title":"What runs by default"},{"category":"section","location":"howto/validation/#If-too-many-good-vectors-are-flagged","page":"Tune validation","text":"Raise uod_threshold (e.g. 2.0 → 3.0). Higher is less sensitive.\nKeep uod_neighborhood = 2. The 5×5 neighborhood exists because 3×3 falsely flags smooth gradients at field edges — shrinking it is rarely the right fix.\nDon't lower epsilon below ~0.1 px. It represents the physical subpixel noise floor; with near-zero epsilon, a uniform flow field gets flagged wholesale because the neighbor residuals are pure noise.","title":"If too many good vectors are flagged"},{"category":"section","location":"howto/validation/#If-spurious-vectors-survive","page":"Tune validation","text":"Enable the peak-ratio check: min_peak_ratio = 1.3 is a reasonable starting point (a peak barely taller than the noise peak is unreliable).\nAdd validators via the validation parameter, as Symbol => value specs or validator objects:\n\nparams = PIVParameters(\n    min_peak_ratio = 1.3,\n    validation = (\n        :velocity_magnitude => (max = 12,),         # px per frame interval\n        :correlation_moment => 4.0,                 # peak too broad\n    ),\n)","title":"If spurious vectors survive"},{"category":"section","location":"howto/validation/#The-validators,-in-one-place","page":"Tune validation","text":"Every validator can be given either as a validator object or as a Symbol => value pair spec in the validation tuple. The full set:\n\nValidator Flags a vector when… Pair spec Default / recommended\nUniversalOutlierValidator its normalized median residual vs. its neighbors exceeds threshold (the default UOD; runs even without a spec) :uod => (threshold = 2.0, neighborhood_size = 2, epsilon = 0.1) (alias :universal_outlier; only threshold is required) threshold = 2.0, neighborhood_size = 2 (5×5), epsilon = 0.1 px\nPeakRatioValidator its correlation peak ratio is below threshold (NaN too) :peak_ratio => threshold off (1.0); 1.3 to enable\nCorrelationMomentValidator its correlation peak is broader than threshold (NaN too) :correlation_moment => threshold data-dependent; e.g. 4.0\nVelocityMagnitudeValidator its displacement magnitude is outside [min, max] px (NaN too) :velocity_magnitude => (min = 0, max = 50) (min defaults to 0) set max to your physical limit\n\nThe UOD and peak-ratio checks have dedicated PIVParameters keywords (uod_threshold/uod_neighborhood, min_peak_ratio); the rest go in the validation tuple. See validate_vectors! for how a pipeline is applied.\n\nFor time-resolved sequences, add validate_temporal! after processing: a vector consistent with its spatial neighbors can still be exposed by the point's time history.","title":"The validators, in one place"},{"category":"section","location":"howto/validation/#Keep-or-replace-outliers?","page":"Tune validation","text":"replace_outliers = false (final pass) leaves flagged vectors holding their measured values, with result.outliers telling you which they are — appropriate when you want to apply your own replacement or reject them outright. For a smoothness-based fill that also interpolates gaps, use smoothn (Garcia, 2010) with the outlier/mask flags as weights:\n\nw = .!(result.outliers .| result.mask)\nu_smooth = smoothn(result.u; weights = w).z","title":"Keep or replace outliers?"},{"category":"section","location":"howto/validation/#Judge-the-tuning","page":"Tune validation","text":"Count flags, and look at where they are:\n\ncount(result.outliers) / length(result.outliers)   # flag fraction\n\nA well-tuned setup flags a few percent, concentrated where the image data is genuinely poor (edges, reflections, dropout), not tracing the contours of real flow structures. If flags follow your shear layers, validation is too tight.","title":"Judge the tuning"},{"category":"section","location":"tutorials/stereo_real/#Stereo-on-a-real-recording:-vortex-ring","page":"Stereo on a real recording: vortex ring","text":"The synthetic stereo tutorial walked the full stereo chain — plate to three-component field — with ground truth available at every step. This tutorial repeats that chain on a real experiment and teaches what only real data can: what calibration residuals from a physical plate look like, how to judge self-calibration when its convergence flag says no, and how to read a three-component measurement when nobody knows the right answer.\n\nThe data is case E of the 4th International Particle Image Velocimetry (PIV) Challenge (Kähler et al., 2016): a vortex ring at Reynolds number Re approx 2300, recorded time-resolved by R. R. La Foy in the AEThER laboratory at Virginia Tech. We use the two cameras distributed to the Challenge participants: camera 1 views the light sheet head-on, camera 3 is pitched about 25° about the horizontal axis — without a Scheimpflug adapter, so its particle images are kept in focus by a stopped-down aperture instead. Both are 1024 × 1024 px high-speed cameras running at 1000 Hz (consecutive frames are 1 ms apart). A subset ships with Hammerhead's test suite: the calibration plate at three of the seven traverse positions, and two consecutive particle frames per camera.\n\nusing Hammerhead\nusing Statistics: median\n\ndir = joinpath(pkgdir(Hammerhead), \"test\", \"reference_images\", \"E\")\nplate(cam, k) = load_image(joinpath(dir, \"E_camera_$(cam)_z_$(k).png\"))\nframe(cam, f) = joinpath(dir, \"E_camera_$(cam)_frame_000$(f).png\")\n\nusing CairoMakie\n\nlet\n    fig = Figure(size = (720, 380))\n    for (i, cam) in enumerate((1, 3))\n        ax = Axis(fig[1, i]; title = \"camera $cam, plate at z = 0\",\n                  yreversed = true, aspect = DataAspect())\n        image!(ax, plate(cam, 4)'; colormap = :grays)\n    end\n    fig\nend\n\nA LaVision Type #21 two-level plate: dots every 15 mm on each level, the back level 3 mm behind the front, a filled square anchoring the origin and a filled triangle for orientation. Camera 3's higher magnification — one of this case's deliberate difficulties — means it sees far fewer dots than camera 1.","title":"Stereo on a real recording: vortex ring"},{"category":"section","location":"tutorials/stereo_real/#Detect-the-plates-and-calibrate","page":"Stereo on a real recording: vortex ring","text":"The plate was traversed through seven Z positions at 1 mm spacing; the committed subset keeps planes 1, 4, and 7 (z = −3, 0, +3 mm) — the minimum three planes the default Soloff model needs, spanning the full traverse range. Detection is identical to the synthetic tutorial; the origin_offset (the origin dot sits 30 mm right of and 7.5 mm above the square marker) comes from the experiment's documentation and is what makes both cameras agree on the world frame:\n\nzs = [-3.0, 0.0, 3.0]\ndetect(img) = detect_calibration_grid(img; spacing = 15.0,\n    two_level = true, level_separation = 3.0, origin_offset = (30.0, 7.5))\n\ngrids1 = [detect(plate(1, k)) for k in (1, 4, 7)]\ngrids3 = [detect(plate(3, k)) for k in (1, 4, 7)]\n(cam1 = [length(g.pixels) for g in grids1],\n cam3 = [length(g.pixels) for g in grids3],\n markers = all(g -> g.square !== nothing, [grids1; grids3]))\n\nCamera 1 sees the same 61 dots on every plane; camera 3 sees 28, and loses a few on the last plane as the traverse carries edge dots out of view — normal, the fit simply uses what each plane provides. Now one Soloff model per camera, and the fit-quality check:\n\ncam1 = calibrate_camera(grids1, zs)\ncam3 = calibrate_camera(grids3, zs)\n(quality1 = calibration_quality(cam1, grids1, zs),\n quality3 = calibration_quality(cam3, grids3, zs))\n\nOn the synthetic plates of the previous tutorial these residuals were in the thousandths of a pixel. Here they are ~0.7 and ~1.1 px root-mean-square (RMS) — and that is fine. Re-detecting the plate at different positions reproduces each dot's residual to 0.15–0.3 px: the residuals are dominated by where the dots actually sit on the physical plate (manufacturing tolerance), not by detection noise or model error. Chasing them with a richer model would fit the plate's defects, not the optics. The stereo-rig how-to discusses what residuals to expect and when to worry.","title":"Detect the plates and calibrate"},{"category":"section","location":"tutorials/stereo_real/#A-common-grid-from-the-stereo-overlap","page":"Stereo on a real recording: vortex ring","text":"Both cameras must be dewarped onto one world-plane grid. On real data the grid's extent is not a design parameter you know in advance — it is whatever region both cameras actually see. common_dewarp_grid constructs it: it projects each camera's image border to the z = 0 world plane, intersects the footprints (camera 3's smaller footprint sets the limits here), and — with spacing = :auto — matches the coarsest camera's resolution so dewarping discards no information. Its y range comes out descending, so the dewarped image displays upright (world +Y up); PIV downstream is orientation-agnostic because dv * step(y) carries the sign (see Stereo geometry and self-calibration).\n\ngrid = common_dewarp_grid((cam1, cam3), (1024, 1024), 0.0)\ndw1 = ImageDewarper(cam1, grid, (1024, 1024))\ndw3 = ImageDewarper(cam3, grid, (1024, 1024))\ngrid\n\nThe union mask dw1.mask .| dw3.mask marks grid nodes either camera cannot see; because the grid is the overlap bounding box (not the exact overlap polygon), a few corner regions are masked. Everything downstream excludes them automatically. Here is the same instant through both cameras, dewarped onto the shared grid:\n\nA1 = load_image(frame(1, 50))\nA3 = load_image(frame(3, 50))\n\nlet\n    fig = Figure(size = (720, 400))\n    for (i, (cam, dw, img)) in enumerate(((1, dw1, A1), (3, dw3, A3)))\n        ax = Axis(fig[1, i]; title = \"camera $cam, frame 50, dewarped\",\n                  yreversed = true, aspect = DataAspect())\n        image!(ax, dewarp(dw, img)'; colormap = :grays, colorrange = (0, 0.5))\n    end\n    fig\nend\n\nThe two views now share pixel-for-pixel geometry — the vortex ring's seeded disk sits in the same place in both. They are not identical: camera 3's out-of-focus, non-Scheimpflug particle images are visibly blurrier, and any residual pattern shift between the two is exactly the disparity that self-calibration is about to measure.","title":"A common grid from the stereo overlap"},{"category":"section","location":"tutorials/stereo_real/#Self-calibration,-judged-like-a-practitioner","page":"Stereo on a real recording: vortex ring","text":"The plate never sits exactly in the light sheet. self_calibrate measures the camera-1-vs-camera-3 disparity on same-instant frames, triangulates where the sheet really is, and rigidly moves both camera models onto it (Wieneke, 2005). Wieneke recommends ensembling 5–50 instants; on seeding this dense, the two committed frames already give a clean disparity correlation:\n\ndw1c, dw3c, report = self_calibrate([frame(1, 50), frame(1, 51)],\n                                    [frame(3, 50), frame(3, 51)],\n                                    dw1, dw3; keep_disparity_maps = true)\nreport\n\nOn the synthetic rig this converged below 0.05 px. Here it reports not converged — and that is the single most important lesson of this tutorial: on real data the default tolerance is not the right yardstick. Look at the per-pass numbers instead:\n\n[(disparity_rms = round(p.disparity_rms; digits = 3),\n  triangulation_rms = round(p.triangulation_rms; digits = 3),\n  plane = p.plane === nothing ? nothing :\n          map(x -> round(x; sigdigits = 3), p.plane))\n for p in report.passes]\n\nThe first pass found a systematic ~2.8 px disparity and triangulated it to a sheet sitting about 0.7 mm behind the plate's z = 0, tilted a quarter of a degree. One correction removes it; the later passes find essentially no further plane (offsets of a few micrometers) while the disparity RMS stalls near 0.5 px. That residual is not misalignment — it is decorrelation noise: the light sheet has thickness, the two cameras weight particles across it differently, and no rigid transform can remove that. Two diagnostics separate \"noise floor\" from \"still misaligned\":\n\nThe signed median disparity. Misalignment is systematic; noise is not. The median magnitude stays at the noise floor, but the signed component medians collapse:\n\nmaps = report.disparity_maps\n[begin\n     ok = .!(m.outliers .| m.mask)\n     (median_du = round(median(m.u[ok]); digits = 2),\n      median_dv = round(median(m.v[ok]); digits = 2))\n end for m in maps]\n\nA 2.8 px systematic v-disparity became a few hundredths of a pixel — two orders of magnitude below the residual RMS. The correction is done.\n\nThe triangulation RMS (~0.1 px here): how well the disparity vectors agree with any plane. If it were large while the final disparity stayed small, the problem would be the calibration itself, not the sheet position.","title":"Self-calibration, judged like a practitioner"},{"category":"section","location":"tutorials/stereo_real/#Reconstruct-three-components","page":"Stereo on a real recording: vortex ring","text":"passes = multipass_parameters([64, 32, 32];\n    padding = true, apodization = :gauss, uncertainty = true)\n\nB1 = load_image(frame(1, 51))\nB3 = load_image(frame(3, 51))\nstereo = run_piv_stereo(A1, B1, A3, B3, dw1c, dw3c, passes;\n    scale = PhysicalScale(dt = 1e-3, length_unit = \"mm\", time_unit = \"s\"))\n\nThe StereoPIVResult's grid and measured arrays are world-side: positions in mm and displacements in mm per frame interval. The attached PhysicalScale records the 1 ms interval; physical(stereo) converts the displacements and uncertainties to velocities in mm/s. Its mask and outliers are the unions of the per-camera flags: a stereo vector is only as good as both two-component (2C) measurements underneath it, and the per-camera results remain available as stereo.cam1 / stereo.cam2.\n\nsel = .!(stereo.mask .| stereo.outliers)\n(vectors = size(stereo.u), valid = count(sel),\n outliers = count(stereo.outliers .& .!stereo.mask))\n\nlet\n    fig = Figure(size = (620, 520))\n    ax = Axis(fig[1, 1]; title = \"vortex ring: arrows (u, v), color w (mm)\",\n              xlabel = \"X (mm)\", ylabel = \"Y (mm)\", aspect = DataAspect())\n    w = copy(stereo.w); w[.!sel] .= NaN\n    hm = heatmap!(ax, stereo.x, stereo.y, permutedims(w);\n                  colormap = :balance, colorrange = (-0.15, 0.15))\n    u = copy(stereo.u); u[.!sel] .= NaN\n    v = copy(stereo.v); v[.!sel] .= NaN\n    plot_vector_field!(ax, stereo.x, stereo.y, u, v)\n    Colorbar(fig[1, 2], hm)\n    fig\nend\n\nThe section through the ring shows the two counter-rotating cores, and the out-of-plane component w — invisible to either camera alone — is comparable to the in-plane motion. With no ground truth, the quality evidence is the same as in the planar real-data tutorial, now propagated into world units:\n\nmed(f) = round(1000 * median(filter(isfinite, f[sel])); digits = 1)  # mm → µm\n(σu = med(stereo.uncertainty_u), σv = med(stereo.uncertainty_v),\n σw = med(stereo.uncertainty_w))\n\nThe in-plane components resolve to a few micrometers; w is about four times worse. That ratio is pure geometry: with one head-on camera and one at 25°, a unit of out-of-plane motion produces less than half a unit of image-plane disparity, so the reconstruction amplifies the correlation noise. Steeper viewing angles would shrink it — the textbook ±45° rig has a ratio near 1. Reading σw/σu from the result tells you what your rig, not your correlator, can resolve.","title":"Reconstruct three components"},{"category":"section","location":"tutorials/stereo_real/#What-did-self-calibration-actually-buy?","page":"Stereo on a real recording: vortex ring","text":"A natural check: run the same reconstruction with the uncorrected dewarpers and compare.\n\nstereo0 = run_piv_stereo(A1, B1, A3, B3, dw1, dw3, passes)\nboth = sel .& .!(stereo0.mask .| stereo0.outliers)\nΔ(a, b) = round(1000 * median(abs.(a[both] .- b[both])); digits = 1)   # µm\n(Δu = Δ(stereo.u, stereo0.u), Δv = Δ(stereo.v, stereo0.v),\n Δw = Δ(stereo.w, stereo0.w))\n\nThe vectors barely move — the median change in every component is below its own uncertainty. That is expected, and it reframes what the correction is for: a 0.7 mm, 0.25° plate-to-sheet misregistration hardly perturbs the displacement values, but it means the uncorrected result reports them at the wrong place — a coordinate system attached to the plate, not to the fluid that was actually illuminated, with the two cameras' interrogation windows sampling regions offset by the 2.8 px disparity. Self-calibration buys you the coordinate system: the measurement plane where the light sheet really is, and both cameras correlating the same fluid. On flows with strong gradients that window misregistration also biases the vectors themselves; here the check confirms the geometry was the main casualty.","title":"What did self-calibration actually buy?"},{"category":"section","location":"tutorials/stereo_real/#Where-to-go-next","page":"Stereo on a real recording: vortex ring","text":"What residuals to expect from physical plates, and the full rig-calibration checklist: Calibrate a real stereo rig.\nThe geometry behind disparity, triangulation, and the plane fit: Stereo geometry and self-calibration.\nWhat the per-vector σ does and does not cover: Uncertainty quantification.\nTime-resolved sequences (this recording has 100 frames): Batch processing and Ensemble correlation.\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"tutorials/first_vector_field/#Your-first-vector-field","page":"Your first vector field","text":"This tutorial takes you from a pair of particle images to a validated, uncertainty-quantified velocity field. Everything is synthetic and self-contained: we generate the images ourselves, so we know the true flow and can check every claim the analysis makes.","title":"Your first vector field"},{"category":"section","location":"tutorials/first_vector_field/#Generate-a-synthetic-image-pair","page":"Your first vector field","text":"Particle image velocimetry (PIV) measures displacement between two images of a flow seeded with small tracer particles. A short light pulse freezes the particles in each exposure; their pattern shifts between exposures as the fluid moves. Hammerhead's SyntheticData submodule renders such image pairs from a prescribed velocity field, displacing each particle by its local velocity times Delta t — so the ground truth is known exactly.\n\nA velocity field is any function (x, y, z, t) -> (u, v, w). Ready-made fields exist (vortex_flow, shear_flow, linear_flow); here we write a Lamb–Oseen vortex — a viscous vortex with a smooth core — by hand:\n\nusing Hammerhead\nusing Hammerhead.SyntheticData\nusing Random\n\ncenter = (128.0, 128.0)\nrc = 40.0        # core radius, px\nΓ = 1200.0       # circulation, px² per frame interval (≈3 px peak displacement)\n\nfunction flow(x, y, z, t)\n    dx, dy = x - center[1], y - center[2]\n    r² = dx^2 + dy^2\n    k = r² < 1e-9 ? Γ / (2π * rc^2) : Γ / (2π * r²) * (1 - exp(-r² / rc^2))\n    return (-k * dy, k * dx, 0.0)\nend\n\nrng = MersenneTwister(42)\nimgA, imgB, particles1, particles2 = generate_synthetic_piv_pair(\n    flow, (256, 256), 1.0;\n    particle_density = 0.05,     # particles per pixel\n    background_noise = 0.03,     # sensor noise level\n    z_range = (-1.0, 1.0),       # keep particles inside the laser sheet\n    rng,\n)\nsize(imgA), extrema(imgA)\n\nA quick look at the pair (with real data, you would start from load_image(\"frame_0001.tif\") instead):\n\nusing CairoMakie\n\nlet\n    fig = Figure(size = (720, 380))\n    for (i, (img, title)) in enumerate(((imgA, \"frame A\"), (imgB, \"frame B\")))\n        ax = Axis(fig[1, i]; title, yreversed = true, aspect = DataAspect())\n        image!(ax, img'; colormap = :grays)\n    end\n    fig\nend","title":"Generate a synthetic image pair"},{"category":"section","location":"tutorials/first_vector_field/#Run-a-basic-analysis","page":"Your first vector field","text":"run_piv tiles the images into interrogation windows, correlates each window pair, and refines every correlation peak to subpixel precision. With no configuration it runs a single pass of 32×32 windows at 50% overlap:\n\nresult = run_piv(imgA, imgB)\n\nAn interrogation window is a small image region containing several particles. Correlation finds the shift that best aligns its particle pattern between frames, so each window produces one representative displacement vector rather than one vector per particle.\n\nThe PIVResult holds the interrogation grid (x along columns, y along rows, in pixels) and the displacement fields (u along x, v along y — a particle at (row, col) in frame A is found at (row + v, col + u) in frame B). The Makie extension plots it directly:\n\nplot_vector_field(result)","title":"Run a basic analysis"},{"category":"section","location":"tutorials/first_vector_field/#Multi-pass-with-image-deformation","page":"Your first vector field","text":"One pass leaves accuracy on the table. A multi-pass schedule starts with large windows, then uses each pass's validated field to deform the images before the next, finer pass — so late passes only measure a small residual and can afford small windows. Two more switches, padding and apodization, remove the systematic bias of plain fast Fourier transform (FFT) correlation (see Correlation accuracy). This is the recommended configuration for real work:\n\npasses = multipass_parameters([64, 32, 16, 16];\n    padding = true,\n    apodization = :gauss,\n    uncertainty = true,     # per-vector uncertainty, estimated on the final pass\n)\nresult = run_piv(imgA, imgB, passes)\n\nplot_vector_field(result)\n\nTwice the spatial resolution (16 px windows instead of 32), and — as we're about to verify — much better accuracy. Note the schedule [64, 32, 16, 16]: repeating the final window size adds a convergence sweep, which the uncertainty estimator requires (it assumes the deformation has converged).","title":"Multi-pass with image deformation"},{"category":"section","location":"tutorials/first_vector_field/#Check-against-the-ground-truth","page":"Your first vector field","text":"The generator displaces each particle by its velocity at the launch point, while symmetric image deformation attributes each measured vector to the midpoint of the particle trajectory (that midpoint attribution is what makes the scheme second-order accurate — see Multi-pass interrogation). To compare like with like, we evaluate the reference velocity at the launch point x - d/2, and hand the reference fields to error_statistics:\n\nmidpoint_reference(r) = (\n    [flow(x - r.u[i, j] / 2, y - r.v[i, j] / 2, 0.0, 0.0)[1]\n     for (i, y) in enumerate(r.y), (j, x) in enumerate(r.x)],\n    [flow(x - r.u[i, j] / 2, y - r.v[i, j] / 2, 0.0, 0.0)[2]\n     for (i, y) in enumerate(r.y), (j, x) in enumerate(r.x)],\n)\n\nu_ref, v_ref = midpoint_reference(result)\nerr = error_statistics(result, u_ref, v_ref)\n(bias_u = err.bias_u, rms_u = err.rms_u, rms_v = err.rms_v, n = err.n)\n\nAbout 0.03 pixels (px) of root-mean-square (RMS) error and negligible bias over the whole field — this is what the padded, apodized, multi-pass configuration is for. For comparison, plain un-padded single-pass correlation carries a systematic bias of ~0.15 px on its own.","title":"Check against the ground truth"},{"category":"section","location":"tutorials/first_vector_field/#Per-vector-uncertainty","page":"Your first vector field","text":"With uncertainty = true, the final pass estimates each vector's random error from correlation statistics (Wieneke 2015) into uncertainty_u / uncertainty_v. The median estimate should sit at the noise-driven share of the root-mean-square error we just measured — without ever seeing the ground truth:\n\nusing Statistics: median\n\nvalid = .!(result.outliers .| result.mask)\nσu = filter(isfinite, result.uncertainty_u[valid])\n(median_uncertainty_u = median(σu), measured_rms_u = err.rms_u)\n\nThe estimator captures the random error of each correlation; the remaining gap to the measured RMS is residual deformation error that no per-window estimator can see. See Uncertainty quantification for what the numbers mean and when to trust them.","title":"Per-vector uncertainty"},{"category":"section","location":"tutorials/first_vector_field/#Outliers,-validation,-and-masking","page":"Your first vector field","text":"So far the data was clean and validation had nothing to do (count(result.outliers) == 0). Real recordings are not so kind. Let's simulate a saturated reflection — a bright static patch in both frames:\n\nimgA_refl, imgB_refl = copy(imgA), copy(imgB)\nfor img in (imgA_refl, imgB_refl)\n    img[97:144, 41:104] .= 1.0\nend\nresult_refl = run_piv(imgA_refl, imgB_refl, passes)\ncount(result_refl.outliers)\n\nA static reflection correlates perfectly with itself, producing confident zero vectors that disagree with their neighbors — exactly what the default validation (universal outlier detection plus peak-ratio checks) is built to catch. Flagged vectors are replaced by the local median of their valid neighbors (replace_outliers = true), and the flag tells you which values are interpolated rather than measured:\n\nplot_vector_field(result_refl)\n\nValidation is damage control, not a fix: windows partially covering the patch produce subtly biased vectors that can survive the tests. Since we know where the reflection is, the right tool is a mask — the region then produces no vectors at all and cannot contaminate its neighbors' validation, replacement, or statistics:\n\nmask = falses(size(imgA))\nmask[97:144, 41:104] .= true\nresult_masked = run_piv(imgA_refl, imgB_refl, passes; mask)\n\nu_ref, v_ref = midpoint_reference(result_masked)\nerr_masked = error_statistics(result_masked, u_ref, v_ref)\n(rms_u = err_masked.rms_u,\n outliers = count(result_masked.outliers),\n masked = count(result_masked.mask))\n\nFull accuracy is restored around the (now vector-free) masked region. result.mask records the dropped windows — deliberately kept distinct from result.outliers (see The masking model):\n\nplot_vector_field(result_masked)","title":"Outliers, validation, and masking"},{"category":"section","location":"tutorials/first_vector_field/#Where-to-go-next","page":"Your first vector field","text":"The same workflow on a real wind-tunnel recording, where there is no ground truth to check against: the real-data tutorial.\nReal image files: load_image and the batch-processing guide.\nPolygon and image-file masks: the masking guide.\nWhen the defaults flag too much or too little: Tune validation.\nNoisy recordings: the preprocessing and ensemble-correlation guides.\nTwo cameras: the stereo tutorial.\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"reference/ensemble/#Ensemble-and-statistics","page":"Ensemble and statistics","text":"Ensemble (sum-of-correlation) particle image velocimetry (PIV) for recordings with low signal-to-noise ratio (SNR), time-series statistics over planar and stereo result sequences, temporal validation, accuracy diagnostics, and spectra. See the ensemble how-to. GPU backends keep summed planes and uncertainty statistics device-resident; setup and memory sizing are covered in Run PIV on a GPU.\n\nPages = [\"ensemble.md\"]","title":"Ensemble and statistics"},{"category":"section","location":"reference/ensemble/#Ensemble-correlation","page":"Ensemble and statistics","text":"","title":"Ensemble correlation"},{"category":"section","location":"reference/ensemble/#Time-series-statistics-and-diagnostics","page":"Ensemble and statistics","text":"","title":"Time-series statistics and diagnostics"},{"category":"method","location":"reference/ensemble/#Hammerhead.run_piv_ensemble-Tuple{AbstractVector, Union{PIVParameters, AbstractVector{PIVParameters}}}","page":"Ensemble and statistics","text":"run_piv_ensemble(pairs, params = PIVParameters(); kwargs...) -> PIVResult\nrun_piv_ensemble(pairs; effort = :low/:medium/:high, kwargs...) -> PIVResult\n\nEnsemble PIV over a sequence of image pairs: each interrogation window's correlation planes are summed across all pairs and the displacement peak is located once on the ensemble plane, so a peak too weak to detect in any single pair emerges from the average. Assumes statistically stationary flow; the result is the ensemble-mean displacement field.\n\npairs is as in run_piv_sequence (2-tuples of file paths and/or matrices; paths are reloaded once per pass). params may be a single PIVParameters, an explicit multi-pass schedule, or omitted in favor of effort = :low, :medium, or :high; effort keywords follow run_piv, except that ensemble :high repeats the final window size because this driver ignores per-pass max_iterations. With a multi-pass schedule the previous pass's ensemble field acts as a shared deformation predictor for every pair. peak_ratio and correlation_moment describe the ensemble planes.\n\nWith uncertainty = true the correlation-statistics estimator (Wieneke 2015, see PIVParameters) pools its per-window sums over all pairs — the ensemble correlation plane is itself such a sum — so uncertainty_u / uncertainty_v describe the noise-driven uncertainty of the ensemble-mean vector and shrink as pairs are added. Like ensemble correlation itself, this assumes the displacement is the same in every pair: genuine pair-to-pair flow fluctuation is not captured (its coherent window-wide shifts violate the estimator's short-range pixel-covariance assumption); quantify it with field_statistics over single-pair results instead.\n\nKeyword arguments: threaded, predictor_smoothing, mask, mask_threshold, backend, and scale (attach a PhysicalScale to the result) as in run_piv; preprocess, image_type, progress as in run_piv_sequence. On the KA-family backends (:ka and the device selectors) the summed correlation planes stay resident on the device — only the final vector grid returns to the host. With uncertainty = true, the additive Float64 statistics also remain device-resident until finalization. Phase correlation, subpixel_method = :gauss2d, and retained correlation planes still require backend = :cpu.\n\n\n\n\n\n","title":"Hammerhead.run_piv_ensemble"},{"category":"method","location":"reference/ensemble/#Hammerhead.error_statistics-Tuple{PIVResult, Any, Any}","page":"Ensemble and statistics","text":"error_statistics(result, u_ref, v_ref; include_invalid = false) -> NamedTuple\n\nCompare a PIV field against a known reference — the bias-error tooling for ground-truthed cases (e.g. PIV Challenge 4F solid-body rotation). u_ref/v_ref are grid-sized arrays or functions (x, y) -> value evaluated at the interrogation grid points. Returns (; err_u, err_v, bias_u, bias_v, rms_u, rms_v, n): signed error fields (NaN where invalid) plus mean (bias) and RMS errors over the n valid vectors (finite, unmasked, and unflagged unless include_invalid).\n\n\n\n\n\n","title":"Hammerhead.error_statistics"},{"category":"method","location":"reference/ensemble/#Hammerhead.field_statistics-Tuple{AbstractVector{<:PIVResult}}","page":"Ensemble and statistics","text":"field_statistics(results; include_invalid = false) -> NamedTuple\n\nPointwise temporal statistics over a sequence of same-grid PIVResults: returns (x, y, mean_u, mean_v, rms_u, rms_v, reynolds_uv, count), where rms_u/rms_v are the RMS of the fluctuating components and reynolds_uv is the Reynolds shear-stress correlation mean(u'v') (denominator n, the per-point valid-sample count in count). Outlier-flagged vectors, masked windows, and non-finite entries are excluded unless include_invalid; points with no valid samples are NaN.\n\n\n\n\n\n","title":"Hammerhead.field_statistics"},{"category":"method","location":"reference/ensemble/#Hammerhead.field_statistics-Tuple{AbstractVector{<:StereoPIVResult}}","page":"Ensemble and statistics","text":"field_statistics(results::AbstractVector{<:StereoPIVResult};\n                 include_invalid = false) -> NamedTuple\n\nPointwise statistics over a same-grid stereo sequence. Returns coordinates, means and fluctuating-component RMS values for u, v, and w, all six independent Reynolds-stress terms (reynolds_uu, reynolds_vv, reynolds_ww, reynolds_uv, reynolds_uw, reynolds_vw), and the per-point valid-sample count. Normal stresses equal the corresponding RMS squared. A sample is admitted only when all three components are finite and the vector is unmasked and, unless include_invalid, unflagged.\n\n\n\n\n\n","title":"Hammerhead.field_statistics"},{"category":"method","location":"reference/ensemble/#Hammerhead.peak_locking-Tuple{AbstractArray{<:Real}}","page":"Ensemble and statistics","text":"peak_locking(displacements; nbins = 21) -> (fractions, counts, index)\n\nDiagnose peak locking from the fractional parts f = x − round(x) ∈ [−0.5, 0.5) of a displacement sample (any array; non-finite entries are skipped). Returns the histogram bin centers and counts, plus a locking index comparing the sample density near integer displacements (|f| ≤ 0.1) with the density near half-integers (|f| ≥ 0.4): 0 for a uniform (locking free) distribution, → 1 as fractions pile up on integers, negative if they avoid them. NaN when there are no valid samples.\n\n\n\n\n\n","title":"Hammerhead.peak_locking"},{"category":"method","location":"reference/ensemble/#Hammerhead.power_spectrum-Tuple{AbstractVector{<:Real}}","page":"Ensemble and statistics","text":"power_spectrum(signal; dt = 1.0, window = :hann) -> (frequencies, psd)\n\nOne-sided power spectral density of a uniformly sampled series (sampling interval dt): the mean is removed, the taper applied (:hann or :none, power-normalized), and sum(psd) * Δf recovers the signal variance. Frequencies are in cycles per unit of dt. Extract a per-point velocity time series from a sequence with e.g. [r.u[i, j] for r in results]; with a PhysicalScale attached, pass dt = first(results).scale.dt for physical frequencies.\n\n\n\n\n\n","title":"Hammerhead.power_spectrum"},{"category":"method","location":"reference/ensemble/#Hammerhead.validate_temporal!-Tuple{AbstractVector{<:PIVResult}}","page":"Ensemble and statistics","text":"validate_temporal!(results; threshold = 3, epsilon = 0.1) -> results\n\nTemporal normalized-median test across a sequence of same-grid PIVResults: at each grid point the median and median absolute deviation (MAD) of the valid samples over time form a robust reference, and samples with |component − median| / (MAD + epsilon) > threshold in either component are flagged into their result's outliers. Points with fewer than 3 valid samples, masked windows, and already-flagged vectors are left untouched. Complements the spatial UOD: a vector consistent with its spatial neighbors can still be exposed as an outlier by the point's time history.\n\n\n\n\n\n","title":"Hammerhead.validate_temporal!"},{"category":"method","location":"reference/ensemble/#Hammerhead.validate_temporal!-Tuple{AbstractVector{<:StereoPIVResult}}","page":"Ensemble and statistics","text":"validate_temporal!(results::AbstractVector{<:StereoPIVResult};\n                   threshold = 3, epsilon = 0.1) -> results\n\nStereo form of the temporal normalized-median test. The robust reference is built independently for all three components and a sample is flagged when any component exceeds threshold. Masked, non-finite, already flagged, and short (fewer than three valid samples) histories are unchanged.\n\n\n\n\n\n","title":"Hammerhead.validate_temporal!"},{"category":"section","location":"howto/gpu/#Run-PIV-on-a-GPU","page":"Run PIV on a GPU","text":"Goal: run particle image velocimetry (PIV) on a graphics processing unit (GPU), select and validate the backend, and understand which work stays on the device, and avoid configurations where transfer or Float64 cost outweighs the acceleration.\n\nHammerhead's default backend = :cpu uses the central processing unit (CPU) and remains the complete reference implementation. GPU support is optional: loading AMDGPU.jl or CUDA.jl activates a package extension without adding either device stack to Hammerhead's normal installation.","title":"Run PIV on a GPU"},{"category":"section","location":"howto/gpu/#Install-and-select-a-backend","page":"Run PIV on a GPU","text":"Add the package for the device in the environment where Hammerhead runs:\n\nusing Pkg\nPkg.add(\"AMDGPU\")  # AMD/ROCm\n# Pkg.add(\"CUDA\")  # NVIDIA/CUDA\n\nLoad that package before selecting its backend:\n\nusing Hammerhead\nusing AMDGPU\n\nAMDGPU.functional() || error(\"AMDGPU cannot use the current ROCm installation\")\n\nresult = run_piv(imgA, imgB; effort = :high, backend = :amdgpu)\n\nFor NVIDIA, use using CUDA, check CUDA.functional(), and pass backend = :cuda. The built-in backend = :ka runs the same portable KernelAbstractions kernels on the CPU. It is a correctness and development tier, not a faster replacement for the FFTW-backed :cpu path.\n\nThe available selectors are:\n\nSelector Provider Intended use\n:cpu Hammerhead Default, complete FFTW reference path\n:ka Hammerhead Hardware-free test of the portable kernels\n:amdgpu AMDGPU.jl extension AMD GPUs through ROCm\n:cuda CUDA.jl extension NVIDIA GPUs through CUDA\n\nHammerhead currently supports AMDGPU.jl 2 and CUDA.jl 5 or 6. Driver and runtime compatibility is controlled by the device package. Follow the AMDGPU.jl setup guide or CUDA.jl installation guide when functional() is false. The AMD path is hardware-validated on an RX 6800 XT with ROCm 6.4; the CUDA path is hardware-validated on an RTX 2000 Ada with CUDA.jl 6.2.","title":"Install and select a backend"},{"category":"section","location":"howto/gpu/#Use-the-backend-across-drivers","page":"Run PIV on a GPU","text":"The selector is accepted by the planar, sequence, ensemble, and stereo drivers:\n\nsingle = run_piv(imgA, imgB, passes; backend = :amdgpu)\n\nseries = run_piv_sequence(pairs, passes;\n    backend = :amdgpu,\n    output = \"recording.jld2\",\n)\n\nmean_field = run_piv_ensemble(pairs, passes;\n    backend = :amdgpu,\n    progress = false,\n)\n\nstereo = run_piv_stereo(A1, B1, A2, B2, dw1, dw2, passes;\n    backend = :amdgpu,\n)\n\nrun_piv_sequence and run_piv_ensemble create a matching PIVWorkspace and reuse device buffers and fast Fourier transform (FFT) plans across pairs. For a hand-written loop, do the same explicitly:\n\nworkspace = piv_workspace(backend = :amdgpu)\nresults = [run_piv(a, b, passes; backend = :amdgpu, workspace)\n           for (a, b) in pairs]\n\nA workspace is backend-specific. Passing a CPU workspace to a GPU run, or the reverse, is an error.","title":"Use the backend across drivers"},{"category":"section","location":"howto/gpu/#Check-that-the-configuration-is-supported","page":"Run PIV on a GPU","text":"The KernelAbstractions (KA) family of backends (:ka, :amdgpu, and :cuda) currently implements:\n\nFeature :cpu KA-family\nCross-correlation yes yes\nPhase correlation yes yes, filtered normalized spectrum\n:gauss3 / :gauss9 subpixel fit yes yes\n:gauss2d subpixel fit yes no\nPadding and Gaussian apodization yes yes\nMulti-pass and iterative deformation yes yes\nCorrelation-statistics uncertainty yes yes, Float64 statistics\nMasked interrogation windows yes yes\nEnsemble correlation yes yes, device-resident accumulator\nkeep_correlation_planes = true yes no\n\nUnsupported combinations raise an ArgumentError before analysis rather than silently switching algorithms or falling back to the CPU. Use backend = :cpu when :gauss2d or retained correlation planes are required.\n\nStereo forwards the backend to both per-camera PIV analyses. Dewarping and three-component (3C) reconstruction remain on the CPU. Loading, preprocessing, validation, outlier replacement, smoothing, and result construction are also CPU work.","title":"Check that the configuration is supported"},{"category":"section","location":"howto/gpu/#Understand-device-residency","page":"Run PIV on a GPU","text":"For a non-deforming pass, each source pair is uploaded once. For deforming passes, Hammerhead performs the cubic B-spline prefilter on the CPU, stages the coefficients once, and keeps warped images on the device between deformation and correlation. Each correlation batch performs window gather, FFT, cross-power, shift/gain, peak analysis, and subpixel refinement on the device. Only packed per-window outputs return to the host.\n\nUncertainty statistics use Float64 even when images are Float32. An iterative pass computes them once from the last converged deformation. Ensemble runs accumulate both correlation planes and uncertainty statistics on the device across all pairs, then transfer final packed values.\n\nDevice correlation is internally tiled to bound FFT scratch memory. Ensemble planes, however, cover the complete vector grid for the duration of a pass. Their approximate footprint is:\n\nnumber of windows * correlation-plane rows * correlation-plane columns * sizeof(T)\n\nPadding doubles each plane dimension and therefore quadruples this part of the footprint. For example, a 2048x2048 image with 32-pixel windows, 16-pixel overlap, padding, and Float64 data has about 16,129 windows and a 64x64 plane per window, or roughly 504 MiB for the ensemble accumulator alone. Pair count increases runtime but not accumulator size.","title":"Understand device residency"},{"category":"section","location":"howto/gpu/#Decide-whether-GPU-execution-pays-off","page":"Run PIV on a GPU","text":"GPU execution is intended for large images, dense window grids, iterative deformation, and large ensembles. The CPU is usually faster for small images or low-effort single passes because device setup, launches, and transfers do not amortize. threaded controls CPU work; device backends process one logical window batch and do not fan it out over Julia host threads.\n\nFloat32 reduces image, FFT, and correlation-plane memory and is often much faster on consumer GPUs:\n\nresult = run_piv_sequence(pairs, passes;\n    backend = :amdgpu,\n    image_type = Float32,\n)\n\nThe returned fields follow the image precision, while uncertainty's internal statistics remain Float64. On GPUs with weak Float64 throughput, uncertainty may erase the speedup from correlation. Route only the uncertainty sweep to the threaded CPU without changing where correlation and deformation run:\n\nresult = run_piv(imgA, imgB; effort = :high,\n    backend = :amdgpu,\n    uncertainty_backend = :cpu,\n)\n\nThe policy is backend-independent: the same keyword works with :cuda, :amdgpu, :ka, and future device backends. The default uncertainty_backend = :same preserves fully device-resident execution. Neither choice changes the requested estimator or its Float64 statistics.\n\nDo not infer the best policy from the GPU vendor or product family. Benchmark the actual precision, image size, schedule, and mask on the target machine:\n\nrows = benchmark_piv_configurations(imgA, imgB;\n    effort = :high,\n    backends = (:cpu, :amdgpu),\n    samples = 3,\n)\n\nforeach(println, rows)\nbest = rows[argmin(getfield.(rows, :seconds))]\n\nThis measures all-CPU, all-device, and hybrid configurations, reusing a workspace within each configuration and reporting numerical deltas against the CPU reference. Replace :amdgpu with :cuda as appropriate.","title":"Decide whether GPU execution pays off"},{"category":"section","location":"howto/gpu/#Validate-a-device-and-benchmark-the-workload","page":"Run PIV on a GPU","text":"The repository includes a correctness script covering single-pass, multi-pass, masks, ensemble accumulation, and uncertainty:\n\njulia --project=/path/to/gpu-env -t 4 bench/gpu_validate.jl amdgpu\njulia --project=/path/to/gpu-env -t 4 bench/gpu_benchmarks.jl amdgpu\njulia --project=/path/to/gpu-env -t 4 bench/gpu_configurations.jl amdgpu 2048 Float32 high 3\n\nPass cuda for NVIDIA. GPU comparisons use scientific tolerances because device FFT and intrinsic order can differ from FFTW. Validate after changing drivers, device-package versions, or Julia versions, then benchmark the same image size, precision, pass schedule, mask, and pair count used in production.","title":"Validate a device and benchmark the workload"},{"category":"section","location":"howto/gpu/#Troubleshoot-common-failures","page":"Run PIV on a GPU","text":"Unsupported backend selector: load AMDGPU or CUDA in the same Julia process before calling Hammerhead. Installing the package alone does not activate its extension.\nfunctional() is false: repair the driver/runtime installation using the device package's diagnostics before running Hammerhead.\nUnsupported option error: switch the option or use backend = :cpu; Hammerhead does not silently change the requested analysis.\nWorkspace backend mismatch: rebuild it with piv_workspace(backend = backend).\nOut of device memory: use Float32, reduce overlap, avoid retained full grids where possible, or process a smaller region. For ensembles, padding and vector-grid density are the main accumulator-memory multipliers.\nGPU slower than CPU: benchmark a warmed run, reuse a workspace, and test a production-sized workload. Small transfers and weak device Float64 are common causes.","title":"Troubleshoot common failures"},{"category":"section","location":"explanation/conventions/#Coordinates,-signs,-and-units","page":"Coordinates, signs, and units","text":"Every quantity Hammerhead produces follows one consistent coordinate convention. This page states it once, so the other pages don't have to.","title":"Coordinates, signs, and units"},{"category":"section","location":"explanation/conventions/#Image-coordinates","page":"Coordinates, signs, and units","text":"Julia stores images as matrices indexed img[row, col]. Hammerhead names the two directions:\n\nx runs along columns (image-right is +x),\ny runs along rows (image-down is +y, because row numbers grow downward).\n\nPixel positions are written (x, y) in that order. The interrogation grid of a PIVResult uses the same frame: result.x holds window-center column coordinates and result.y window-center row coordinates, and field matrices such as result.u are indexed [iy, ix] — row index first, matching the image.","title":"Image coordinates"},{"category":"section","location":"explanation/conventions/#Displacement-sign-convention","page":"Coordinates, signs, and units","text":"A particle at (row, col) in the first image that is found at (row + dv, col + du) in the second image yields a positive displacement (du, dv):\n\nu is the x-displacement (along columns; positive = image-right),\nv is the y-displacement (along rows; positive = image-down).\n\nThis is the natural convention for image data, but note that +v points down in the usual display orientation. The Makie plotting extension (plot_vector_field) reverses the y-axis so vector plots match the image orientation.\n\nDisplacements are measured in pixels per frame interval, and the stored arrays of every result keep those measured units. Physical calibration is metadata: attach a PhysicalScale (pixel size, frame interval dt, and display unit labels) with the scale keyword of any driver or with with_scale — the arrays don't change — and convert explicitly with physical, which returns a same-type result whose positions are lengths and whose displacements (and uncertainties) are velocities (pixel_size / dt). The units are whatever you put in: millimeters and seconds in, mm/s out. Load Unitful for quantity-based construction (PhysicalScale(20.0u\"µm\", 0.5u\"ms\")).\n\nConvert last: validators, peak_locking, the epsilon floor used by universal outlier detection (UOD), and the correlation diagnostics (peak_ratio, correlation_moment) are pixel-native and are never converted — run them on the raw result. A converted result carries an identity scale with the same unit labels, so physical is idempotent and plots label their axes correctly either way. See the scaling how-to.","title":"Displacement sign convention"},{"category":"section","location":"explanation/conventions/#World-coordinates-(stereo)","page":"Coordinates, signs, and units","text":"Stereo processing works in a physical world frame (X, Y, Z) (units set by the calibration target, typically mm), with X/Y in the calibration-plate plane and Z out of plane. When cameras are mounted roughly upright, detect_calibration_grid anchors +X to the lattice direction nearest image-right and +Y to the one nearest image-up, making the frame right-handed with +Z toward the cameras.\n\nDewarped images (DewarpGrid, ImageDewarper) are indexed in the order the grid ranges are given: out[r, c] shows the world point (grid.x[c], grid.y[r], grid.z). With an ascending y range, world +Y therefore points down the dewarped image; pass a descending y range if you want a display-oriented (+Y up) image. Either way the bookkeeping is consistent: displacements measured on dewarped images convert to world units as du * step(grid.x) and dv * step(grid.y), signs included, and that is exactly what run_piv_stereo does internally.\n\nA StereoPIVResult reports x/y in world units on the dewarp grid and (u, v, w) in world units per frame interval, with w along world +Z. Spatial scaling is therefore already done; to get velocities, attach a PhysicalScale with dt and the unit labels only (pixel_size stays 1) and call physical — the per-camera cam1/ cam2 results always stay in dewarped pixels.","title":"World coordinates (stereo)"},{"category":"section","location":"explanation/conventions/#Particle-tracking-velocimetry-(PTV)","page":"Coordinates, signs, and units","text":"Particle tracking velocimetry measures the same displacement as particle image velocimetry (PIV) but per particle instead of per window, and it is the better tool wherever the seeding is sparse or Lagrangian information matters: low-density flows, the near-wall region where large windows straddle a velocity gradient, or any case where you want individual particle paths rather than an Eulerian field. Everything on this page still holds — u along x, v along y, pixels per frame interval, physical units via an attached PhysicalScale and physical — with three PTV-specific conventions. (One tracking nuance: a TrackingResult stores only positions, velocities being derived by differencing, so its converted scale keeps dt — pass it to trajectory_velocities for physical velocities.)\n\nFrame-A attribution. A PTVResult reports x/y as the frame-A particle positions and u/v as the displacement to frame B. This differs from PIV's symmetric image deformation, which attributes each vector to the midpoint of the trajectory. Frame-A attribution matches the forward-Euler contract of the synthetic generator exactly (each particle's true displacement is the velocity at its launch point), so ground-truth comparisons are direct — no midpoint correction, no interpolation error.\n\nFlag, don't replace. A tracked displacement is a measurement of one specific particle; there is nothing meaningful to substitute for it. So the scattered outlier test (Duncan et al., 2010) only flags suspicious vectors (in result.outliers) and leaves u/v untouched — unlike PIV, which replaces flagged windows with a local median. In the multi-frame tracker, a flagged link is simply not made (it would poison the constant-velocity predictor downstream).\n\nHybrid by default. With sparse seeding the true displacement can exceed the particle spacing, and pure nearest-neighbor matching then links the wrong particles. run_ptv therefore runs a coarse run_piv internally to predict where each particle goes, and matches against that prediction (Keane et al., 1995) — so it works out of the box at realistic displacements. Pass an existing PIVResult, a displacement NamedTuple, or nothing (pure nearest neighbor) to override.","title":"Particle tracking velocimetry (PTV)"},{"category":"section","location":"explanation/conventions/#Grid-layout","page":"Coordinates, signs, and units","text":"Interrogation windows use a stride of window_size - overlap; result.x/result.y are their window centers. All per-vector fields (u, v, peak_ratio, outliers, …) share the (length(y), length(x)) grid shape.\n\nWith the default search_area_size == window_size, tiling begins at the top-left corner. A larger centered search area moves the outer centers inward so its full footprint stays inside both images, without changing the stride.","title":"Grid layout"},{"category":"section","location":"tutorials/stereo/#Stereo-particle-image-velocimetry-(PIV)-end-to-end","page":"Stereo PIV end to end","text":"Ordinary planar PIV measures two in-plane displacement components. Two cameras viewing the same light sheet from different angles provide enough information to recover the third, out-of-plane component as well. This tutorial walks the entire stereo chain on a synthetic rig — calibration plate to three-component vector field — so every step has a known ground truth:\n\nphotograph a calibration target (render_calibration_target),\ndetect and index its dots (detect_calibration_grid),\nfit camera models (calibrate_camera),\ndewarp onto a common world plane (ImageDewarper),\ncorrect the plate-to-sheet misalignment (self_calibrate),\nreconstruct (u v w) (run_piv_stereo).\n\nThe concepts are explained in Stereo geometry and self-calibration; the real-data version of this workflow is the stereo-rig how-to.","title":"Stereo particle image velocimetry (PIV) end to end"},{"category":"section","location":"tutorials/stereo/#A-synthetic-stereo-rig","page":"Stereo PIV end to end","text":"The \"true\" optics: two pinhole cameras in a standard stereo arrangement, yawed ±20° about the vertical axis, looking at the world origin from 500 mm. In a real experiment these exist as hardware; here they render our images and provide the ground truth.\n\nusing Hammerhead\nusing Random, LinearAlgebra\nusing Statistics: median\n\nimage_size = (384, 384)\n\nfunction make_camera(yaw_deg; f = 2600.0, cx = 192.0, cy = 192.0, dist = 500.0)\n    th = deg2rad(yaw_deg)\n    R = [cos(th) 0.0 -sin(th); 0.0 1.0 0.0; sin(th) 0.0 cos(th)]\n    C = R' * [0.0, 0.0, -dist]           # camera center in world coordinates\n    K = [f 0.0 cx; 0.0 -f cy; 0.0 0.0 1.0]  # -f: world +Y points up in the image\n    return PinholeCamera(K, R, -R * C)\nend\n\ntrue_cams = (make_camera(-20.0), make_camera(20.0))","title":"A synthetic stereo rig"},{"category":"section","location":"tutorials/stereo/#Photograph-the-calibration-target","page":"Stereo PIV end to end","text":"The target is a LaVision-style two-level dot plate: dots every 15 mm on each level, the back level 3 mm behind the front, plus a filled square marker that anchors the origin and a triangle for orientation diagnostics. We image it at three traverse positions (z = −3, 0, +3 mm), as one would with a real traverse:\n\nzs = [-3.0, 0.0, 3.0]\ntarget_kwargs = (spacing = 15.0, two_level = true, level_separation = 3.0,\n                 marker_square = (-30.0, -7.5), marker_triangle = (-15.0, -7.5))\n\nplates = [[render_calibration_target(cam, image_size; z, target_kwargs...)\n           for z in zs] for cam in true_cams]\n\nusing CairoMakie\n\nlet\n    fig = Figure(size = (720, 380))\n    for (i, title) in enumerate((\"camera 1 (−20°)\", \"camera 2 (+20°)\"))\n        ax = Axis(fig[1, i]; title, yreversed = true, aspect = DataAspect())\n        image!(ax, plates[i][2]'; colormap = :grays)\n    end\n    fig\nend\n\nThe perspective differs between the cameras — that difference is what encodes the out-of-plane component.","title":"Photograph the calibration target"},{"category":"section","location":"tutorials/stereo/#Detect-the-grid-and-calibrate","page":"Stereo PIV end to end","text":"detect_calibration_grid finds the dots (subpixel intensity-weighted centroids), indexes them on the lattice, and anchors the world frame to the square marker. origin_offset = (30.0, 7.5) says \"the origin dot sits 30 mm right of and 7.5 mm above the marker\" — the same convention as PIV Challenge case 4E. Both cameras therefore agree on the world frame. One (default Soloff) model per camera:\n\ngrids = map(plates) do cam_plates\n    [detect_calibration_grid(img; spacing = 15.0,\n         two_level = true, level_separation = 3.0,\n         origin_offset = (30.0, 7.5))\n     for img in cam_plates]\nend\ncams = [calibrate_camera(g, zs) for g in grids]\ncams[1]\n\nAlways check the fit. On these noise-free synthetic plates the residual is tiny; on real plates expect ~0.5–1 px root-mean-square (RMS) error dominated by the plate's manufacturing tolerance (see the stereo-rig how-to):\n\n[calibration_quality(cam, gs, zs) for (cam, gs) in zip(cams, grids)]","title":"Detect the grid and calibrate"},{"category":"section","location":"tutorials/stereo/#Dewarp-onto-a-common-world-plane","page":"Stereo PIV end to end","text":"A DewarpGrid defines the measurement plane as a regular grid of world coordinates, shared by both cameras; an ImageDewarper per camera precomputes the resampling map once and reuses it for every frame.\n\nThe dewarped image is indexed out[r, c] = world (x[c], y[r], z) in the order the ranges are given, so a descending y range puts world +Y at the top — the image displays upright. (An ascending y would flip it, with +Y running down the image; PIV downstream is unaffected either way because dv * step(y) carries the sign — see Stereo geometry and self-calibration.)\n\ngrid = DewarpGrid(x = -25.0:0.2:25.0, y = 25.0:-0.2:-25.0)\ndw1 = ImageDewarper(cams[1], grid, image_size)\ndw2 = ImageDewarper(cams[2], grid, image_size)\n\nlet\n    fig = Figure(size = (720, 380))\n    for (i, (dw, plate)) in enumerate(((dw1, plates[1][2]), (dw2, plates[2][2])))\n        ax = Axis(fig[1, i]; title = \"camera $i, dewarped\",\n                  yreversed = true, aspect = DataAspect())\n        image!(ax, dewarp(dw, plate)'; colormap = :grays)\n    end\n    fig\nend\n\nAfter dewarping, the two cameras' views of the z = 0 plate coincide dot for dot: the same world point sits at the same pixel in both images. Zero-filled corners are regions that camera cannot see; they are recorded in dw.mask and excluded from the analysis automatically.","title":"Dewarp onto a common world plane"},{"category":"section","location":"tutorials/stereo/#Self-calibration:-find-the-actual-light-sheet","page":"Stereo PIV end to end","text":"In a real experiment the plate never sits exactly in the light sheet. We simulate that: particles live on a sheet that is offset by 0.8 mm and slightly tilted relative to the calibrated z = 0 plane. Both cameras record the same instants (this is what makes the disparity measurable):\n\nsheet = (a = 0.8, b = 0.010, c = -0.006)   # z = a + b·X + c·Y\nsheet_z(X, Y) = sheet.a + sheet.b * X + sheet.c * Y\n\nusing Hammerhead.SyntheticData: generate_gaussian_particle!\n\nfunction render_sheet(cam, pts, displacement = (0.0, 0.0, 0.0))\n    img = zeros(image_size)\n    for (X, Y) in pts\n        Z = sheet_z(X, Y)\n        p = world_to_pixel(cam, (X + displacement[1], Y + displacement[2],\n                                 Z + displacement[3]))\n        generate_gaussian_particle!(img, (p[1], p[2]), 6.0)\n    end\n    return img\nend\n\nrng = MersenneTwister(7)\ninstants = [[(56 * rand(rng) - 28, 56 * rand(rng) - 28) for _ in 1:400]\n            for _ in 1:3]\nframes1 = [render_sheet(true_cams[1], pts) for pts in instants]\nframes2 = [render_sheet(true_cams[2], pts) for pts in instants]\n\ndw1c, dw2c, report = self_calibrate(frames1, frames2, dw1, dw2)\nreport\n\nThe report tells the story: the first pass measured a systematic disparity of several dewarped pixels, triangulated it to the true sheet, fitted a plane, and rigidly moved both camera models onto it; the final verification pass confirms sub-tolerance residual disparity. The fitted plane matches the one we simulated:\n\nreport.passes[1].plane","title":"Self-calibration: find the actual light sheet"},{"category":"section","location":"tutorials/stereo/#Reconstruct-three-components","page":"Stereo PIV end to end","text":"Now the actual measurement: a frame pair in which every particle moves by a known world displacement, including 0.25 mm out of plane — invisible to either camera alone:\n\ntruth = (0.30, -0.20, 0.25)   # (dx, dy, dz) in mm per frame interval\npts = [(56 * rand(rng) - 28, 56 * rand(rng) - 28) for _ in 1:400]\nA1, B1 = render_sheet(true_cams[1], pts), render_sheet(true_cams[1], pts, truth)\nA2, B2 = render_sheet(true_cams[2], pts), render_sheet(true_cams[2], pts, truth)\n\nparams = PIVParameters(window_size = 32, overlap = 16,\n                       padding = true, apodization = :gauss)\nstereo = run_piv_stereo(A1, B1, A2, B2, dw1c, dw2c, params)\n\nThe StereoPIVResult carries (u, v, w) in world units on the shared grid. Compare the medians over valid vectors with the truth:\n\nsel = .!stereo.mask .& .!stereo.outliers\n(u = median(stereo.u[sel]), v = median(stereo.v[sel]), w = median(stereo.w[sel]),\n truth = truth)\n\nAll three components are recovered to a few hundredths of a millimeter — including w, thanks to the self-calibrated geometry. Had we skipped self-calibration, the sheet offset would have biased the reconstruction.\n\nThe in-plane field with the out-of-plane component as background:\n\nlet\n    fig = Figure(size = (560, 460))\n    ax = Axis(fig[1, 1]; title = \"stereo field: arrows (u, v), color w (mm)\",\n              xlabel = \"X (mm)\", ylabel = \"Y (mm)\", aspect = DataAspect())\n    hm = heatmap!(ax, stereo.x, stereo.y, permutedims(stereo.w); colormap = :viridis)\n    u = copy(stereo.u); u[.!sel] .= NaN\n    v = copy(stereo.v); v[.!sel] .= NaN\n    plot_vector_field!(ax, stereo.x, stereo.y, u, v)\n    Colorbar(fig[1, 2], hm)\n    fig\nend","title":"Reconstruct three components"},{"category":"section","location":"tutorials/stereo/#Where-to-go-next","page":"Stereo PIV end to end","text":"The same chain on a real experiment — plate-tolerance residuals, judging self-calibration without a convergence flag, reading σw/σu: Stereo on a real recording.\nThe rig-calibration checklist, including what residuals to expect from physical plates: Calibrate a real stereo rig.\nWhat the disparity self-calibration actually does: Stereo geometry and self-calibration.\nPer-vector uncertainty propagation into (u v w): enable uncertainty = true with a converged multi-pass schedule — see Uncertainty quantification.\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"tutorials/real_data/#A-real-recording:-tip-vortex-with-seeding-dropout","page":"A real recording: tip vortex","text":"The first tutorial generated its own images, so every claim could be checked against ground truth. Real measurements offer no such luxury — and that changes how you work. This tutorial analyzes a genuine wind-tunnel recording and shows how to judge a measurement using only what the analysis itself reports: validation flags, peak ratios, and per-vector uncertainty.\n\nThe data is case A of the first International Particle Image Velocimetry (PIV) Challenge (Stanislas et al., 2003): a wing-tip vortex 1.64 m behind a transport aircraft half-model in the German–Dutch Wind Tunnels Large Low-Speed Facility (DNW-LLF), recorded by C. Kähler of the German Aerospace Center (DLR). The Challenge organizers chose it because it concentrates common large-facility problems in one image pair — strong velocity gradients, varying particle image sizes, and loss of seeding in the vortex core. The pair ships with Hammerhead's test suite, so this tutorial runs on the real recording with no download step.","title":"A real recording: tip vortex with seeding dropout"},{"category":"section","location":"tutorials/real_data/#Load-and-inspect","page":"A real recording: tip vortex","text":"load_image reads any FileIO-supported image (here 12-bit grayscale TIFF) into a Matrix{Float64} scaled to 0 1:\n\nusing Hammerhead\n\ndir = joinpath(pkgdir(Hammerhead), \"test\", \"reference_images\", \"A\")\nimgA = load_image(joinpath(dir, \"A001_1.tif\"))\nimgB = load_image(joinpath(dir, \"A001_2.tif\"))\nsize(imgA), extrema(imgA)\n\nAlways look at real images before correlating them:\n\nusing CairoMakie\n\nlet\n    fig = Figure(size = (860, 400))\n    ax1 = Axis(fig[1, 1]; title = \"frame A\", yreversed = true, aspect = DataAspect())\n    image!(ax1, imgA'; colormap = :grays)\n    ax2 = Axis(fig[1, 2]; title = \"vortex core (closeup)\", yreversed = true,\n               aspect = DataAspect())\n    image!(ax2, imgA[395:695, 425:725]'; colormap = :grays)\n    fig\nend\n\nThree things stand out. The seeding is dense and the particle images are large and bright — good news for correlation. The illumination is far from uniform: the light sheet is much brighter near the top and bottom edges (the row-wise mean intensity varies by a factor of four across the frame). And near the center sits a dark disk roughly 200 px across: the vortex core, nearly empty of particles because the swirl centrifuges the seeding out of it. No preprocessing can restore information that was never recorded there — keep that disk in mind throughout.","title":"Load and inspect"},{"category":"section","location":"tutorials/real_data/#Run-the-analysis","page":"A real recording: tip vortex","text":"In-plane displacements in this pair reach about 10 px, so the first pass needs 64 px windows (the quarter-window rule); later passes refine at 32 px. Repeating the final window size adds the convergence sweep that the uncertainty estimator requires. This is the same accuracy configuration as in the first tutorial:\n\npasses = multipass_parameters([64, 32, 32];\n    padding = true,\n    apodization = :gauss,\n    uncertainty = true,\n)\nresult = run_piv(imgA, imgB, passes)\n\nplot_vector_field(result)\n\nA clean tip vortex, its center sitting exactly on the dark disk. The free-stream flow is perpendicular to the light sheet, so the in-plane field is almost pure swirl.","title":"Run the analysis"},{"category":"section","location":"tutorials/real_data/#Judging-a-measurement-without-ground-truth","page":"A real recording: tip vortex","text":"With synthetic data we verified accuracy by subtraction. Here the only evidence is what the correlation itself reports, and Hammerhead records three layers of it in the PIVResult:\n\nresult.outliers — binary validation verdicts (universal outlier detection plus peak-ratio checks by default).\nresult.peak_ratio — the height ratio of the primary to the secondary correlation peak, the classic detectability metric.\nresult.uncertainty_u / uncertainty_v — the Wieneke (2015) per-vector random-error estimate (Wieneke, 2015).\n\nStart with the flags:\n\ncount(result.outliers), length(result.outliers)\n\nUnder one percent of nearly 5000 vectors — scattered single vectors, mostly near the frame edges, and (perhaps surprisingly) not clustered in the empty core. Validation is a binary verdict on catastrophic failure; a window that still contains a handful of particles produces a plausible, neighbor-consistent vector and passes. To see the quality of what passed, look at the continuous metrics:\n\nvalid = .!(result.outliers .| result.mask);\n\nlet\n    fig = Figure(size = (900, 330))\n    pr = copy(result.peak_ratio)\n    pr[.!valid] .= NaN\n    ax1 = Axis(fig[1, 1]; title = \"peak ratio\", yreversed = true, aspect = DataAspect())\n    hm1 = heatmap!(ax1, result.x, result.y, pr'; colorrange = (1, 5))\n    Colorbar(fig[1, 2], hm1)\n    σu = copy(result.uncertainty_u)\n    σu[.!valid] .= NaN\n    ax2 = Axis(fig[1, 3]; title = \"σᵤ (px)\", yreversed = true, aspect = DataAspect())\n    hm2 = heatmap!(ax2, result.x, result.y, σu'; colorrange = (0, 0.3))\n    Colorbar(fig[1, 4], hm2)\n    fig\nend\n\nBoth maps point at the core without being told where it is: the peak ratio dips and the estimated uncertainty flares exactly on the dark disk. Quantitatively (medians, because near-outlier windows can legitimately report enormous σ):\n\nusing Statistics: median, quantile\n\nr_core = [hypot(x - 577, y - 545) for y in result.y, x in result.x]  # px from disk center\n\nfunction region_quality(sel)\n    pr = filter(isfinite, result.peak_ratio[sel .& valid])\n    σu = filter(isfinite, result.uncertainty_u[sel .& valid])\n    (median_pr = round(median(pr); digits = 2),\n     median_σu = round(median(σu); digits = 3),\n     q90_σu = round(quantile(σu, 0.9); digits = 3))\nend\n(core = region_quality(r_core .< 120), far_field = region_quality(r_core .> 300))\n\nIn the far field the estimator reports a typical random error near 0.09 px. Inside the core the median rises by half and the upper decile roughly doubles — the sparse windows correlate on fewer, weaker particle images, and the uncertainty says so honestly, vector by vector. This is the division of labor on real data: validation removes the wreckage, uncertainty grades everything that survives. See Uncertainty quantification for what the estimate does and doesn't cover.","title":"Judging a measurement without ground truth"},{"category":"section","location":"tutorials/real_data/#Preprocessing:-measure,-don't-assume","page":"A real recording: tip vortex","text":"The preprocessing guide ends with a rule: check a chain's effect before committing to it, using the peak-ratio distribution as the judge. This recording suggests three candidates — highpass_filter for the illumination gradient, intensity_cap for the bright particles (Shavit et al., 2007), and contrast-limited adaptive histogram equalization (clahe, commonly abbreviated CLAHE) for the dim core. Run the same analysis behind each and compare:\n\ncandidates = [\n    \"raw\"              => identity,\n    \"highpass (σ = 8)\" => img -> highpass_filter(img; sigma = 8),\n    \"intensity cap\"    => img -> intensity_cap(img),\n    \"CLAHE\"            => img -> clahe(img),\n]\n\nfunction chain_quality(f)\n    res = run_piv(f(imgA), f(imgB), passes)\n    ok = .!(res.outliers .| res.mask)\n    pr = filter(isfinite, res.peak_ratio[ok])\n    (median_pr = round(median(pr); digits = 2),\n     q10_pr = round(quantile(pr, 0.1); digits = 2),\n     outliers = count(res.outliers))\nend\n[name => chain_quality(f) for (name, f) in candidates]\n\nThe result upends the assumptions. The high-pass filter — the textbook response to illumination gradients — lowers the median peak ratio and triples the outlier count: these particle images are large, so a filter tuned to remove smooth background removes particle energy too, and the smooth gradient never bothered the correlator in the first place (each window subtracts its own mean). Intensity capping doubles the outliers for the same reason: the brightest particles are signal here, not noise. Only CLAHE helps, and modestly — it flattens the visible banding and nudges peak ratios up in the dim regions.\n\nThe lesson is not that preprocessing is useless — on recordings with static glare or genuinely low contrast it is decisive. The lesson is that every step must pay for itself in measured correlation quality on your images. Here the honest conclusion is that the recording is already good, and the elevated uncertainty in the core is a property of the flow (the particles really are missing), not a defect any filter can repair. With more than one image pair, ensemble correlation can pool the few particles that do transit the core across many instants — see Ensemble correlation for low signal-to-noise ratio (SNR).","title":"Preprocessing: measure, don't assume"},{"category":"section","location":"tutorials/real_data/#Where-to-go-next","page":"A real recording: tip vortex","text":"Static background removal needs a frame sequence (compute_background): the preprocessing guide.\nIf the default checks flag too much or too little: Tune validation.\nWhole recordings, incremental result files: Batch processing.\nWhat σᵤ means and when to trust it: Uncertainty quantification.\nTwo cameras: the stereo tutorial.\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"reference/feature_matrix/#Feature-matrix","page":"Feature matrix","text":"Workflow CPU KA CPU CUDA AMDGPU\nPlanar single-pair / multipass yes yes yes yes\nPlanar sequence yes yes yes yes\nEnsemble correlation yes yes yes yes\nStereo 2D3C, sequence, ensemble yes yes yes yes\nCorrelation-statistics uncertainty yes yes yes yes\nEnlarged search areas yes no no no\nPTV and trajectory linking yes CPU CPU CPU\nRetained correlation planes yes no no no\n\nKA CPU is the hardware-free proving backend selected by backend = :ka. CUDA and AMDGPU accelerate correlation, deformation, peak analysis, and the Wieneke uncertainty statistics; image loading, dewarping, 3C reconstruction, PTV, validation, exports, and statistics remain host operations. GPU backends support cross/phase correlation and gauss3/gauss9 subpixel fits; enlarged search areas, gauss2d, and retained planes are rejected explicitly.\n\nAll workflows accept Float32 and Float64. See KernelAbstractions and GPU backends for the precise device boundary and supported options.","title":"Feature matrix"},{"category":"section","location":"reference/synthetic/#Synthetic-data","page":"Synthetic data","text":"The SyntheticData submodule generates particle images with exact ground truth: particle positions are displaced directly through a velocity function (no interpolation warping), including out-of-plane motion through a laser-sheet intensity profile. Used throughout the test suite and the tutorials.\n\nPages = [\"synthetic.md\"]","title":"Synthetic data"},{"category":"module","location":"reference/synthetic/#Hammerhead.SyntheticData","page":"Synthetic data","text":"SyntheticData\n\nSynthetic PIV test data with particle-based image generation. Ground truth comes from displacing particle positions directly (no interpolation warping), including out-of-plane motion and a laser-sheet intensity profile.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData"},{"category":"function","location":"reference/synthetic/#Hammerhead.SyntheticData.displace_particles","page":"Synthetic data","text":"displace_particles(particles::ParticleField3D, velocity_function, dt, t = 0.0) -> ParticleField3D\n\nNon-mutating version of displace_particles!.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.displace_particles"},{"category":"function","location":"reference/synthetic/#Hammerhead.SyntheticData.displace_particles!","page":"Synthetic data","text":"displace_particles!(particles::ParticleField3D, velocity_function, dt, t = 0.0)\n\nDisplace particles in place by one forward-Euler step of velocity_function (signature (x, y, z, t) -> (u, v, w)): each particle moves by its local velocity — evaluated at its current position and time t — times dt. The exact displacement of every particle is therefore the velocity field at its launch point, whatever the field's curvature.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.displace_particles!"},{"category":"method","location":"reference/synthetic/#Hammerhead.SyntheticData.generate_gaussian_particle!-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Tuple{Real, Real}, Real}, Tuple{AbstractMatrix{T}, Tuple{Real, Real}, Real, Real}} where T<:Real","page":"Synthetic data","text":"generate_gaussian_particle!(image, (cx, cy), diameter, intensity = 1.0)\n\nAdd a Gaussian particle image centered at (cx, cy) — cx along columns (x), cy along rows (y) — with σ = diameter / 4. Intensity is accumulated within a 3σ bounding box, clipped to the image.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.generate_gaussian_particle!"},{"category":"method","location":"reference/synthetic/#Hammerhead.SyntheticData.generate_particle_field-Tuple{Tuple{Int64, Int64}, Real}","page":"Synthetic data","text":"generate_particle_field(image_size, particle_density; kwargs...) -> ParticleField3D\n\nGenerate a random 3D particle field for synthetic PIV images.\n\nArguments\n\nimage_size::Tuple{Int,Int}: image size (rows, cols)\nparticle_density::Real: average particles per pixel\n\nKeywords\n\nz_range = (-5.0, 5.0): range of out-of-plane positions\ndiameter_mean = 3.0, diameter_std = 0.5: particle image diameter distribution in pixels (normal, clipped to ≥ 0.5)\nintensity_mean = 1.0, intensity_std = 0.2: particle intensity distribution (normal, clipped to ≥ 0.1)\nrng = Random.default_rng(): random number generator\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.generate_particle_field"},{"category":"method","location":"reference/synthetic/#Hammerhead.SyntheticData.generate_synthetic_piv_pair-Tuple{Any, Tuple{Int64, Int64}, Real}","page":"Synthetic data","text":"generate_synthetic_piv_pair(velocity_function, image_size, dt;\n                            kwargs...) -> (img1, img2, particles1, particles2)\n\nGenerate a synthetic PIV image pair with known ground truth: a random particle field is rendered at time t0, displaced through velocity_function (signature (x, y, z, t) -> (u, v, w)) over dt, and rendered again. The displacement is a single forward-Euler step (see displace_particles!), so each particle's true displacement is exactly velocity × dt at its starting position. Out-of-plane motion changes particle brightness via the laser sheet profile.\n\nKeywords\n\nparticle_density = 0.05: particles per pixel\nlaser_sheet = GaussianLaserSheet(0.0, 2.0, 1.0)\nbackground_noise = 0.01: per-image Gaussian noise level\nz_range = (-5.0, 5.0): range of particle z-positions\nt0 = 0.0: time at the first image\nrng = Random.default_rng()\nremaining keywords are forwarded to generate_particle_field\n\nReturns the two images and the two particle fields (for ground-truth analysis).\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.generate_synthetic_piv_pair"},{"category":"method","location":"reference/synthetic/#Hammerhead.SyntheticData.linear_flow-NTuple{7, Any}","page":"Synthetic data","text":"linear_flow(u₀, v₀, w₀, ∂u∂x, ∂u∂y, ∂v∂x, ∂v∂y) -> velocity_function\n\nCreate a linear velocity field (x, y, z, t) -> (u, v, w).\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.linear_flow"},{"category":"method","location":"reference/synthetic/#Hammerhead.SyntheticData.render_particle_image-Tuple{Hammerhead.SyntheticData.ParticleField3D, Tuple{Int64, Int64}, Hammerhead.SyntheticData.LaserSheet}","page":"Synthetic data","text":"render_particle_image(particles::ParticleField3D, image_size, laser::LaserSheet;\n                      kwargs...) -> Matrix{Float64}\n\nRender a particle field into an image, modulating each particle's intensity by the laser sheet profile at its z position. Particles whose centers lie just outside the frame still contribute their in-frame tails, so pairs of rendered images stay consistent near the borders.\n\nKeywords\n\nbackground_noise = 0.01: Gaussian background noise level (0 disables)\nintensity_threshold = 0.01: minimum laser intensity multiplier for a particle to be rendered\nrng = Random.default_rng(): random number generator (noise only)\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.render_particle_image"},{"category":"function","location":"reference/synthetic/#Hammerhead.SyntheticData.shear_flow","page":"Synthetic data","text":"shear_flow(shear_rate, w₀ = 0.0) -> velocity_function\n\nCreate a simple shear velocity field u = shear_rate * y.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.shear_flow"},{"category":"function","location":"reference/synthetic/#Hammerhead.SyntheticData.vortex_flow","page":"Synthetic data","text":"vortex_flow(center_x, center_y, strength, w₀ = 0.0) -> velocity_function\n\nCreate a circular vortex velocity field with constant azimuthal speed strength around (center_x, center_y).\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.vortex_flow"},{"category":"type","location":"reference/synthetic/#Hammerhead.SyntheticData.GaussianLaserSheet","page":"Synthetic data","text":"GaussianLaserSheet(z_center, thickness, peak_intensity)\n\nGaussian laser sheet profile: intensity falls off as a Gaussian in z with full width at half maximum thickness, centered at z_center.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.GaussianLaserSheet"},{"category":"type","location":"reference/synthetic/#Hammerhead.SyntheticData.LaserSheet","page":"Synthetic data","text":"LaserSheet\n\nAbstract type for laser sheet intensity profiles. Subtypes implement laser_intensity(sheet, z).\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.LaserSheet"},{"category":"type","location":"reference/synthetic/#Hammerhead.SyntheticData.ParticleField3D","page":"Synthetic data","text":"ParticleField3D\n\nA collection of tracer particles with 3D positions and per-particle properties. x is the in-plane column coordinate, y the in-plane row coordinate, and z the out-of-plane coordinate (the laser sheet typically sits at z = 0).\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.ParticleField3D"},{"category":"section","location":"explanation/compatibility/#Compatibility-policy","page":"Compatibility policy","text":"Hammerhead follows semantic versioning. Before 1.0, exported Julia APIs and the package-native JLD2 representation may change in a minor release; changes are called out in release notes and backward-compatible constructors are used where practical. A stable release will not make an incompatible exported-API or native-format change without a major version bump.\n\nJLD2 is the lossless Julia round-trip format. Files carry format_version; readers reject unknown versions rather than silently misinterpreting data. Users who need long-lived, language-neutral archives should also export the table or VTK form.\n\nThe long-form table contract is identified by TABLE_SCHEMA_VERSION and the ordered TABLE_COLUMNS constant. Columns are a backward-compatible superset across planar, stereo, and PTV results: unavailable values are empty rather than changing shape. Existing columns will not be renamed or change meaning within a schema version. An incompatible contract change increments the schema version; additive columns may be introduced without invalidating readers that select columns by name.\n\nVTK export uses the legacy structured-grid contract documented by export_vtk. Coordinate/component units are written in field metadata and follow the attached PhysicalScale when present.","title":"Compatibility policy"},{"category":"section","location":"tutorials/ptv/#Particle-tracking-velocimetry-(PTV)","page":"Particle tracking (PTV)","text":"Particle image velocimetry (PIV) reports one vector per interrogation window. Particle tracking velocimetry (PTV) instead follows identifiable particles. When the seeding is sparse — near a wall, in a dilute spray, or wherever you want paths that follow individual particles (Lagrangian measurements) rather than velocities reported at fixed spatial locations (an Eulerian field) — it is better to track individual particles. Hammerhead's PTV pipeline detects particles in each frame, matches them across a pair, and links them across a sequence. Everything here is synthetic and self-contained, so the ground truth is known exactly.","title":"Particle tracking velocimetry (PTV)"},{"category":"section","location":"tutorials/ptv/#A-tracked-image-pair","page":"Particle tracking (PTV)","text":"We start from the same kind of synthetic pair as the PIV tutorials, but at a lower seeding density — the regime where tracking shines.\n\nusing Hammerhead\nusing Hammerhead.SyntheticData\nusing Random\n\nvortex = vortex_flow(128.0, 128.0, 6.0, 0.0)   # constant 6 px azimuthal speed\nimgA, imgB, truthA, truthB = generate_synthetic_piv_pair(\n    vortex, (256, 256), 1.0;\n    particle_density = 0.015,\n    background_noise = 0.01,\n    z_range = (-1.0, 1.0),\n    rng = MersenneTwister(42),\n)\nsize(imgA)\n\nrun_ptv does the whole two-frame analysis. By default it is hybrid: a coarse run_piv runs internally to predict where each particle goes, so nearest-neighbor matching stays reliable even when the displacement (here ~6 px) is larger than the particle spacing (Keane et al., 1995). Matched vectors are then validated with a scattered normalized-median test (Duncan et al., 2010) that flags outliers but never replaces them — a tracked displacement is a measurement of one particle, with nothing meaningful to substitute.\n\nptv = run_ptv(imgA, imgB)\n\nThe PTVResult stores the frame-A particle positions (x, y), the displacement to frame B (u, v), and diagnostics. The Makie extension draws the scattered vectors directly (outliers in red):\n\nusing CairoMakie\n\nplot_vector_field(ptv; axis = (title = \"PTV: one vector per particle\",))","title":"A tracked image pair"},{"category":"section","location":"tutorials/ptv/#Frame-A-attribution-makes-the-ground-truth-check-exact","page":"Particle tracking (PTV)","text":"generate_synthetic_piv_pair returns index-aligned truth (truthA[i] ↔ truthB[i]), and PTV attributes each vector to the frame-A particle position — exactly matching the forward-Euler contract of the generator. So unlike PIV's symmetric deformation (which needs a midpoint correction), the comparison is direct, with no interpolation error. We map each detection to its nearest truth particle and measure the displacement error over correct matches:\n\nusing Statistics: median\n\nfunction nearest_truth(p, tx, ty; tol = 0.5)\n    map = fill(-1, length(p))\n    for i in 1:length(p)\n        best, bj = Inf, 0\n        for j in eachindex(tx)\n            d2 = (p.x[i] - tx[j])^2 + (p.y[i] - ty[j])^2\n            d2 < best && ((best, bj) = (d2, j))\n        end\n        sqrt(best) < tol && (map[i] = bj)\n    end\n    return map\nend\n\nmA = nearest_truth(ptv.particles_a, truthA.x, truthA.y)\nmB = nearest_truth(ptv.particles_b, truthB.x, truthB.y)\nerrs = Float64[]\ncorrect = 0\nfor k in eachindex(ptv.index_a)\n    ta, tb = mA[ptv.index_a[k]], mB[ptv.index_b[k]]\n    (ta > 0 && tb > 0) || continue\n    if ta == tb\n        global correct += 1\n        push!(errs, hypot(ptv.u[k] - (truthB.x[tb] - truthA.x[ta]),\n                          ptv.v[k] - (truthB.y[tb] - truthA.y[ta])))\n    end\nend\n(correct_fraction = correct / count(>(0), mA[ptv.index_a]),\n median_error_px = median(errs))\n\nAlmost every match is a correct correspondence, and the displacement error is a few hundredths of a pixel.","title":"Frame-A attribution makes the ground-truth check exact"},{"category":"section","location":"tutorials/ptv/#Scattered-vectors-versus-a-gridded-field","page":"Particle tracking (PTV)","text":"PIV and PTV answer different questions. PIV gives a dense field on a regular grid (spatially smoothed by the window size); PTV gives sparse, unsmoothed measurements exactly where particles happen to be. ptv_to_grid bins the tracked vectors back onto an interrogation grid — a median per cell — when you need a field-shaped result (it plugs into field_statistics, plotting, and predictors like any masked PIVResult):\n\npiv = run_piv(imgA, imgB, multipass_parameters([64, 32]; padding = true, apodization = :gauss))\ngridded = ptv_to_grid(ptv, size(imgA); window_size = (32, 32), overlap = (16, 16))\n\nfig = Figure(size = (760, 380))\nplot_vector_field!(Axis(fig[1, 1]; title = \"PIV field\", yreversed = true,\n                        aspect = DataAspect()), piv)\nplot_vector_field!(Axis(fig[1, 2]; title = \"PTV binned to grid\", yreversed = true,\n                        aspect = DataAspect()), gridded.x, gridded.y, gridded.u, gridded.v)\nfig","title":"Scattered vectors versus a gridded field"},{"category":"section","location":"tutorials/ptv/#Linking-a-sequence-into-trajectories","page":"Particle tracking (PTV)","text":"Given more than two frames, track_particles chains matches into Lagrangian tracks. Each track head is predicted by constant velocity once it has two points, so long paths stay locked onto their particle. We build a short sequence by stepping a particle field through the vortex:\n\nsheet = GaussianLaserSheet(0.0, 40.0, 1.0)     # thick sheet: no dropout\nfield = generate_particle_field((256, 256), 0.008; z_range = (-0.5, 0.5),\n                                rng = MersenneTwister(7))\nframes = Matrix{Float64}[]\nfor k in 1:8\n    push!(frames, render_particle_image(field, (256, 256), sheet;\n                                        background_noise = 0.0,\n                                        rng = MersenneTwister(100 + k)))\n    global field = displace_particles(field, vortex, 1.0)\nend\n\ntracks = track_particles(frames, PTVParameters(); min_track_length = 5, progress = false)\nlength(tracks.trajectories)\n\nEach Trajectory is one particle's path; trajectory_velocities gives per-point velocity by finite differences. We draw every track, colored by instantaneous speed:\n\nfig2 = Figure(size = (560, 520))\nax = Axis(fig2[1, 1]; title = \"Lagrangian tracks\", yreversed = true,\n          aspect = DataAspect(), xlabel = \"x (px)\", ylabel = \"y (px)\")\nfor t in tracks.trajectories\n    u, v = trajectory_velocities(t)\n    lines!(ax, t.x, t.y; color = hypot.(u, v), colormap = :viridis, colorrange = (0, 6))\nend\nColorbar(fig2[1, 2]; colorrange = (0, 6), colormap = :viridis, label = \"speed (px/frame)\")\nfig2\n\nThe circular streaks trace the vortex; every line is one particle followed across the whole sequence.","title":"Linking a sequence into trajectories"},{"category":"section","location":"tutorials/ptv/#Where-to-go-next","page":"Particle tracking (PTV)","text":"When PTV beats PIV, and the frame-A attribution and flag-don't-replace conventions: Coordinates, signs, and units.\nDetection, matching, and tracking parameters: the PTV reference.\nBatch tracking over many pairs: run_ptv_sequence mirrors run_piv_sequence (see the batch guide).\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"explanation/masking/#The-masking-model","page":"The masking model","text":"Real recordings contain regions that must not produce vectors: model geometry, walls, reflections, or (in stereo) parts of the plane one camera cannot see. Hammerhead handles these with a single, consistent masking model.","title":"The masking model"},{"category":"section","location":"explanation/masking/#Masks-are-image-sized-and-true-excluded","page":"The masking model","text":"An analysis mask is a Bool matrix the size of the images, with true marking pixels to exclude. Build one with polygon_mask, load one from an image file with load_mask, or use any Bool array; combine regions with .|. Pass it as run_piv(imgA, imgB, passes; mask).\n\nA single mask describes static lab-frame geometry and is deliberately not warped between passes. Sequence drivers also accept one mask per pair, one mask per frame (the two exposure masks are unioned), or an (i, frameA, frameB) -> mask callback. Union semantics prevent geometry that occupies either exposure from contributing to that pair's correlation.","title":"Masks are image-sized and true = excluded"},{"category":"section","location":"explanation/masking/#Masked-outlier","page":"The masking model","text":"Hammerhead keeps two distinct per-vector flags in a PIVResult, and conflating them is the most common masking mistake in particle image velocimetry (PIV) software:\n\nresult.mask — windows dropped because they overlap the mask. They carry no measurement: u, v, peak_ratio, and correlation_moment are NaN there. A masked window is not \"bad data\"; it is no data.\nresult.outliers — windows that produced a measurement which then failed validation. When replacement is active, u/v at these positions hold the local-median replacement.\n\nA window is never both: masked cells are excluded from validation entirely.","title":"Masked ≠ outlier"},{"category":"section","location":"explanation/masking/#How-masked-pixels-enter-the-correlation","page":"The masking model","text":"A node is dropped when the masked-pixel fraction reaches mask_threshold (default 0.5) in either its frame-A interrogation footprint or frame-B search footprint. Footprints below the threshold are still correlated, over their valid pixels only: masked pixels are loaded at the mean of the window's valid pixels, which is zero after the correlator's mean subtraction. This choice means the mask edge introduces no intensity step — a hard zero would act like a bright edge and bias the correlation peak toward zero displacement.","title":"How masked pixels enter the correlation"},{"category":"section","location":"explanation/masking/#Masked-regions-downstream","page":"The masking model","text":"The exclusion propagates through every stage that looks at neighbors:\n\nValidation — universal outlier detection never flags a masked cell and never includes one in a neighbor median (NaNs cannot poison the test).\nReplacement — local-median replacement neither fills masked cells nor draws donor vectors from them.\nMulti-pass predictor — before smoothing and deformation, masked cells are filled from valid neighbors so the interpolated predictor stays finite everywhere; the filled values are only used to condition the deformation, and the cells are re-dropped in the pass output.\nStatistics — field_statistics, error_statistics, and friends skip masked cells via their valid-sample logic.","title":"Masked regions downstream"},{"category":"section","location":"explanation/masking/#Stereo-masks","page":"The masking model","text":"Dewarping produces a validity mask per camera (dw.mask, grid-sized, true = that camera cannot see the node). run_piv_stereo and self_calibrate combine both cameras' masks with any user mask (dw1.mask .| dw2.mask .| user), so both cameras are analyzed on identical grids restricted to the stereo overlap region.","title":"Stereo masks"},{"category":"section","location":"howto/gui/#Work-interactively-with-the-graphical-user-interface-(GUI)","page":"Work interactively with the GUI","text":"Goal: do the common interactive jobs — browse results, draw a mask, run a batch, review a calibration — with the HammerheadGUI tools, and get their output back into scripted analyses. For a guided walkthrough with figures, start with the GUI tour.\n\nThe GUI ships as the separate HammerheadGUI package (the core stays free of GL dependencies); using HammerheadGUI loads GLMakie, and each call below opens a window.","title":"Work interactively with the graphical user interface (GUI)"},{"category":"section","location":"howto/gui/#Explore-a-results-file","page":"Work interactively with the GUI","text":"result_explorer takes anything load_results produces — planar, stereo, PTV, and tracking entries, mixed files included — or in-memory results:\n\nusing HammerheadGUI\n\nresult_explorer(\"run_042.jld2\")\nresult_explorer(result)              # a PIVResult / StereoPIVResult\nresult_explorer(ptv_result)          # a PTVResult / TrackingResult\nresult_explorer([r1, r2, r3])        # a sequence — the slider scrubs frames\n\nFor gridded results the field menu lists displacement magnitude, components, the validation diagnostics, and per-vector σ when the analysis ran with uncertainty = true; click any vector to inspect its numbers. A PTVResult is drawn as a colored particle scatter with optional displacement arrows (flagged particles in red), and a TrackingResult as trajectory polylines colored by mean speed, with breaks at bridged frame gaps.\n\nWhen a result carries a PhysicalScale — attached at analysis time or with with_scale — the explorer displays in physical units: axis labels, the colorbar, and the inspection panel all read mm, mm/s, and so on instead of px / px/frame.\n\nThe colorbar defaults to a robust 2–98% percentile range over the valid (non-masked, non-flagged) vectors (color_limits), so outliers cannot wash out the display. The \"color range\" group switches to the full extrema or pins either bound; manual bounds persist across frame/field switches until cleared:\n\nset_color_mode!(ex, :full)             # extrema instead of percentiles\nset_color_limits!(ex; min = 0, max = 5)\nset_color_limits!(ex; max = \"auto\")    # clear one bound\n\nFor planar results the field menu also carries the derived fields — vorticity, divergence, strain rate |S|, swirling strength, and Q — via flow_derivatives, computed once per frame and labelled 1/s (1/s² for Q) when a scale is attached. The tool menu adds interactive analysis on the same results:\n\nset_tool!(ex, :profile)      # two clicks sample u/v/|V| along a line\nset_tool!(ex, :circulation)  # click a contour, right-click to close:\n                             # line-integral + vorticity-area circulation\ntool_summary(ex)             # the live numbers, with units\nset_tool!(ex, :inspect)      # back to click-to-inspect\n\nTool state clears when the frame changes, and the analysis tools revert to :inspect on result types without derived analysis (stereo/PTV/tracking).","title":"Explore a results file"},{"category":"section","location":"howto/gui/#Draw-a-mask-and-use-it","page":"Work interactively with the GUI","text":"mask_editor(\"frame_0001.tif\")\n\nLeft-click adds vertices (inside an existing polygon it selects instead), right-click closes the polygon, Backspace undoes a vertex, Delete removes the selected polygon. \"Save mask…\" writes the white-=-excluded image that load_mask reads back:\n\nmask = load_mask(\"mask.png\")\nresult = run_piv(imgA, imgB, passes; mask)\n\nTo skip the file, hold the MaskEditor controller and export directly — and seed it with existing polygons to resume editing:\n\nme = MaskEditor(load_image(\"frame_0001.tif\"))\nmask_editor(me)                      # draw…\nmask = polygon_mask(me)              # image-sized Bool, true = excluded\n\nme = MaskEditor(img; polygons = [[(120, 40), (480, 90), (450, 300)]])","title":"Draw a mask and use it"},{"category":"section","location":"howto/gui/#Run-a-batch-from-the-form","page":"Work interactively with the GUI","text":"batch_runner()\n\n\"Add frames…\" picks the image files, the menu chooses paired (1-2, 3-4) or chained (1-2, 2-3) pairing, the windows textbox takes a schedule like 64, 32, 32, and \"choose output…\" sets an incremental JLD2 file (written pair by pair; read it with load_results). \"Cancel\" stops after the pair in flight and keeps every finished pair. \"View results\" activates as soon as the first pair completes: it opens the finished prefix in the result explorer and appends later pairs live, so you can inspect a long batch while it runs (the mechanism is the completed observable plus push_result! — available for your own live consumers too, and scripted runs get the same stream through run_piv_sequence's on_result callback).\n\nThe effort menu switches between the manual schedule (:custom) and run_piv_sequence's :low / :medium / :high presets — when a preset is active the manual schedule is ignored (the summary says so). The physical scale group (pixel size, dt, and unit labels) attaches a PhysicalScale to every output when any field is non-default, so the batch results carry units straight into the explorer. From code:\n\nset_effort!(bc, :high)\nset_scale!(bc; pixel_size = 50.0, dt = 0.001, length_unit = \"mm\", time_unit = \"s\")\n\nInstead of typing the pixel size, derive it from an image with the scale tool — click the two endpoints of a feature of known physical size (a ruler, or two dots of a calibration plate) and apply the result to the form:\n\nst = ScaleTool(\"calibration_plate.tif\")\nscale_tool(st; batch = bc)     # click two points, enter the separation…\napply_scale!(bc, st)           # …or do the hand-off from code\n\n\"Preprocess…\" opens a preprocess_preview on the first frame: an ordered, toggleable pipeline over the core preprocessing set (background subtraction, intensity cap, highpass, CLAHE, percentile stretch, inversion, local-variance normalization) with a live raw/processed comparison; \"use in batch\" installs it. From code, build the pipeline yourself:\n\npp = PreprocessPreview(first_frame; enabled = [:highpass_filter, :clahe])\nset_step_param!(pp, :highpass_filter, :sigma, 5)\nset_preprocess!(bc, pp)        # snapshot: later edits don't affect the run\n\nThe exported closure copies each frame before its in-place steps, so in-memory arrays are never mutated.\n\nGive the preview the frame's correlation partner and it gains a single-window probe: click the processed image and that window's displacement and peak ratio recompute live with every pipeline change — judge a preprocessing choice by what it does to the correlation, before committing to a batch:\n\npp = PreprocessPreview(frameA; pair = frameB)   # the batch pop-out does this\nset_probe_window!(pp, 48)\nprobe_summary(pp)      # \"du = …, dv = … px, peak ratio = …\" after a click\n\nSeed the form from code with a BatchRunner — every form field is an observable on the controller:\n\nbc = BatchRunner(files = readdir(\"run42\"; join = true), uncertainty = true)\nbc.output_path[] = \"run_042.jld2\"\nbatch_runner(bc)\n\nstart!(bc; async = false) runs the same batch without the window at all.","title":"Run a batch from the form"},{"category":"section","location":"howto/gui/#Review-a-calibration","page":"Work interactively with the GUI","text":"calibration_review shows each plate image with its detected dots colored by reprojection error (fiducial markers outlined), a plane slider, and a camera-model menu that refits on switch. The keyword arguments are detect_calibration_grid's:\n\ncalibration_review(plate_images, zs; spacing = 15.0, two_level = true,\n                   level_separation = 3.0, origin_offset = (30.0, 7.5))\n\nFor judging the numbers it shows — what residuals a physical plate produces and when to worry — see Calibrate a real stereo rig.\n\nAfter stereo self-calibration, selfcal_review browses the report; keep the disparity maps to inspect them pass by pass in an embedded explorer:\n\ndw1c, dw2c, report = self_calibrate(frames1, frames2, dw1, dw2;\n                                    keep_disparity_maps = true)\nselfcal_review(report)","title":"Review a calibration"},{"category":"section","location":"howto/gui/#Run-a-stereo-batch","page":"Work interactively with the GUI","text":"stereo_calibration sets the rig up: both cameras' CalibrationReviews embedded side by side, the dewarp-grid options (coverage, spacing), and a \"build dewarpers\" button running build_dewarpers over the two fitted cameras:\n\ncr1 = CalibrationReview(plates_cam1, zs; spacing = 15.0, ...)\ncr2 = CalibrationReview(plates_cam2, zs; spacing = 15.0, ...)\nsbc = StereoBatchRunner()\nstereo_calibration(cr1, cr2; batch = sbc)   # …or from code:\nset_dewarpers!(sbc, cr1, cr2)               # build_dewarpers + install\n\nDewarpers you built at the REPL (e.g. after self_calibrate) go straight in with set_dewarpers!(sbc, dw1, dw2). Then stereo_batch_runner is the stereo form: each camera's frame list, the effort/schedule form, a dt-only physical scale (stereo results are already in world units), and incremental output — run_piv_stereo_sequence underneath. Cancellation uses the driver's native between-acquisition predicate, so the completed prefix always lands in results, and \"view results\" opens the StereoPIVResults in the explorer live, exactly like the planar batch:\n\nadd_files!(sbc, cam1_paths; camera = 1)\nadd_files!(sbc, cam2_paths; camera = 2)\nset_effort!(sbc, :medium)\nset_dt!(sbc, 0.001); sbc.time_unit[] = \"s\"; sbc.length_unit[] = \"mm\"\nstereo_batch_runner(sbc)     # or start!(sbc; async = false) headless","title":"Run a stereo batch"},{"category":"section","location":"howto/gui/#Embed-a-view-in-your-own-figure","page":"Work interactively with the GUI","text":"result_explorer! builds the explorer into a GridPosition, for composing dashboards — it is the embeddable form the composite views use internally:\n\nusing GLMakie\n\nfig = Figure(size = (1400, 700))\nresult_explorer!(fig[1, 1], ResultExplorer(results))\nax = Axis(fig[1, 2])  # your own plots alongside\n\nBecause the view renders a controller, several views (or your own code) can share one ResultExplorer and stay in sync through its observables.","title":"Embed a view in your own figure"},{"category":"section","location":"howto/batch/#Batch-processing-and-result-files","page":"Batch processing and result files","text":"Goal: process a whole recording to a results file, survive crashes mid-batch, and read everything back later.","title":"Batch processing and result files"},{"category":"section","location":"howto/batch/#Build-the-pair-list","page":"Batch processing and result files","text":"image_pairs groups an ordered frame list (file paths and/or in-memory matrices) into correlation pairs. Build the list by filtering a directory with readdir and Julia's own predicates — no extra dependency:\n\nusing Hammerhead\n\ndir = \"run_042\"\n# Every .tif in the directory, in sorted order (readdir already sorts):\nfiles = filter(endswith(\".tif\"), readdir(dir; join = true))\n# Or a stricter pattern — frames B00001.tif … B09999.tif only:\nfiles = filter(f -> occursin(r\"^B\\d{5}\\.tif$\", basename(f)),\n               readdir(dir; join = true))\n\npairs = image_pairs(files)                   # double-frame: (1,2), (3,4), ...\npairs = image_pairs(files; mode = :chained)  # time-resolved: (1,2), (2,3), ...\n\nFor sampled or multi-delay recordings, use stride, zero-based offset, and one or more deltas:\n\npairs = image_pairs(files; mode=:chained, stride=2, offset=1, deltas=(1, 4))\n\nFrameSource(n, i -> read_vendor_frame(i)) adapts a proprietary CINE, MRAW, SEQ, video, or camera reader without a package dependency. Pair construction does not read frames. Optional timestamps travel with the pairs; when a PhysicalScale is passed to the sequence driver, each result receives the pair's actual dt. Use TIFFStack(\"recording.tif\") for multi-page TIFFs.\n\nFor true shell-style * globbing across nested directories, add Glob.jl (Glob.glob(\"cam1/*.tif\")); the predicate filter above covers most batch needs without a dependency.","title":"Build the pair list"},{"category":"section","location":"howto/batch/#Run-the-sequence-with-incremental-output","page":"Batch processing and result files","text":"passes = multipass_parameters([64, 32, 16]; padding = true, apodization = :gauss)\nresults = run_piv_sequence(pairs, passes;\n    preprocess = img -> intensity_cap!(img),\n    output = \"run_042_piv.jld2\",\n    mask = mask,                  # optional, shared by all pairs\n)\n\nWith output set, each result is written to the JLD2-format Julia data file as it completes — a crashed batch keeps its finished pairs. For file-path pairs the source image paths are stored alongside the results. Threading applies within each pair (the window grid is split across tasks); results are bitwise identical to serial processing.\n\nTo halve memory traffic on large recordings, load frames in single precision with image_type = Float32 (see the precision policy).\n\nFor synchronized stereo recordings, pass one pair list per camera and reuse the calibrated dewarpers across the run:\n\nstereo = run_piv_stereo_sequence(cam1_pairs, cam2_pairs, dw1, dw2, passes;\n    preprocess = (preprocess_cam1, preprocess_cam2),\n    output = \"run_042_stereo.jld2\",\n    progress = (done, total) -> update_progress(done, total),\n    cancel = () -> cancellation_requested[],\n)\n\nmask may also be a vector with one entry per pair or a callback (i, frameA, frameB) -> mask. Return (maskA, maskB) when geometry moves between exposures; Hammerhead excludes their union (true always means excluded). Pass roi=ROI(101:900, 201:1200) to analyze a crop while keeping returned coordinates in the original image frame.\n\nThe driver stops cleanly between acquisitions when cancel() becomes true and returns the completed prefix. Each completed StereoPIVResult is already persisted. A single shared preprocessing function is also accepted. The equivalent one-list layout is (A1, B1, A2, B2) per acquisition. Timestamped camera FramePairs must have matching intervals and attach their actual dt to each scaled stereo result.","title":"Run the sequence with incremental output"},{"category":"section","location":"howto/batch/#Run-the-sequence-on-a-GPU","page":"Batch processing and result files","text":"Load the device package and pass its selector:\n\nusing AMDGPU\nresults = run_piv_sequence(pairs, passes;\n    backend = :amdgpu,\n    image_type = Float32,\n    output = \"run_042_piv.jld2\",\n)\n\nThe sequence driver reuses one device workspace across pairs while the next pair's load and preprocessing are prefetched on the CPU. Output remains incremental and results remain ordinary host-side PIVResult objects. See Run PIV on a GPU for installation, supported options, and when offload pays for itself.","title":"Run the sequence on a GPU"},{"category":"section","location":"howto/batch/#One-file-per-pair","page":"Batch processing and result files","text":"To write each pair to its own JLD2 file instead of one combined file, pass output a function (i, pair) -> outpath (i is the 1-based pair index, pair the original 2-tuple). frame_index_strings pulls the differing frame-index substrings out of a path pair, so the outputs can mirror the inputs:\n\noutdir = \"run_042_piv\"\nrun_piv_sequence(pairs, passes;\n    output = (i, pair) -> begin\n        a, b = frame_index_strings(pair...)      # \"img_0001.tif\",\"img_0002.tif\" → \"0001\",\"0002\"\n        joinpath(outdir, \"piv_$(a)_$(b).jld2\")\n    end,\n)\n\nParent directories are created automatically, and each file is a standalone single-result file (with source paths recorded for file-path pairs) readable with load_results. Fall back to the index i when frames aren't named by index. The same output function works for run_ptv_sequence.","title":"One file per pair"},{"category":"section","location":"howto/batch/#Read-results-back","page":"Batch processing and result files","text":"results = load_results(\"run_042_piv.jld2\")   # Vector of results, in order\n\nload_results returns PIVResult and/or StereoPIVResult entries in sequence order. The stored source paths, when present, are retrievable directly with JLD2:\n\nusing JLD2\nsources = JLD2.load(\"run_042_piv.jld2\", \"sources/000001\")\n\nStandalone results (e.g. from interactive work) round-trip with save_results / load_results:\n\nsave_results(\"snapshot.jld2\", result)        # single result or a vector\n\nTrackingResult is supported by the same lossless JLD2 persistence. For language-neutral exchange, export_table(\"field.csv\", result) writes the stable hammerhead-table-1 long-form schema. Its fixed columns are TABLE_COLUMNS; planar, stereo, and PTV rows use the same superset and leave inapplicable values empty. Identifiers, flags, quality values, uncertainties, and unit strings are included, and attached scaling is applied. Planar and stereo grids can also be written for ParaView with export_vtk(\"field.vtk\", result) (legacy ASCII structured-grid VTK).","title":"Read results back"},{"category":"section","location":"howto/batch/#Post-process-the-sequence","page":"Batch processing and result files","text":"Sequences of same-grid results feed the statistics utilities directly:\n\nvalidate_temporal!(results)                  # flag temporal outliers in place\nstats = field_statistics(results)            # planar or stereo mean/RMS/stresses/counts\nf, psd = power_spectrum([r.u[12, 8] for r in results]; dt = 1/10_000)","title":"Post-process the sequence"},{"category":"section","location":"howto/batch/#A-note-on-failure","page":"Batch processing and result files","text":"If a pair fails (unreadable file, size mismatch), run_piv_sequence logs which pair and rethrows — the incremental output file retains everything processed up to that point, so you can fix the offending frame and resume from a trimmed pair list.","title":"A note on failure"},{"category":"section","location":"reference/io/#Input/output-(I/O)-and-batch-processing","page":"I/O and batch processing","text":"Image and mask loading (FileIO/ImageIO), result serialization in the JLD2 Julia data format, and the batch sequence driver. See the batch-processing how-to for workflow recipes and Run PIV on a GPU for persistent device-workspace behavior.\n\nPages = [\"io.md\"]","title":"Input/output (I/O) and batch processing"},{"category":"method","location":"reference/io/#Hammerhead.frame_index_strings-Tuple{AbstractString, AbstractString}","page":"I/O and batch processing","text":"frame_index_strings(pathA, pathB) -> (strA, strB)\n\nExtract the differing frame-index substrings from a pair of frame paths: strip each path's directory and extension, then return the portions of the two stems that differ, with any shared leading/trailing digits of that field kept intact (so a zero-padded index is not truncated). Handy for naming per-pair output files — see the output keyword of run_piv_sequence.\n\njulia> frame_index_strings(\"path/to/img_0001.tif\", \"path/to/img_0002.tif\")\n(\"0001\", \"0002\")\n\njulia> frame_index_strings(\"a/f_099.png\", \"a/f_100.png\")\n(\"099\", \"100\")\n\nThrows an ArgumentError if the two stems are identical (no differing field).\n\n\n\n\n\n","title":"Hammerhead.frame_index_strings"},{"category":"method","location":"reference/io/#Hammerhead.image_pairs-Tuple{AbstractVector}","page":"I/O and batch processing","text":"image_pairs(files; mode = :paired) -> Vector{Tuple}\n\nGroup an ordered list of frames (file paths or matrices) into correlation pairs for run_piv_sequence:\n\nmode = :paired — double-frame recordings: (1, 2), (3, 4), … (requires an even number of frames).\nmode = :chained — time-resolved recordings: (1, 2), (2, 3), …\n\n\n\n\n\n","title":"Hammerhead.image_pairs"},{"category":"method","location":"reference/io/#Hammerhead.load_image-Union{Tuple{T}, Tuple{Type{T}, AbstractString}} where T<:AbstractFloat","page":"I/O and batch processing","text":"load_image(path)          -> Matrix{Float64}\nload_image(T, path)       -> Matrix{T}\n\nLoad an image file as a grayscale floating-point matrix, ready for run_piv or the preprocessing functions. Color images are converted to grayscale; integer-valued images are scaled by their type's full range (e.g. 16-bit TIFF values map to [0, 1] as n / 65535).\n\nThe element type defaults to Float64; pass T = Float32 to run the downstream pipeline in single precision (see PIVResult).\n\nFormats are dispatched through FileIO: TIFF (including 16-bit) and PNG are supported out of the box; BMP and other exotic formats work when ImageMagick is installed (using ImageMagick).\n\n\n\n\n\n","title":"Hammerhead.load_image"},{"category":"method","location":"reference/io/#Hammerhead.load_mask-Tuple{AbstractString}","page":"I/O and batch processing","text":"load_mask(path; threshold = 0.5, invert = false) -> BitMatrix\n\nLoad an analysis mask from an image file: pixels with grayscale value >= threshold become true (excluded from analysis — see mask in run_piv). Use invert = true when dark pixels mark the excluded region instead.\n\n\n\n\n\n","title":"Hammerhead.load_mask"},{"category":"method","location":"reference/io/#Hammerhead.load_results-Tuple{AbstractString}","page":"I/O and batch processing","text":"load_results(path) -> Vector\n\nLoad the results (PIVResult and/or StereoPIVResult entries) stored in a JLD2 file written by save_results or run_piv_sequence, in sequence order. Files written by run_piv_sequence(...; output = path) from file-path pairs also carry the source image paths, retrievable with JLD2.load(path, \"sources/000001\") etc.\n\n\n\n\n\n","title":"Hammerhead.load_results"},{"category":"method","location":"reference/io/#Hammerhead.run_piv_sequence-Tuple{AbstractVector, Union{PIVParameters, AbstractVector{PIVParameters}}}","page":"I/O and batch processing","text":"run_piv_sequence(pairs, params = PIVParameters();\n                 preprocess = nothing, output = nothing,\n                 progress = true, backend = :cpu, kwargs...) -> Vector{PIVResult}\nrun_piv_sequence(pairs; effort = :low/:medium/:high, kwargs...) -> Vector{PIVResult}\n\nRun PIV over a sequence of image pairs. pairs is a vector of 2-tuples whose entries are file paths (loaded with load_image) and/or real-valued matrices — see image_pairs for building it from a frame list. params is a single PIVParameters or a multi-pass schedule; alternatively, omit it and pass effort = :low, :medium, or :high to use the built-in effort schedules from run_piv. Remaining kwargs (e.g. mask for a static analysis mask shared by all pairs, scale for a shared PhysicalScale, or PIV-parameter overrides when effort is set) are forwarded to run_piv.\n\npreprocess: function applied to each frame after loading, e.g. img -> clahe!(subtract_background!(img, bg)). Frames loaded from file paths are fresh buffers, so mutating preprocessors are safe; in-memory matrix pairs are passed through as-is — use the allocating versions there to leave the caller's arrays untouched.\noutput: either a path of a single JLD2 file (overwritten) to which all results are written incrementally as they complete — so a crashed batch keeps its finished pairs — or a function (i, pair) -> outpath mapping the 1-based pair index and the original pair tuple to a per-pair output path (each written as its own single-result JLD2 file as that pair completes; parent directories are created). For file-path pairs the source paths are stored alongside in either mode. Read any of these back with load_results; see frame_index_strings for building per-pair names from the frame paths.\nprogress: show a progress meter (true/false), or a function (i, n) -> nothing called after each completed pair (for driving an external progress display). Throwing from the callback aborts the batch; pairs finished before the abort stay in output.\non_result: optional function (i, result) -> nothing called with each pair's result immediately after it completes (before the incremental output write and the progress callback) — for consuming results live, e.g. streaming them into a viewer while the batch runs. Runs on the calling task, in pair order. Throwing aborts the batch like a throwing progress callback; pairs already persisted stay in output.\nbackend: execution backend selector. The core package provides :cpu; package extensions add device selectors (see Run PIV on a GPU).\nimage_type: element type frames are loaded as (default Float64); Float32 runs the pipeline in single precision. In-memory matrix pairs are used as-is, so convert those yourself.\n\nThe interpolant, deformation, and correlator scratch is reused across pairs via a single PIVWorkspace (the pairs share an image size), so a batch pays those allocations once; results are bitwise identical to calling run_piv per pair. Because pairs are processed serially — while the next pair's frames are prefetched on a background task that never touches the workspace — this stays race-free.\n\n\n\n\n\n","title":"Hammerhead.run_piv_sequence"},{"category":"function","location":"reference/io/#Hammerhead.run_ptv_sequence","page":"I/O and batch processing","text":"run_ptv_sequence(pairs, params = PTVParameters();\n                 preprocess = nothing, output = nothing,\n                 progress = true, kwargs...) -> Vector{PTVResult}\n\nRun PTV over a sequence of image pairs, mirroring run_piv_sequence: pairs entries are file paths (loaded with load_image) and/or real-valued matrices, params is a PTVParameters, and the same preprocess, output (incremental JLD2), progress, on_result, and image_type options apply. Remaining kwargs (e.g. predictor, mask, or scale) are forwarded to run_ptv. Results are PTVResults; when output is a path they are persisted incrementally as they complete (with source paths for file-path pairs), readable with load_results.\n\n\n\n\n\n","title":"Hammerhead.run_ptv_sequence"},{"category":"method","location":"reference/io/#Hammerhead.save_results-Tuple{AbstractString, AbstractVector{<:Union{PIVResult, PTVResult, StereoPIVResult, TrackingResult}}}","page":"I/O and batch processing","text":"save_results(path, results) -> path\n\nSave a PIVResult, StereoPIVResult, or PTVResult (or a vector of them, possibly mixed) to a JLD2 file (conventionally *.jld2), overwriting path if it exists. Read back with load_results.\n\n\n\n\n\n","title":"Hammerhead.save_results"},{"category":"type","location":"reference/io/#Hammerhead.AbstractFrameSource","page":"I/O and batch processing","text":"Abstract interface for lazily addressable image recordings.\n\n\n\n\n\n","title":"Hammerhead.AbstractFrameSource"},{"category":"type","location":"reference/io/#Hammerhead.FramePair","page":"I/O and batch processing","text":"Pair descriptor carrying source indices and optional timestamp difference.\n\n\n\n\n\n","title":"Hammerhead.FramePair"},{"category":"type","location":"reference/io/#Hammerhead.FrameRef","page":"I/O and batch processing","text":"A frame reference. Calling load_frame materializes only this frame.\n\n\n\n\n\n","title":"Hammerhead.FrameRef"},{"category":"type","location":"reference/io/#Hammerhead.FrameSource","page":"I/O and batch processing","text":"FrameSource(n, loader; timestamps=nothing, labels=nothing)\n\nCreate a lazy source around loader(i). Camera-format adapters only need to provide a frame count and an index loader; optional timestamps are carried into FramePair.dt by image_pairs.\n\n\n\n\n\n","title":"Hammerhead.FrameSource"},{"category":"type","location":"reference/io/#Hammerhead.ROI","page":"I/O and batch processing","text":"ROI(rows, cols)\n\nRectangular image region used by run_piv, run_ptv, and the sequence drivers. rows and cols are integer unit ranges in the original image. Returned coordinates remain in that original image coordinate system.\n\n\n\n\n\n","title":"Hammerhead.ROI"},{"category":"type","location":"reference/io/#Hammerhead.TIFFStack","page":"I/O and batch processing","text":"TIFFStack(path; image_type=Float64)\n\nOpen a multi-page TIFF as a frame source. The TIFF container is opened once; individual pages are converted to matrices only when indexed. Single-page TIFFs are valid one-frame stacks.\n\n\n\n\n\n","title":"Hammerhead.TIFFStack"},{"category":"method","location":"reference/io/#Hammerhead.export_table-Tuple{AbstractString, Union{PIVResult, PTVResult, StereoPIVResult}}","page":"I/O and batch processing","text":"export_table(path, result; frame_id=\"\", source_a=\"\", source_b=\"\")\n\nWrite a long-form UTF-8 CSV using the stable hammerhead-table-1 schema. Planar, stereo, and PTV exports share the same columns; non-applicable values are empty. Attached physical scaling is applied and its units are recorded.\n\n\n\n\n\n","title":"Hammerhead.export_table"},{"category":"method","location":"reference/io/#Hammerhead.export_vtk-Tuple{AbstractString, Union{PIVResult, StereoPIVResult}}","page":"I/O and batch processing","text":"export_vtk(path, result)\n\nWrite a planar or stereo result as an ASCII VTK legacy structured grid. The file contains point coordinates, a three-component velocity vector, mask and outlier flags, and available uncertainty/quality scalar arrays.\n\n\n\n\n\n","title":"Hammerhead.export_vtk"},{"category":"method","location":"reference/io/#Hammerhead.image_pairs-Tuple{AbstractFrameSource}","page":"I/O and batch processing","text":"image_pairs(source; mode=:paired, stride=1, offset=0, deltas=(1,))\n\nBuild lazy pairs from a frame source, with arbitrary start offset, sampling stride, and one or more frame separations (deltas). Timestamped sources attach the actual time difference to each returned FramePair.\n\n\n\n\n\n","title":"Hammerhead.image_pairs"},{"category":"section","location":"reference/stereo/#Calibration,-dewarping,-and-stereo","page":"Calibration, dewarping, and stereo","text":"Everything stereoscopic: two-dimensional, three-component (2D3C) particle image velocimetry (PIV), including camera calibration models and fitting, dot-grid target detection, image dewarping onto a common world plane, three-component reconstruction, synchronized sequence processing, stereo ensemble correlation, and disparity self-calibration. The stereo tutorial walks the whole chain; Stereo geometry and self-calibration explains the method. run_piv_stereo, run_piv_stereo_sequence, and run_piv_stereo_ensemble can forward a GPU backend to their two per-camera PIV calls; see Run PIV on a GPU for the CPU/GPU boundary.\n\nPages = [\"stereo.md\"]","title":"Calibration, dewarping, and stereo"},{"category":"section","location":"reference/stereo/#Camera-models-and-calibration","page":"Calibration, dewarping, and stereo","text":"This section also includes PlanarTransform and planar_calibration, the lightweight two-point calibration for planar 2D2C work.","title":"Camera models and calibration"},{"category":"section","location":"reference/stereo/#Calibration-target-detection","page":"Calibration, dewarping, and stereo","text":"","title":"Calibration-target detection"},{"category":"section","location":"reference/stereo/#Image-dewarping","page":"Calibration, dewarping, and stereo","text":"","title":"Image dewarping"},{"category":"section","location":"reference/stereo/#Stereo-reconstruction","page":"Calibration, dewarping, and stereo","text":"","title":"Stereo reconstruction"},{"category":"section","location":"reference/stereo/#Self-calibration","page":"Calibration, dewarping, and stereo","text":"","title":"Self-calibration"},{"category":"type","location":"reference/stereo/#Hammerhead.CameraCalibration","page":"Calibration, dewarping, and stereo","text":"CameraCalibration\n\nAbstract supertype for camera mapping models (PinholeCamera, SoloffCamera). Subtypes implement world_to_pixel and pixel_to_world; fit either with calibrate_camera.\n\n\n\n\n\n","title":"Hammerhead.CameraCalibration"},{"category":"type","location":"reference/stereo/#Hammerhead.PinholeCamera","page":"Calibration, dewarping, and stereo","text":"PinholeCamera(P::AbstractMatrix)\nPinholeCamera(K, R, t)\n\nPinhole (projective / DLT) camera: world point (X, Y, Z) maps to the pixel (x, y) = (h₁/h₃, h₂/h₃) where h = P * [X, Y, Z, 1] and P is the 3×4 projection matrix. The second form builds P = K * [R t] from a 3×3 intrinsic matrix K, a 3×3 rotation R, and a translation 3-vector t.\n\nP is stored normalized so that its third row's rotational part is a unit vector (making h₃ the point's depth along the optical axis). Fit from point correspondences with calibrate_camera(...; model = :pinhole); the fit requires non-coplanar world points, i.e. a calibration target imaged at two or more Z positions.\n\n\n\n\n\n","title":"Hammerhead.PinholeCamera"},{"category":"type","location":"reference/stereo/#Hammerhead.PlanarTransform","page":"Calibration, dewarping, and stereo","text":"PlanarTransform\n\nExplicit affine transform from image (x,y) coordinates to planar physical coordinates. matrix supports rotation, reflection, and anisotropic scales; offset sets the physical origin.\n\n\n\n\n\n","title":"Hammerhead.PlanarTransform"},{"category":"type","location":"reference/stereo/#Hammerhead.SoloffCamera","page":"Calibration, dewarping, and stereo","text":"SoloffCamera\n\nSoloff polynomial camera (Soloff, Adrian & Liu 1997): each pixel coordinate is a 19-term polynomial in the world coordinates — cubic in X and Y, quadratic in Z — evaluated on internally normalized coordinates. The polynomial absorbs lens distortion and refraction that a pinhole model cannot, at the cost of having no closed-form inverse (pixel_to_world uses Newton iteration).\n\nFit from point correspondences with calibrate_camera(...; model = :soloff); the fit requires calibration points at three or more distinct Z positions.\n\n\n\n\n\n","title":"Hammerhead.SoloffCamera"},{"category":"type","location":"reference/stereo/#Hammerhead.TransformedCamera","page":"Calibration, dewarping, and stereo","text":"TransformedCamera(cam::CameraCalibration, R, t)\n\nCamera model pre-composed with a rigid world-coordinate transform: a point w in the transformed (new) world frame maps to the pixel world_to_pixel(cam, R * w + t), where the 3×3 rotation R and 3-vector t express the new frame in the original calibration's frame. Wrapping a TransformedCamera collapses into a single composed transform.\n\nProduced by self_calibrate when correcting camera models whose functional form cannot absorb a world rotation exactly (e.g. SoloffCamera — a PinholeCamera correction is instead baked directly into its projection matrix). Behaves like any other CameraCalibration: world_to_pixel, pixel_to_world, and ImageDewarper all apply.\n\n\n\n\n\n","title":"Hammerhead.TransformedCamera"},{"category":"method","location":"reference/stereo/#Hammerhead.calibrate_camera-Tuple{AbstractVector, AbstractVector}","page":"Calibration, dewarping, and stereo","text":"calibrate_camera(pixel_points, world_points; model = :soloff) -> CameraCalibration\n\nFit a camera model to corresponding calibration points. pixel_points are 2-element indexables (x, y) (pixels, x along columns) and world_points 3-element indexables (X, Y, Z) (physical units); typically these come from a dot-grid target via detect_calibration_grid and calibration_points, imaged at several Z positions.\n\nmodel = :soloff (default): SoloffCamera, 19-term polynomial; needs ≥ 19 points on ≥ 3 distinct Z planes.\nmodel = :pinhole: PinholeCamera via the normalized DLT; needs ≥ 6 non-coplanar points (≥ 2 Z planes).\n\nCheck the fit with calibration_quality.\n\n\n\n\n\n","title":"Hammerhead.calibrate_camera"},{"category":"method","location":"reference/stereo/#Hammerhead.calibration_quality-Tuple{CameraCalibration, AbstractVector, AbstractVector}","page":"Calibration, dewarping, and stereo","text":"calibration_quality(cam::CameraCalibration, pixel_points, world_points)\n    -> (rms, max, n)\n\nSummary of the reprojection error of cam over the given correspondences: root-mean-square and maximum error in pixels, and the number of points.\n\n\n\n\n\n","title":"Hammerhead.calibration_quality"},{"category":"method","location":"reference/stereo/#Hammerhead.pixel_to_world-Tuple{PinholeCamera, Any, Real}","page":"Calibration, dewarping, and stereo","text":"pixel_to_world(cam::CameraCalibration, pixel, z) -> SVector{3,Float64}\n\nMap pixel coordinates (x, y) back to the world point (X, Y, z) on the plane at out-of-plane position z. For a PinholeCamera this is an exact linear solve (the ray–plane intersection); for a SoloffCamera or TransformedCamera it is a 2D Newton iteration and returns a NaN-filled point if the iteration fails to converge (e.g. for pixels far outside the calibrated domain).\n\n\n\n\n\n","title":"Hammerhead.pixel_to_world"},{"category":"method","location":"reference/stereo/#Hammerhead.planar_calibration-Tuple{Tuple{Real, Real}, Tuple{Real, Real}, Real}","page":"Calibration, dewarping, and stereo","text":"planar_calibration(p1, p2, distance; origin=(0,0), reflection=false,\n                   perpendicular_scale=nothing)\n\nBuild a two-point planar calibration. The physical x axis runs from image point p1 to p2 and has the supplied physical distance; origin is the physical coordinate assigned to p1. By default pixels are isotropic. perpendicular_scale supplies a separate physical-units-per-pixel y scale, and reflection=true reverses that physical y axis.\n\n\n\n\n\n","title":"Hammerhead.planar_calibration"},{"category":"method","location":"reference/stereo/#Hammerhead.reprojection_errors-Tuple{CameraCalibration, AbstractVector, AbstractVector}","page":"Calibration, dewarping, and stereo","text":"reprojection_errors(cam::CameraCalibration, pixel_points, world_points) -> Vector{Float64}\n\nPer-point Euclidean distance (pixels) between each measured pixel position and the projection of its world point through cam.\n\n\n\n\n\n","title":"Hammerhead.reprojection_errors"},{"category":"method","location":"reference/stereo/#Hammerhead.world_to_pixel-Tuple{PinholeCamera, Any}","page":"Calibration, dewarping, and stereo","text":"world_to_pixel(cam::CameraCalibration, world) -> SVector{2,Float64}\n\nMap a world point (X, Y, Z) (any 3-element indexable) to its pixel coordinates (x, y) (x along columns, y along rows).\n\n\n\n\n\n","title":"Hammerhead.world_to_pixel"},{"category":"type","location":"reference/stereo/#Hammerhead.CalibrationGrid","page":"Calibration, dewarping, and stereo","text":"CalibrationGrid\n\nDetected calibration-target dot grid (see detect_calibration_grid).\n\nFields\n\npixels: dot centroids in pixel coordinates (x along columns, y along rows), intensity-weighted, subpixel.\nindices: integer dot positions in units of half the dot spacing along the world X/Y axes; the origin dot is (0, 0). Front-level dots have two even indices, back-level dots two odd indices.\nlevel: 0 for dots on the reference (\"front\") level, 1 for the other level (all zero for single-level targets).\nspacing: dot spacing on one level, world units.\nlevel_separation: Z offset of level 1 behind level 0, world units.\nsquare, triangle: fiducial marker centroids in pixel coordinates, or nothing when not found.\n\n\n\n\n\n","title":"Hammerhead.CalibrationGrid"},{"category":"method","location":"reference/stereo/#Hammerhead.calibrate_camera-Tuple{AbstractVector{CalibrationGrid}, AbstractVector{<:Real}}","page":"Calibration, dewarping, and stereo","text":"calibrate_camera(grids::AbstractVector{CalibrationGrid}, zs; model = :soloff)\n\nCalibrate a camera from dot grids detected at several plate positions zs (reference-level Z of each grid, world units).\n\n\n\n\n\n","title":"Hammerhead.calibrate_camera"},{"category":"method","location":"reference/stereo/#Hammerhead.calibration_points-Tuple{CalibrationGrid, Real}","page":"Calibration, dewarping, and stereo","text":"calibration_points(grid::CalibrationGrid, z) -> (pixel_points, world_points)\n\nConvert a detected grid into calibration point pairs: pixel centroids and world (X, Y, Z) positions with the reference level of the plate at out-of-plane position z (level-1 dots sit at z - level_separation). Concatenate the pairs from several plate positions and feed them to calibrate_camera, or use the calibrate_camera(grids, zs) convenience method.\n\n\n\n\n\n","title":"Hammerhead.calibration_points"},{"category":"method","location":"reference/stereo/#Hammerhead.calibration_quality-Tuple{CameraCalibration, AbstractVector{CalibrationGrid}, AbstractVector{<:Real}}","page":"Calibration, dewarping, and stereo","text":"calibration_quality(cam::CameraCalibration, grids::AbstractVector{CalibrationGrid}, zs)\n    -> (rms, max, n)\n\nReprojection-error summary of cam over dot grids detected at several plate positions zs, as accepted by calibrate_camera(grids, zs).\n\n\n\n\n\n","title":"Hammerhead.calibration_quality"},{"category":"method","location":"reference/stereo/#Hammerhead.detect_calibration_grid-Tuple{AbstractMatrix{<:Real}}","page":"Calibration, dewarping, and stereo","text":"detect_calibration_grid(img; spacing, kwargs...) -> CalibrationGrid\n\nDetect a rectilinear calibration dot grid in img (grayscale, any real element type) and index it: dots are found as bright blobs (intensity-weighted subpixel centroids), assigned integer lattice positions, and anchored to a world coordinate frame. World +X is the lattice direction closest to image-right, +Y the one closest to image-up; the origin dot is chosen from the square fiducial marker when present (see origin_offset), otherwise the dot nearest the image center. Dots clipped by the image border are excluded.\n\nKeywords\n\nspacing (required): dot spacing on one level, world units (e.g. mm).\ntwo_level = false: two-level plate (back-level dots at half-spacing diagonal offsets, e.g. LaVision type 21/31 plates). Requires level_separation.\nlevel_separation = 0.0: Z distance by which level 1 sits behind the reference level (level 1 world Z = z - level_separation in calibration_points; may be negative if the plate faces the other way).\norigin_level = :front: which level the origin dot belongs to; that level becomes level 0, the Z reference.\norigin_offset = nothing: world-units (ΔX, ΔY) from the square marker centroid to the origin dot (e.g. (30.0, 7.5) for PIV Challenge case 4E: \"the dot 30 mm right of and 7.5 mm above the square marker\"). When nothing, the dot nearest the square marker (or the image center when no marker is found) becomes the origin. A consistent origin across cameras requires the marker.\ninvert = false: set for dark dots on a bright background.\norientation = :image: :image keeps +X image-right and +Y image-up. :fiducials instead uses the square-to-triangle direction as +X and plate handedness for +Y, so arbitrary camera roll does not change world indices; both markers must be visible.\nthreshold = :otsu: foreground threshold — :otsu or an absolute value in image intensity units.\nmin_area = 9: minimum blob area in pixels; blobs smaller than 15% of the median blob area are dropped as well.\n\n\n\n\n\n","title":"Hammerhead.detect_calibration_grid"},{"category":"method","location":"reference/stereo/#Hammerhead.render_calibration_target-Tuple{CameraCalibration, Tuple{Int64, Int64}}","page":"Calibration, dewarping, and stereo","text":"render_calibration_target(cam::CameraCalibration, image_size; spacing, kwargs...)\n    -> Matrix{Float64}\n\nRender a synthetic calibration-plate image as seen through cam: the ground-truth fixture for testing target detection and calibration fitting (and, downstream, the stereo reconstruction chain). Dots are anti-aliased by supersampled back-projection onto the plate plane(s), so each rendered dot's centroid is the exact projection of its world-lattice position.\n\nKeywords\n\nspacing (required): dot spacing on one level, world units.\nz = 0.0: world Z of the front (reference) level.\ndot_diameter = 0.21 * spacing: dot diameter, world units.\ntwo_level = false, level_separation = 0.0: back level at z - level_separation, dots offset by half a spacing in X and Y.\ndot_intensity = 0.85, back_intensity = 0.65, background = 0.05.\nmarker_square, marker_triangle: world (X, Y) centers of fiducial markers on the front level, or nothing.\nmarker_size = 1.1 * dot_diameter: marker side length, world units.\nsupersample = 3: antialiasing subsamples per pixel axis.\n\n\n\n\n\n","title":"Hammerhead.render_calibration_target"},{"category":"type","location":"reference/stereo/#Hammerhead.DewarpGrid","page":"Calibration, dewarping, and stereo","text":"DewarpGrid(; x, y, z = 0.0)\n\nRegular world-plane pixel grid for image dewarping: x and y are ranges of world coordinates (physical units, e.g. mm) spanning the measurement region, and z is the out-of-plane position of the measurement plane. The grid's resolution is step(x) / step(y) world units per dewarped pixel; choose it finer than the target vector spacing, since correlation windows live on the dewarped images.\n\nA dewarped image is indexed [row, col] with row running along y and col along x in the order given: out[r, c] shows the world point (x[c], y[r], z). With an ascending y, world +Y therefore points down the image; pass a descending y range for a display-oriented (+Y up) image. Displacements measured on dewarped images convert to world units as du * step(x), dv * step(y) — signs included.\n\nOne DewarpGrid is shared by all cameras of a stereo rig (build one ImageDewarper per camera on it), and the stereo vector grid is later derived from it.\n\n\n\n\n\n","title":"Hammerhead.DewarpGrid"},{"category":"type","location":"reference/stereo/#Hammerhead.ImageDewarper","page":"Calibration, dewarping, and stereo","text":"ImageDewarper(cam::CameraCalibration, grid::DewarpGrid, image_size)\n\nPrecomputed dewarping map for one camera: for every node of grid the source pixel coordinate world_to_pixel(cam, (X, Y, grid.z)) is evaluated once and stored, so applying the map to a frame (dewarp!) is a pure resampling — build the dewarper once per camera and reuse it across a whole sequence. image_size is the camera's image size (rows, cols).\n\nGrid nodes whose source coordinate falls outside the camera's image (or is non-finite) are recorded in mask, a grid-sized BitMatrix in the package mask convention (true = excluded, static lab-frame geometry) — pass it to run_piv(...; mask) on the dewarped images, and combine the cameras' masks with .| for the stereo overlap region.\n\n\n\n\n\n","title":"Hammerhead.ImageDewarper"},{"category":"function","location":"reference/stereo/#Hammerhead.common_dewarp_grid","page":"Calibration, dewarping, and stereo","text":"common_dewarp_grid(cameras, image_size, z = 0.0;\n                   spacing = :auto, margin = 0.0,\n                   coverage = :intersection) -> DewarpGrid\n\nConstruct a DewarpGrid covering the world-plane region that a set of cameras jointly image at out-of-plane position z — the grid-construction step of a stereo (or multi-camera) analysis, without hand-tuning extents.\n\ncameras is an iterable of CameraCalibrations; image_size is the pixel size (rows, cols) used for every camera, or a vector of per-camera sizes. Each camera's image border is projected to the z plane (pixel_to_world, ~50 samples per edge) and reduced to an axis-aligned world bounding box.\n\ncoverage = :intersection (default — the stereo case) keeps only the region all cameras see; :union keeps everything any camera sees (out-of-view nodes are still flagged per camera in ImageDewarper's mask). An empty intersection throws.\nmargin (world units) shrinks the region on all sides when positive, grows it when negative.\nspacing = :auto estimates each camera's median world-units-per-pixel over its footprint and uses the coarsest camera's value, so no camera is resampled below its native resolution; pass a Real to set it directly.\n\nThe y range is built descending so the dewarped image displays upright (world +Y up); PIV downstream is orientation-agnostic (dv * step(y) carries the sign — see DewarpGrid).\n\n\n\n\n\n","title":"Hammerhead.common_dewarp_grid"},{"category":"method","location":"reference/stereo/#Hammerhead.dewarp!-Tuple{AbstractMatrix{<:AbstractFloat}, ImageDewarper, AbstractMatrix{<:Real}}","page":"Calibration, dewarping, and stereo","text":"dewarp!(out, dw::ImageDewarper, img) -> out\n\nResample the camera frame img onto dw's world grid, writing into out (size size(dw.grid)). Sampling uses cubic B-spline interpolation at the precomputed source coordinates; values are computed in the image's precision (float(eltype(img))). Out-of-view grid nodes (see dw.mask) are filled with zero.\n\n\n\n\n\n","title":"Hammerhead.dewarp!"},{"category":"method","location":"reference/stereo/#Hammerhead.dewarp-Tuple{ImageDewarper, AbstractMatrix{<:Real}}","page":"Calibration, dewarping, and stereo","text":"dewarp(dw::ImageDewarper, img) -> Matrix\n\nAllocating form of dewarp!: returns the dewarped image as a Matrix{float(eltype(img))} of size size(dw.grid).\n\n\n\n\n\n","title":"Hammerhead.dewarp"},{"category":"type","location":"reference/stereo/#Hammerhead.StereoPIVResult","page":"Calibration, dewarping, and stereo","text":"StereoPIVResult{T<:AbstractFloat}\n\nResult of run_piv_stereo. The numeric precision T follows the input images, like PIVResult.\n\nFields\n\nx, y: world coordinates of the vector grid (x along the dewarp grid's x range, y along its y range, in the grid's world units, e.g. mm).\nz: out-of-plane position of the measurement plane (the DewarpGrid's z).\nu, v, w: displacement components on the (length(y), length(x)) grid, in world units per frame interval: u along world X, v along world Y (signs follow the dewarp grid's ranges), w along world +Z.\nuncertainty_u, uncertainty_v, uncertainty_w: per-vector measurement uncertainty (one standard deviation, world units) propagated from the two cameras' correlation-statistics estimates through the reconstruction — NaN unless the uncertainty parameter was enabled.\noutliers: union of the two cameras' outlier flags. Flagged vectors were reconstructed from at least one replaced/substituted 2C vector.\nmask: windows dropped because they overlap either camera's out-of-view region or the user mask (NaN fields, never outliers).\ncam1, cam2: the per-camera 2C PIVResults on the dewarped images (displacements in dewarped pixels), retained for diagnostics.\nparameters: the PIVParameters of the (final) pass.\nscale: the PhysicalScale attached via the scale keyword of run_piv_stereo or with_scale; nothing when none was attached. For stereo, set dt (and the unit labels) only and leave pixel_size = 1 — the arrays are already in world units. Metadata only until physical converts them; cam1/cam2 always stay raw.\n\n\n\n\n\n","title":"Hammerhead.StereoPIVResult"},{"category":"method","location":"reference/stereo/#Hammerhead.run_piv_stereo-Tuple{AbstractMatrix{<:Real}, AbstractMatrix{<:Real}, AbstractMatrix{<:Real}, AbstractMatrix{<:Real}, ImageDewarper, ImageDewarper, Union{PIVParameters, AbstractVector{PIVParameters}}}","page":"Calibration, dewarping, and stereo","text":"run_piv_stereo(A1, B1, A2, B2, dw1, dw2,\n               params = PIVParameters(); mask = nothing, kwargs...)\n    -> StereoPIVResult\nrun_piv_stereo(A1, B1, A2, B2, dw1, dw2;\n               effort = :low/:medium/:high, mask = nothing, kwargs...)\n\nStereoscopic (2D3C) PIV on one frame pair: A1/B1 are camera 1's raw frames and A2/B2 camera 2's, dw1/dw2 the cameras' ImageDewarpers (which must share one DewarpGrid). Each camera's pair is dewarped onto the common world plane, analyzed with the 2D engine (run_piv with params, which may be a multi-pass schedule, or with effort = :low, :medium, or :high), and the two 2C fields are combined per vector into the three-component displacement (u, v, w) in world units per frame interval (no time scaling is applied).\n\nReconstruction: at each grid point, camera i's dewarped in-plane displacement measures uᵢ = dx - dz·tXᵢ, vᵢ = dy - dz·tYᵢ, where (tXᵢ, tYᵢ) is the in-plane drift per unit Z along that camera's viewing ray (evaluated from the calibration). The four equations are solved for (dx, dy, dz) by unweighted least squares; points with degenerate geometry (parallel viewing rays, e.g. identical cameras) come out NaN. With the uncertainty parameter enabled, the per-camera Wieneke (2015) estimates are propagated through the same least-squares operator into uncertainty_u/v/w (world units, assuming independent per-camera errors).\n\nmask is an optional grid-sized Bool matrix of world-plane pixels to exclude (true = excluded); it is combined with the dewarpers' out-of-view masks (dw1.mask .| dw2.mask), so only the stereo overlap region is analyzed. scale attaches a PhysicalScale to the stereo result (not to the per-camera results): set dt and the unit labels only, leaving pixel_size = 1 — the stereo fields are already in world units, so physical only needs to divide by the frame interval. Remaining keyword arguments (threaded, predictor_smoothing, backend, mask_threshold) are forwarded to run_piv. A GPU backend accelerates the two per-camera PIV analyses; dewarping and reconstruction remain on the CPU (see Run PIV on a GPU).\n\nBoth cameras are analyzed with the same parameters and mask, so their vector grids, masks, and (via the union) outlier maps are directly compatible; the per-camera results are retained in the returned StereoPIVResult.\n\n\n\n\n\n","title":"Hammerhead.run_piv_stereo"},{"category":"method","location":"reference/stereo/#Hammerhead.run_piv_stereo_ensemble-Tuple{AbstractVector, AbstractVector, ImageDewarper, ImageDewarper, Union{PIVParameters, AbstractVector{PIVParameters}}}","page":"Calibration, dewarping, and stereo","text":"run_piv_stereo_ensemble(pairs1, pairs2, dw1, dw2,\n                        params = PIVParameters(); kwargs...) -> StereoPIVResult\n\nLow-SNR stereoscopic PIV by composing two synchronized run_piv_ensemble analyses followed by the same calibrated 3C reconstruction used by run_piv_stereo. The two camera pair lists must have equal nonzero length. Frames are loaded, optionally preprocessed, and dewarped lazily on every ensemble pass; the whole dewarped recording is never retained in memory. preprocess may be shared or a camera-specific 2-tuple. The dewarper overlap and optional world-grid mask are applied to both cameras. Other keywords follow run_piv_ensemble, including effort, backend, image_type, and progress.\n\nThe result estimates one stationary ensemble-mean 3C field. Its propagated uncertainty is the noise-driven uncertainty of that mean, not physical pair-to-pair turbulence; use field_statistics on a stereo sequence for the latter.\n\n\n\n\n\n","title":"Hammerhead.run_piv_stereo_ensemble"},{"category":"method","location":"reference/stereo/#Hammerhead.run_piv_stereo_sequence-Tuple{AbstractVector, AbstractVector, ImageDewarper, ImageDewarper, Union{PIVParameters, AbstractVector{PIVParameters}}}","page":"Calibration, dewarping, and stereo","text":"run_piv_stereo_sequence(pairs1, pairs2, dw1, dw2, params = PIVParameters(); kwargs...)\nrun_piv_stereo_sequence(acquisitions, dw1, dw2, params = PIVParameters(); kwargs...)\n\nProcess a synchronized stereo recording. pairs1 and pairs2 are equal-length camera pair lists in the same format accepted by run_piv_sequence. As a convenience, acquisitions may instead contain 4-tuples (A1, B1, A2, B2). The supplied ImageDewarpers, a dewarped-image buffer pair, and one PIVWorkspace are reused for the whole sequence.\n\npreprocess may be one function shared by both cameras or a tuple (preprocess1, preprocess2); each hook runs after loading and before dewarping. output is either one incrementally written JLD2 path or a function (i, acquisition) -> path for per-acquisition files. Four source paths are recorded when all four inputs are paths. progress and on_result have the same callback contracts as run_piv_sequence (the latter receives each StereoPIVResult as it completes). cancel may be a zero-argument predicate; when it becomes true, processing stops between acquisitions and the completed prefix is returned (and remains persisted). The next synchronized acquisition is prefetched while the current one runs. Timestamped FramePairs attach their actual pair-specific dt to each result when scale is supplied; the two cameras' intervals must agree.\n\nPass explicit params, or omit them and use effort = :low, :medium, or :high. Other keywords are forwarded to run_piv_stereo.\n\n\n\n\n\n","title":"Hammerhead.run_piv_stereo_sequence"},{"category":"type","location":"reference/stereo/#Hammerhead.SelfCalibrationReport","page":"Calibration, dewarping, and stereo","text":"SelfCalibrationReport\n\nDiagnostics returned by self_calibrate.\n\nFields\n\npasses: per-measurement diagnostics (SelfCalPass); entries with plane === nothing measured without correcting (the final verification).\nconverged: whether the residual disparity RMS reached tol.\ntol: the convergence tolerance used (dewarped pixels).\nR, t: the cumulative rigid world transform — a point w of the corrected frame sits at R * w + t in the original calibration frame (the fitted sheet plane maps onto the corrected frame's z = grid.z plane).\ndisparity_maps: the disparity PIVResult of every pass when keep_disparity_maps = true (camera-1 → camera-2 displacements in dewarped pixels); empty otherwise.\n\n\n\n\n\n","title":"Hammerhead.SelfCalibrationReport"},{"category":"method","location":"reference/stereo/#Hammerhead.self_calibrate-Tuple{AbstractVector, AbstractVector, ImageDewarper, ImageDewarper}","page":"Calibration, dewarping, and stereo","text":"self_calibrate(frames1, frames2, dw1, dw2; kwargs...)\n    -> (dw1′, dw2′, report)\n\nDisparity self-calibration (Wieneke 2005): correct residual misalignment between the calibration-target plane and the actual laser sheet. The two cameras' dewarped images of the same instant should coincide; the residual disparity field (camera 1 vs camera 2, ensemble sum-of-correlation over all instants) is triangulated to world points on the true sheet, a plane is fitted through them, and both camera models are rigidly transformed so the fitted plane becomes the measurement plane z = grid.z — repeated until the disparity RMS drops below tol or iterations corrections have been applied, then verified with a final disparity measurement.\n\nframes1 and frames2 are vectors of raw same-instant frames of cameras 1 and 2 (file paths and/or matrices, as in run_piv_sequence); entry k of both must show the same instant. Wieneke recommends 5–50 instants for a well-shaped ensemble correlation peak; a single dense image per camera also works and may be passed directly as self_calibrate(A1, A2, dw1, dw2). dw1/dw2 are the cameras' ImageDewarpers sharing one DewarpGrid.\n\nReturns replacement dewarpers built from the corrected cameras on the same grid (drop-in for run_piv_stereo; the corrected models are dw1′.cam/dw2′.cam — a corrected PinholeCamera stays a PinholeCamera, other models come back wrapped in a TransformedCamera) and a SelfCalibrationReport with per-pass diagnostics and the cumulative world transform. When the first measurement is already below tol, the input dewarpers are returned as-is.\n\nKeyword arguments\n\nparams = nothing: PIVParameters (or multi-pass schedule) for the disparity correlation; the default is the paper's recipe — a single pass of large windows (128 px or less on small grids) with 50% overlap, padded and Gaussian-apodized.\niterations = 3: maximum number of plane-fit corrections.\ntol = 0.05: convergence threshold on the disparity RMS (dewarped px).\nmax_triangulation_error = 0.5: reject disparity vectors whose triangulation residual exceeds this (dewarped px) as false correlations.\nmask = nothing: grid-sized Bool matrix of world-plane pixels to exclude (true = excluded), combined with the dewarpers' out-of-view masks as in run_piv_stereo.\nkeep_disparity_maps = false: retain every pass's disparity PIVResult in the report (for troubleshooting; off for production runs — the scalar diagnostics are always recorded).\npreprocess = nothing: function applied to each raw frame before dewarping (e.g. background subtraction).\nimage_type = Float64: precision for frames loaded from file paths.\nprogress = false: show the ensemble-correlation progress meter.\nRemaining keywords (threaded, ...) are forwarded to run_piv_ensemble.\n\n\n\n\n\n","title":"Hammerhead.self_calibrate"},{"category":"section","location":"reference/gui/#Graphical-user-interface-(GUI;-HammerheadGUI)","page":"GUI (HammerheadGUI)","text":"The desktop GUI companion package, built on GLMakie. Each component is a framework-free controller (plain Julia + Observables, in the HammerheadGUI.Controllers submodule) paired with a GLMakie view function that renders it and forwards user input into it.\n\nPages = [\"gui.md\"]","title":"Graphical user interface (GUI; HammerheadGUI)"},{"category":"section","location":"reference/gui/#Application-and-views","page":"GUI (HammerheadGUI)","text":"","title":"Application and views"},{"category":"section","location":"reference/gui/#Controllers","page":"GUI (HammerheadGUI)","text":"Application state and logic, testable without a GL context — the submodule never imports Makie.","title":"Controllers"},{"category":"module","location":"reference/gui/#HammerheadGUI.HammerheadGUI","page":"GUI (HammerheadGUI)","text":"HammerheadGUI\n\nDesktop GUI for Hammerhead.jl (particle image velocimetry) built on GLMakie.\n\nApplication state and logic live in the framework-free Controllers submodule (plain Julia + Observables — no Makie in scope); the view layer renders controllers and pushes user input into them, so the widget shell stays swappable and the logic testable without a GL context.\n\n\n\n\n\n","title":"HammerheadGUI.HammerheadGUI"},{"category":"method","location":"reference/gui/#HammerheadGUI.batch_runner-Tuple{}","page":"GUI (HammerheadGUI)","text":"batch_runner(bc = BatchRunner(); size = (960, 720)) -> Figure\n\nOpen the parameter form + batch runner. Pick frames (\"add frames…\") and the pairing mode, edit the multi-pass window schedule (\"64, 32, 32\" style) and the correlation/validation options, optionally choose an incremental JLD2 output file and an analysis mask image, then run — progress and status update live, \"cancel\" stops after the pair in flight, and \"view results\" opens the batch in result_explorer as soon as the first pair is done: pairs finishing later append into the open explorer live (the frame slider grows with the run).\n\nPass a prebuilt BatchRunner to seed the form (e.g. with in-memory frames) or to drive it programmatically.\n\n\n\n\n\n","title":"HammerheadGUI.batch_runner"},{"category":"method","location":"reference/gui/#HammerheadGUI.calibration_review!-Tuple{Any, CalibrationReview}","page":"GUI (HammerheadGUI)","text":"calibration_review!(target, cr::CalibrationReview) -> GridLayout\n\nBuild the calibration-review view into target (a GridPosition), for embedding in a larger layout — the embeddable form, like result_explorer! (used by stereo_calibration to show both cameras side by side).\n\n\n\n\n\n","title":"HammerheadGUI.calibration_review!"},{"category":"method","location":"reference/gui/#HammerheadGUI.calibration_review-Tuple{Any, Any}","page":"GUI (HammerheadGUI)","text":"calibration_review(images, zs; model = :soloff, detect_kwargs...) -> Figure\ncalibration_review(cr::CalibrationReview) -> Figure\n\nOpen the calibration review: each plate image with its detected dots colored by reprojection error (fiducial markers outlined in cyan), a plane slider, a camera-model menu (soloff / pinhole — switching refits), and per-plane + overall fit summaries. detect_kwargs (spacing, origin_offset, two_level, …) are forwarded to Hammerhead.detect_calibration_grid.\n\n\n\n\n\n","title":"HammerheadGUI.calibration_review"},{"category":"method","location":"reference/gui/#HammerheadGUI.mask_editor-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"mask_editor(source; size = (1000, 700)) -> Figure\n\nOpen the mask editor on source: an image matrix, an image-file path (read with Hammerhead.load_image), or a prebuilt MaskEditor controller — pass the controller when you want the drawn mask back programmatically (polygon_mask(editor)), or to seed existing polygons.\n\nEditing model: left-click adds vertices (on empty background it starts a new polygon; inside an existing polygon it selects it), right-click closes the active polygon, Backspace undoes the last vertex, Delete removes the selected polygon. \"Save mask…\" writes the white-=-excluded grayscale image Hammerhead.load_mask reads back.\n\n\n\n\n\n","title":"HammerheadGUI.mask_editor"},{"category":"method","location":"reference/gui/#HammerheadGUI.preprocess_preview!-Tuple{Any, PreprocessPreview}","page":"GUI (HammerheadGUI)","text":"preprocess_preview!(target, pp::PreprocessPreview) -> GridLayout\n\nBuild the preprocess-preview view into target (a GridPosition), for embedding in a larger layout — the embeddable form, like result_explorer!.\n\n\n\n\n\n","title":"HammerheadGUI.preprocess_preview!"},{"category":"method","location":"reference/gui/#HammerheadGUI.preprocess_preview-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"preprocess_preview(source; size = (1200, 640)) -> Figure\n\nOpen the preprocessing-pipeline preview on source: a representative image (matrix or path) or a prebuilt PreprocessPreview controller. The raw image renders on the left, the processed result on the right, and the step list on the far right: a toggle per step, its parameters, and an \"▲\" button that moves the step earlier in the pipeline (the summary line shows the current order). \"background…\" picks frames to compute the background-subtraction reference from. Hand the pipeline to a batch with build_preprocess or set_preprocess!.\n\nWhen the controller carries a pair frame (set_pair!), clicking the processed image places a single-window correlation probe: the window outline is drawn at the click, and the panel below reports its displacement and peak ratio live as steps are toggled and edited.\n\n\n\n\n\n","title":"HammerheadGUI.preprocess_preview"},{"category":"method","location":"reference/gui/#HammerheadGUI.result_explorer!-Tuple{Any, ResultExplorer}","page":"GUI (HammerheadGUI)","text":"result_explorer!(target, ex::ResultExplorer) -> GridLayout\n\nBuild the result-explorer view into target (a GridPosition, e.g. fig[1, 2]), for embedding in a larger layout.\n\n\n\n\n\n","title":"HammerheadGUI.result_explorer!"},{"category":"method","location":"reference/gui/#HammerheadGUI.result_explorer-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"result_explorer(source; size = (1000, 700)) -> Figure\n\nOpen the result explorer on source: a PIVResult / StereoPIVResult, a vector of them, a results-file path (read with Hammerhead.load_results), or a prebuilt ResultExplorer controller (pass the controller when you want to drive the view programmatically — its observables stay live).\n\nFor a gridded (PIVResult / StereoPIVResult) result the view shows a scalar field (menu: displacement magnitude, components, diagnostics, uncertainty when present) as a heatmap in image orientation (y down) with the vector field as arrows (outliers red). A PTVResult is drawn as a colored particle scatter with optional displacement arrows, and a TrackingResult as trajectory polylines colored by mean speed (breaks at frame gaps). A frame slider scrubs a sequence, and a click-to-inspect panel summarizes the selected item in physical units when a scale is attached.\n\n\n\n\n\n","title":"HammerheadGUI.result_explorer"},{"category":"method","location":"reference/gui/#HammerheadGUI.scale_tool-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"scale_tool(source; batch = nothing, size = (900, 560)) -> Figure\n\nOpen the calibration-line scale tool on source: an image (matrix or path) or a prebuilt ScaleTool controller. Click the two endpoints of a feature of known physical size, enter the separation, units, and frame interval, and the derived pixel size / scale updates live. Pass a BatchRunner as batch to get an \"apply to batch\" button that copies the scale into its form (apply_scale!).\n\n\n\n\n\n","title":"HammerheadGUI.scale_tool"},{"category":"method","location":"reference/gui/#HammerheadGUI.selfcal_review-Tuple{SelfCalibrationReport}","page":"GUI (HammerheadGUI)","text":"selfcal_review(report::SelfCalibrationReport; size = (1150, 650)) -> Figure\n\nOpen the self-calibration report: per-pass disparity/triangulation statistics, the fitted sheet planes, convergence (with the judge-by-signed-median reminder for real data), and the size of the rigid correction. When the report carries disparity maps (self_calibrate(...; keep_disparity_maps = true)), they open in an embedded result explorer with the frame slider stepping through the passes.\n\n\n\n\n\n","title":"HammerheadGUI.selfcal_review"},{"category":"method","location":"reference/gui/#HammerheadGUI.stereo_batch_runner-Tuple{}","page":"GUI (HammerheadGUI)","text":"stereo_batch_runner(sbc = StereoBatchRunner(); size = (960, 600)) -> Figure\n\nOpen the stereo batch runner. Add each camera's frames, set the dewarpers (build them from calibrations with stereo_calibration, or set_dewarpers! from the REPL), pick an effort preset or edit the manual window schedule, optionally enter the frame interval (dt + units — stereo results are already in world units, so the attached scale converts displacements to velocities), choose an output file, and run. Cancellation uses run_piv_stereo_sequence's native between-acquisition predicate, so the completed prefix always lands in results. \"view results\" opens the completed acquisitions in the result explorer as soon as the first one is done, live-appending the rest.\n\n\n\n\n\n","title":"HammerheadGUI.stereo_batch_runner"},{"category":"method","location":"reference/gui/#HammerheadGUI.stereo_calibration-Tuple{CalibrationReview, CalibrationReview}","page":"GUI (HammerheadGUI)","text":"stereo_calibration(cr1, cr2; batch = nothing, size = (1500, 850)) -> Figure\n\nOpen the stereo rig setup: the two cameras' CalibrationReviews embedded side by side (plane sliders, model menus, reprojection errors), the dewarp-grid options (coverage and spacing), and a \"build dewarpers\" button running build_dewarpers over the two fitted cameras. Pass a StereoBatchRunner as batch to install the built pair directly into its form (set_dewarpers!); without one the figure still reports the built grid in its status line (build programmatically for a REPL hand-off).\n\n\n\n\n\n","title":"HammerheadGUI.stereo_calibration"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.BatchCancelled","page":"GUI (HammerheadGUI)","text":"BatchCancelled()\n\nThrown from the progress callback to abort a running batch; pairs finished before the abort stay in the incremental output file.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.BatchCancelled"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.BatchRunner","page":"GUI (HammerheadGUI)","text":"BatchRunner(; kwargs...)\n\nController for the parameter form + batch runner. Holds the frame list and pairing mode, a curated PIVParameters form (multi-pass window schedule + the accuracy-relevant options), the output path and analysis mask, and the run state — all as Observables.\n\nKeyword defaults\n\nfiles = Any[] (frame paths and/or in-memory matrices), pair_mode = :paired\neffort = :custom — :custom runs the manual window schedule below; :low / :medium / :high use run_piv_sequence's effort presets and ignore the schedule/option widgets.\nwindow_schedule = [64, 32, 32], overlap_fraction = 0.5\ncorrelation_method = :cross, padding = true, apodization = :gauss (the accuracy configuration), subpixel_method = :gauss3, uncertainty = false\npixel_size = 1.0, dt = 1.0, length_unit = \"px\", time_unit = \"frame\" — a PhysicalScale is attached to the outputs only when any of these differs from its default.\noutput_path = \"\" (empty = keep results in memory only), mask = nothing\n\nDrive it with add_files!, set_schedule!, set_effort!, set_preprocess! (an optional per-frame preprocessing pipeline, e.g. from a PreprocessPreview), start! and cancel!; watch progress ((done, total)), status, running, results (a Vector{PIVResult} after a completed run, nothing before), and completed (the finished pairs' results so far, appended and notified live while the batch runs — feed them to a ResultExplorer via push_result! to browse a batch in progress).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.BatchRunner"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.CalibrationReview","page":"GUI (HammerheadGUI)","text":"CalibrationReview(images, zs; model = :soloff, detect_kwargs...)\n\nController for the calibration diagnostics: detects the dot grid on every plate image (detect_calibration_grid; detect_kwargs — spacing, two_level, origin_offset, … — are forwarded), then fits a camera and exposes per-plane reprojection errors. images are matrices and/or image paths; zs are the plate positions (world units, one per image).\n\nObservables: plane (selected plane index), model (:soloff / :pinhole; changing it refits), camera (the fitted CameraCalibration, or nothing when the fit fails — e.g. :soloff with fewer than 3 planes), and fit_message (the failure reason, empty on success).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.CalibrationReview"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.MaskEditor","page":"GUI (HammerheadGUI)","text":"MaskEditor(image; polygons = [])\nMaskEditor(path::AbstractString)\n\nController for the mask editor: a reference image (displayed as the drawing background; the string form loads it with Hammerhead.load_image) plus the editing state as Observables — polygons (committed exclusion polygons, each a vector of (x, y) vertices in pixel coordinates), active (the in-progress polygon), selected (index of the selected polygon or nothing), and show_mask (overlay toggle).\n\nGestures (click!, alt_click!) implement the editing model: click to add vertices (a click on empty background starts a new polygon; a click inside an existing polygon selects it), alt/right-click to close the active polygon. Export the combined mask with polygon_mask(editor) or save_mask; seed polygons to resume editing an existing set.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.MaskEditor"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.PreprocStep","page":"GUI (HammerheadGUI)","text":"PreprocStep(name, enabled, params)\n\nOne pipeline entry: a core preprocessing step (name), whether it is applied (enabled), and its numeric parameters (params, a Dict{Symbol,Float64}). Built by PreprocessPreview; mutate through enable_step!, set_step_param!, and move_step! so the preview refreshes.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.PreprocStep"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.PreprocessPreview","page":"GUI (HammerheadGUI)","text":"PreprocessPreview(image; pair = nothing, enabled = Symbol[])\nPreprocessPreview(path::AbstractString; kwargs...)\n\nController for the preprocessing-pipeline preview: a representative image (matrix, or a path loaded with Hammerhead.load_image), the ordered step list steps (every supported step, disabled unless listed in enabled), an optional background for the subtraction step (see set_background!), and the live processed observable — the pipeline applied to image, recomputed on every change.\n\nSupported steps, in default order: :subtract_background, :intensity_cap (n_sigma), :highpass_filter (sigma), :clahe (clip_limit), :percentile_stretch (low/high), :invert_image, and :local_variance_normalize (sigma) — the core preprocessing set. build_preprocess exports the composed closure for the batch drivers.\n\nWith pair (the representative frame's correlation partner — matrix or path, see set_pair!) a single-window correlation probe becomes available: click! places it, probe_window sizes it (64 px by default, set_probe_window!), and probe_result / probe_summary report the window's displacement and peak ratio, recomputed live as the pipeline changes — a direct readout of what each preprocessing choice does to the correlation.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.PreprocessPreview"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.ResultExplorer","page":"GUI (HammerheadGUI)","text":"ResultExplorer(results; path = nothing)\nResultExplorer(result)\nResultExplorer(path::AbstractString)\n\nController for the result explorer: a loaded sequence of PIVResult, StereoPIVResult, PTVResult, or TrackingResult entries (mixed files are allowed) plus the view state as Observables — frame (1-based index into the sequence), field (the displayed scalar field, see available_fields), show_vectors / highlight_outliers (overlay toggles), selection (the inspected item: a CartesianIndex for a gridded window, a linear Int index for a scattered particle/trajectory, or nothing), the colorbar-limit state color_mode / color_min / color_max (see color_limits and set_color_limits!; manual overrides persist across frame/field switches until cleared), and count (the current sequence length, notified by push_result! so views can grow their frame slider while a batch appends live), and the interactive-analysis state tool / tool_points / profile_data / circulation_result (see set_tool! and click!; planar results only — tool state clears on frame switches).\n\nEach entry is routed through physical at construction, so loaded results carrying a PhysicalScale display in physical units with labelled axes (physical is idempotent and identity-safe, so unscaled results are untouched).\n\nThe string form loads the sequence with Hammerhead.load_results. Changing frames resets field to the new result's default when the current field is unavailable, and drops selection when it no longer refers to a valid item.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.ResultExplorer"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.ScaleTool","page":"GUI (HammerheadGUI)","text":"ScaleTool(image)\nScaleTool(path::AbstractString)\n\nController for the calibration-line scale tool: an image (matrix, or a path loaded with Hammerhead.load_image), the clicked line endpoints (points, at most two), and the physical inputs separation (the known distance between the two points), length_unit, dt, and time_unit — all as Observables.\n\nClick two points along a feature of known size (click!; a third click starts a new line), enter the separation and units, and read the derived pixel_size / physical_scale; hand the result to a batch with apply_scale!.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.ScaleTool"},{"category":"type","location":"reference/gui/#HammerheadGUI.Controllers.StereoBatchRunner","page":"GUI (HammerheadGUI)","text":"StereoBatchRunner(; kwargs...)\n\nController for the stereo batch runner. Holds the two synchronized camera frame lists (files1/files2) and pairing mode, the ImageDewarper pair (dewarpers, nothing until built or supplied — see set_dewarpers! and build_dewarpers), a curated parameter form (effort preset or manual window schedule), the stereo scale form (dt/time_unit/length_unit; stereo results are already in world units, so pixel_size is fixed at 1), the output path, and the run state — all as Observables.\n\nKeyword defaults\n\nfiles1 = Any[], files2 = Any[], pair_mode = :paired\ndewarpers = nothing (a (dw1, dw2) tuple sharing one DewarpGrid)\neffort = :custom, window_schedule = [64, 32, 32], overlap_fraction = 0.5, uncertainty = false (the manual schedule uses the accuracy configuration: padded, Gaussian apodization)\ndt = 1.0, time_unit = \"frame\", length_unit = \"world units\" — a dt-only PhysicalScale is attached when any differs from default\noutput_path = \"\" (empty = in memory only)\n\nDrive it like a BatchRunner: add_files! (with a camera keyword), set_schedule!, set_effort!, start!, cancel! (the stereo driver's native between-acquisition cancellation — the completed prefix is kept in results); watch progress, status, running, results, and the live completed accumulator.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.StereoBatchRunner"},{"category":"method","location":"reference/gui/#Hammerhead.polygon_mask-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"polygon_mask(me::MaskEditor) -> BitMatrix\n\nThe combined exclusion mask of all committed polygons, in the package mask convention (image-sized, true = excluded) — pass it as mask to run_piv. All-false when no polygons are committed.\n\n\n\n\n\n","title":"Hammerhead.polygon_mask"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.add_files!-Tuple{BatchRunner, Any}","page":"GUI (HammerheadGUI)","text":"add_files!(bc::BatchRunner, entries)\n\nAppend frames (file paths and/or matrices) to the frame list.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.add_files!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.add_files!-Tuple{StereoBatchRunner, Any}","page":"GUI (HammerheadGUI)","text":"add_files!(sbc::StereoBatchRunner, entries; camera::Integer = 1)\n\nAppend frames (file paths and/or matrices) to camera camera's frame list.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.add_files!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.add_vertex!-Tuple{MaskEditor, Real, Real}","page":"GUI (HammerheadGUI)","text":"add_vertex!(me::MaskEditor, x, y)\n\nAppend a vertex to the active (in-progress) polygon.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.add_vertex!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.alt_click!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"alt_click!(me::MaskEditor)\n\nSecondary-click gesture: close the active polygon when drawing, otherwise drop the selection.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.alt_click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.alt_click!-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"alt_click!(ex::ResultExplorer)\n\nClose the :circulation contour (right-click in the view): with at least three vertices the line-integral circulation(r, contour) and the vorticity-area form circulation(r; region = contour) are both evaluated into circulation_result; with fewer the gesture is cancelled. A no-op for the other tools.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.alt_click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.apply_pipeline-Tuple{PreprocessPreview, AbstractMatrix{<:Real}}","page":"GUI (HammerheadGUI)","text":"apply_pipeline(pp::PreprocessPreview, img) -> Matrix{Float64}\n\nApply the enabled steps, in order, to a fresh float copy of img (the input is never mutated — same contract as the build_preprocess closure).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.apply_pipeline"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.apply_scale!-Tuple{BatchRunner, ScaleTool}","page":"GUI (HammerheadGUI)","text":"apply_scale!(bc::BatchRunner, st::ScaleTool)\n\nCopy the tool's derived scale into a BatchRunner's scale form (see set_scale!), so the batch outputs carry it. Throws when the calibration line is not defined yet.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.apply_scale!"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.auto_lengthscale","page":"GUI (HammerheadGUI)","text":"auto_lengthscale(result, data = vector_data(result)) -> Float64\n\nArrow length scale that keeps the longest displayed vector at ~85% of the vector spacing. Gridded results use the grid step; scattered (PTVResult) data uses a robust sqrt(area / n) spacing proxy.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.auto_lengthscale"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.available_fields-Tuple{PIVResult}","page":"GUI (HammerheadGUI)","text":"available_fields(result) -> Vector{Symbol}\n\nScalar fields displayable for a result. Gridded results (PIVResult / StereoPIVResult) offer :magnitude (in-plane, or 3-component for stereo, displacement magnitude) plus the per-vector component/diagnostic fields, with uncertainty fields included only when estimates are present. Planar results additionally offer the derived fields :vorticity, :divergence, :strain_rate (the magnitude |S|), :swirling_strength, and :q_criterion (computed via flow_derivatives on grids with at least two points per axis). A PTVResult offers [:magnitude, :u, :v, :match_residual]; a TrackingResult offers [:speed] (per-trajectory mean speed).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.available_fields"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.begin_hole!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"Begin drawing a polygon that removes an area from the exclusion mask.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.begin_hole!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.build_dewarpers-Tuple{CalibrationReview, CalibrationReview}","page":"GUI (HammerheadGUI)","text":"build_dewarpers(cr1::CalibrationReview, cr2::CalibrationReview;\n                z = 0.0, spacing = :auto, coverage = :intersection,\n                margin = 0.0) -> (dw1, dw2)\n\nBuild the stereo ImageDewarper pair from two fitted calibration reviews: Hammerhead.common_dewarp_grid over both fitted cameras (each review's plate image size), then one dewarper per camera on the shared grid. Throws when either review has no fitted camera.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.build_dewarpers"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.build_parameters-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"build_parameters(bc::BatchRunner) -> Vector{PIVParameters}\n\nThe multi-pass schedule for the current form state (Hammerhead.multipass_parameters; propagates PIVParameters validation errors). Only relevant when effort == :custom.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.build_parameters"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.build_preprocess-Tuple{PreprocessPreview}","page":"GUI (HammerheadGUI)","text":"build_preprocess(pp::PreprocessPreview) -> Union{Nothing,Function}\n\nThe composed pipeline as the preprocess closure the batch drivers take (run_piv_sequence(...; preprocess = build_preprocess(pp))), or nothing when no step is enabled. The closure copies each frame before the in-place steps run, so in-memory matrix pairs are never mutated (frames loaded from paths pay one extra copy — negligible next to the analysis). The step list and background are snapshotted at build time: later form edits do not affect a batch already running.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.build_preprocess"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.build_scale-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"build_scale(bc::BatchRunner) -> Union{Nothing,PhysicalScale}\n\nThe PhysicalScale attached to the batch outputs, or nothing when every scale field is at its default (pixel_size = dt = 1, units px/frame) so the results stay in pixel/frame units.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.build_scale"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.build_scale-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"build_scale(sbc::StereoBatchRunner) -> Union{Nothing,PhysicalScale}\n\nThe dt-only PhysicalScale attached to the stereo outputs (pixel_size = 1 — the arrays are already in world units), or nothing when every scale field is at its default.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.build_scale"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.cancel!-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"cancel!(bc::BatchRunner)\n\nRequest cancellation of the running batch; it stops after the pair in flight, keeping finished pairs in the incremental output.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.cancel!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.cancel!-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"cancel!(sbc::StereoBatchRunner)\n\nRequest cancellation: the stereo driver's native predicate stops the run between acquisitions, and the completed prefix is returned in results (and stays in the incremental output).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.cancel!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_files!-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"clear_files!(bc::BatchRunner)\n\nEmpty the frame list.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_files!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_files!-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"clear_files!(sbc::StereoBatchRunner)\n\nEmpty both camera frame lists.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_files!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_points!-Tuple{ScaleTool}","page":"GUI (HammerheadGUI)","text":"clear_points!(st::ScaleTool)\n\nDrop the calibration-line endpoints.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_points!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_polygons!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"clear_polygons!(me::MaskEditor)\n\nDelete all polygons and cancel any active drawing.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_polygons!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_probe!-Tuple{PreprocessPreview}","page":"GUI (HammerheadGUI)","text":"clear_probe!(pp::PreprocessPreview)\n\nRemove the correlation probe.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_probe!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_selection!-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"clear_selection!(ex::ResultExplorer)\n\nDrop the current selection.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_selection!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.clear_tool!-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"clear_tool!(ex::ResultExplorer)\n\nClear the active tool's in-progress gesture and computed outputs (the tool itself stays selected).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.clear_tool!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.click!-Tuple{MaskEditor, Real, Real}","page":"GUI (HammerheadGUI)","text":"click!(me::MaskEditor, x, y)\n\nPrimary-click gesture: while drawing, add a vertex; otherwise select the polygon under the click, or (on empty background) deselect and start a new polygon at (x, y).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.click!-Tuple{PreprocessPreview, Real, Real}","page":"GUI (HammerheadGUI)","text":"click!(pp::PreprocessPreview, x::Real, y::Real)\n\nPlace the correlation probe at data-space (x, y) on the processed image (the window is centered there, clamped to stay inside the frame).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.click!-Tuple{ResultExplorer, Real, Real}","page":"GUI (HammerheadGUI)","text":"click!(ex::ResultExplorer, x::Real, y::Real)\n\nRoute a click through the active tool: :inspect selects the nearest item (select_nearest!); :profile places a line endpoint (the profile is computed when the second point lands, a third click starts a new line); :circulation appends a contour vertex.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.click!-Tuple{ScaleTool, Real, Real}","page":"GUI (HammerheadGUI)","text":"click!(st::ScaleTool, x::Real, y::Real)\n\nPlace a calibration-line endpoint at data-space (x, y): the first two clicks set the endpoints, a third click starts a new line at (x, y).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.click!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.close_active!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"close_active!(me::MaskEditor) -> Bool\n\nCommit the active polygon (returns true) when it has ≥ 3 vertices; otherwise discard it (cancel). No-op returning false when not drawing.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.close_active!"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.color_limits","page":"GUI (HammerheadGUI)","text":"color_limits(result, field::Symbol, mode::Symbol = :robust) -> (lo, hi)\n\nAutomatic colorbar limits for a displayed field. :full is the extrema of all finite values. :robust (the default) is the 2–98% percentile band over finite values at valid items (non-masked, non-outlier grid cells; non-flagged PTV particles) so a few outliers cannot stretch the color range; when no valid values exist it falls back to all finite values. A degenerate range is padded by ±0.5.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.color_limits"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.current_color_limits-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"current_color_limits(ex::ResultExplorer) -> (lo, hi)\n\nThe colorbar limits in effect for the current frame and field: the automatic color_limits under ex.color_mode, with any manual set_color_limits! overrides applied bound-wise (an inverted or degenerate manual pair is padded to a valid range).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.current_color_limits"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.current_result-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"current_result(ex::ResultExplorer)\n\nThe result at the current frame (one of PIVResult, StereoPIVResult, PTVResult, TrackingResult).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.current_result"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.delete_selected!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"delete_selected!(me::MaskEditor)\n\nDelete the selected polygon (no-op without a selection).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.delete_selected!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.describe_selection-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"describe_selection(ex::ResultExplorer) -> String\n\nMulti-line summary of the selected item (empty string when nothing is selected): position, displacement/velocity, diagnostics/uncertainty, and validation status, all with units.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.describe_selection"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.enable_step!","page":"GUI (HammerheadGUI)","text":"enable_step!(pp::PreprocessPreview, name::Symbol, on::Bool = true)\n\nEnable or disable a pipeline step. Enabling :subtract_background requires a background (see set_background!).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.enable_step!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.field_label-Tuple{Union{PIVResult, PTVResult, StereoPIVResult, TrackingResult}, Symbol}","page":"GUI (HammerheadGUI)","text":"field_label(result, field::Symbol) -> String\n\nDisplay name of a scalar field with the result's units appended (colorbar label). Displacement/velocity fields and their uncertainties carry the velocity unit (length_unit/time_unit, e.g. mm/s, or the px/world units fallback when unscaled); a PTVResult's match_residual carries the length unit; the derived gradient fields carry 1/time_unit (1/time_unit² for Q, 1/frame when unscaled); dimensionless diagnostics carry no unit.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.field_label"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.field_name-Tuple{Symbol}","page":"GUI (HammerheadGUI)","text":"field_name(field::Symbol) -> String\n\nShort display name of a scalar field (menu entries).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.field_name"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.field_values-Tuple{Union{PIVResult, StereoPIVResult}, Symbol}","page":"GUI (HammerheadGUI)","text":"field_values(result, field::Symbol)\n\nThe scalar field to display. For gridded results this is a Matrix (:magnitude is hypot of the displacement components, derived fields — see available_fields — come from flow_derivatives, everything else is the result's own matrix). For a PTVResult it is a per-particle Vector (:magnitude, :u, :v, :match_residual). For a TrackingResult it is a per-trajectory Vector of mean speeds (:speed).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.field_values"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.fit_summary-Tuple{CalibrationReview}","page":"GUI (HammerheadGUI)","text":"fit_summary(cr::CalibrationReview) -> String\n\nOverall fit summary (calibration_quality over all planes), or the fit failure message.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.fit_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.frame_pairs-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"frame_pairs(bc::BatchRunner)\n\nThe correlation pairs of the current frame list and pairing mode (Hammerhead.image_pairs; throws on an odd :paired frame count).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.frame_pairs"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.grow_mask!","page":"GUI (HammerheadGUI)","text":"Rasterize the editor state and grow the excluded region by radius pixels.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.grow_mask!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.move_step!-Tuple{PreprocessPreview, Symbol, Integer}","page":"GUI (HammerheadGUI)","text":"move_step!(pp::PreprocessPreview, name::Symbol, offset::Integer)\n\nMove a step by offset positions in the pipeline order (negative = earlier), clamped to the ends. Order matters — e.g. stretching before or after inversion gives different images.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.move_step!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.nframes-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"nframes(ex::ResultExplorer) -> Int\n\nNumber of results in the explored sequence.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.nframes"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.nplanes-Tuple{CalibrationReview}","page":"GUI (HammerheadGUI)","text":"nplanes(cr::CalibrationReview) -> Int\n\nNumber of calibration planes.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.nplanes"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.parse_schedule-Tuple{AbstractString}","page":"GUI (HammerheadGUI)","text":"parse_schedule(str) -> Vector{Int}\n\nParse a window-schedule entry: positive integers separated by commas and/or spaces, e.g. \"64, 32, 32\". Throws ArgumentError on anything else.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.parse_schedule"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.physical_scale-Tuple{ScaleTool}","page":"GUI (HammerheadGUI)","text":"physical_scale(st::ScaleTool) -> Union{Nothing,PhysicalScale}\n\nThe PhysicalScale defined by the calibration line and the entered dt/units, or nothing until the line is defined.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.physical_scale"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.pipeline_summary-Tuple{PreprocessPreview}","page":"GUI (HammerheadGUI)","text":"pipeline_summary(pp::PreprocessPreview) -> String\n\nOne-line summary of the enabled steps in order (\"no preprocessing\" when none are enabled).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.pipeline_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.pixel_distance-Tuple{ScaleTool}","page":"GUI (HammerheadGUI)","text":"pixel_distance(st::ScaleTool) -> Union{Nothing,Float64}\n\nLength of the calibration line in pixels, or nothing until two points are placed (or when they coincide).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.pixel_distance"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.pixel_size-Tuple{ScaleTool}","page":"GUI (HammerheadGUI)","text":"pixel_size(st::ScaleTool) -> Union{Nothing,Float64}\n\nThe derived pixel size, separation / pixel_distance (physical units per pixel), or nothing until the line is defined.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.pixel_size"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.plane_errors","page":"GUI (HammerheadGUI)","text":"plane_errors(cr::CalibrationReview, i = cr.plane[])\n\nDetected dot pixels and their reprojection errors on plane i, as (; pixels, errors) — or nothing when no camera is fitted.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.plane_errors"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.plane_summary","page":"GUI (HammerheadGUI)","text":"plane_summary(cr::CalibrationReview, i = cr.plane[]) -> String\n\nOne-line summary of plane i: z, dot count, markers, reprojection errors.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.plane_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.polygon_at-Tuple{MaskEditor, Real, Real}","page":"GUI (HammerheadGUI)","text":"polygon_at(me::MaskEditor, x, y) -> Union{Nothing,Int}\n\nIndex of the topmost (most recently drawn) committed polygon containing (x, y), or nothing.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.polygon_at"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.probe_summary-Tuple{PreprocessPreview}","page":"GUI (HammerheadGUI)","text":"probe_summary(pp::PreprocessPreview) -> String\n\nHuman-readable state of the correlation probe: the live displacement and peak ratio when it is placed, otherwise what is missing (pair frame, click, or a window that fits the frame).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.probe_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.push_result!-Tuple{ResultExplorer, Union{PIVResult, PTVResult, StereoPIVResult, TrackingResult}}","page":"GUI (HammerheadGUI)","text":"push_result!(ex::ResultExplorer, r)\n\nAppend a result to the explored sequence (routed through physical like the constructor) and notify ex.count, so open views extend their frame slider — this is how a live explorer follows a still-running batch. The current frame is left unchanged.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.push_result!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.refit!-Tuple{CalibrationReview}","page":"GUI (HammerheadGUI)","text":"refit!(cr::CalibrationReview)\n\nRefit the camera for the current model; on failure camera becomes nothing and fit_message carries the reason. Runs automatically when model changes.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.refit!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.save_mask-Tuple{MaskEditor, AbstractString}","page":"GUI (HammerheadGUI)","text":"save_mask(me::MaskEditor, path) -> path\n\nWrite the combined mask as a grayscale image (white = excluded), the convention Hammerhead.load_mask reads back by default.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.save_mask"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.scale_summary-Tuple{ScaleTool}","page":"GUI (HammerheadGUI)","text":"scale_summary(st::ScaleTool) -> String\n\nOne-line status: the line length and derived pixel size, or a prompt while points are missing.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.scale_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.select_nearest!-Tuple{ResultExplorer, Real, Real}","page":"GUI (HammerheadGUI)","text":"select_nearest!(ex::ResultExplorer, x::Real, y::Real)\n\nSelect the item nearest to the data-space point (x, y) (e.g. a mouse click) for inspection: the nearest grid node for a gridded result, the nearest particle for a PTVResult, or the nearest trajectory (by vertex) for a TrackingResult.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.select_nearest!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.selfcal_summary-Tuple{SelfCalibrationReport}","page":"GUI (HammerheadGUI)","text":"selfcal_summary(report::SelfCalibrationReport) -> String\n\nMulti-line summary of a self-calibration run: per-pass disparity and triangulation statistics, the fitted planes, convergence, and the size of the cumulative rigid correction.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.selfcal_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_background!-Tuple{PreprocessPreview, Nothing}","page":"GUI (HammerheadGUI)","text":"set_background!(pp::PreprocessPreview, frames; method = :min)\nset_background!(pp::PreprocessPreview, nothing)\n\nCompute the background for the :subtract_background step from an iterable of frames (matrices and/or paths; Hammerhead.compute_background with method = :min or :mean). Passing nothing clears it and disables the subtraction step.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_background!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_color_limits!-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"set_color_limits!(ex::ResultExplorer; min = missing, max = missing)\n\nManually override the colorbar limits, bound by bound. Each keyword accepts a number (or its string form) to pin that bound, and nothing / \"\" / \"auto\" to clear it back to the automatic color_limits; missing leaves it unchanged. Manual bounds persist across frame and field switches until cleared.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_color_limits!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_color_mode!-Tuple{ResultExplorer, Symbol}","page":"GUI (HammerheadGUI)","text":"set_color_mode!(ex::ResultExplorer, mode::Symbol)\n\nSet the automatic colorbar-limit mode: :robust (2–98% percentile band over valid values, the default) or :full (extrema). See color_limits.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_color_mode!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_dewarpers!-Tuple{StereoBatchRunner, CalibrationReview, CalibrationReview}","page":"GUI (HammerheadGUI)","text":"set_dewarpers!(sbc::StereoBatchRunner, cr1::CalibrationReview,\n               cr2::CalibrationReview; kwargs...)\n\nConvenience composition of build_dewarpers and the direct dewarper setter.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_dewarpers!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_dewarpers!-Tuple{StereoBatchRunner, ImageDewarper, ImageDewarper}","page":"GUI (HammerheadGUI)","text":"set_dewarpers!(sbc::StereoBatchRunner, dw1, dw2)\n\nAttach the two cameras' ImageDewarpers (they must share one DewarpGrid) — built in the GUI (build_dewarpers) or constructed at the REPL and passed in.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_dewarpers!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_dt!-Tuple{StereoBatchRunner, Real}","page":"GUI (HammerheadGUI)","text":"set_dt!(sbc::StereoBatchRunner, value)\n\nSet the frame interval from a positive number or its string form.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_dt!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_effort!-Tuple{BatchRunner, Symbol}","page":"GUI (HammerheadGUI)","text":"set_effort!(bc::BatchRunner, effort::Symbol)\n\nSet the analysis effort. :custom uses the manual window schedule and option widgets; :low / :medium / :high use run_piv_sequence's effort presets and ignore the manual schedule.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_effort!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_effort!-Tuple{StereoBatchRunner, Symbol}","page":"GUI (HammerheadGUI)","text":"set_effort!(sbc::StereoBatchRunner, effort::Symbol)\n\nSet the analysis effort (:custom manual schedule, or the :low/:medium/:high presets of run_piv_stereo_sequence).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_effort!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_field!-Tuple{ResultExplorer, Symbol}","page":"GUI (HammerheadGUI)","text":"set_field!(ex::ResultExplorer, field::Symbol)\n\nDisplay field (must be in available_fields(current_result(ex))).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_field!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_frame!-Tuple{ResultExplorer, Integer}","page":"GUI (HammerheadGUI)","text":"set_frame!(ex::ResultExplorer, i::Integer)\n\nMove to frame i, clamped to 1:nframes(ex).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_frame!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_image!-Tuple{PreprocessPreview, AbstractMatrix{<:Real}}","page":"GUI (HammerheadGUI)","text":"set_image!(pp::PreprocessPreview, image_or_path)\n\nReplace the representative preview image (matrix, or a path loaded with Hammerhead.load_image); the processed preview refreshes.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_image!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_pair!-Tuple{PreprocessPreview, Nothing}","page":"GUI (HammerheadGUI)","text":"set_pair!(pp::PreprocessPreview, image_or_path)\nset_pair!(pp::PreprocessPreview, nothing)\n\nSet the representative frame's correlation partner (matrix, or a path loaded with Hammerhead.load_image; must match the preview image's size) — this enables the correlation probe. nothing clears it.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_pair!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_pixel_size!-Tuple{BatchRunner, Real}","page":"GUI (HammerheadGUI)","text":"set_pixel_size!(bc::BatchRunner, value)\nset_dt!(bc::BatchRunner, value)\n\nSet the physical pixel_size / frame interval dt from a positive number or its string form (see set_scale!).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_pixel_size!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_plane!-Tuple{CalibrationReview, Integer}","page":"GUI (HammerheadGUI)","text":"set_plane!(cr::CalibrationReview, i::Integer)\n\nSelect plane i, clamped to 1:nplanes(cr).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_plane!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_preprocess!-Tuple{BatchRunner, Nothing}","page":"GUI (HammerheadGUI)","text":"set_preprocess!(bc::BatchRunner, pp)\n\nAttach a preprocessing pipeline applied to every frame of the batch: a PreprocessPreview controller (its build_preprocess closure is snapshotted now), a bare function img -> img′, or nothing to clear.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_preprocess!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_probe_window!-Tuple{PreprocessPreview, Integer}","page":"GUI (HammerheadGUI)","text":"set_probe_window!(pp::PreprocessPreview, size)\n\nSet the probe's interrogation window size (an even integer ≥ 8, or its string form).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_probe_window!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_scale!-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"set_scale!(bc::BatchRunner; pixel_size, dt, length_unit, time_unit)\n\nSet any of the physical-scale form fields at once; omitted fields are left unchanged. Positive numbers are required for pixel_size/dt.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_scale!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_schedule!-Tuple{BatchRunner, AbstractString}","page":"GUI (HammerheadGUI)","text":"set_schedule!(bc::BatchRunner, schedule)\n\nSet the multi-pass window schedule from a vector of sizes or a string (see parse_schedule).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_schedule!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_schedule!-Tuple{StereoBatchRunner, AbstractString}","page":"GUI (HammerheadGUI)","text":"set_schedule!(sbc::StereoBatchRunner, schedule)\n\nSet the manual multi-pass window schedule (vector of sizes or a string, see parse_schedule); used when effort == :custom.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_schedule!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_separation!-Tuple{ScaleTool, Real}","page":"GUI (HammerheadGUI)","text":"set_separation!(st::ScaleTool, value)\nset_dt!(st::ScaleTool, value)\n\nSet the physical separation between the two clicked points / the frame interval, from a positive number or its string form.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_separation!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_step_param!-Tuple{PreprocessPreview, Symbol, Symbol, Real}","page":"GUI (HammerheadGUI)","text":"set_step_param!(pp::PreprocessPreview, name::Symbol, param::Symbol, value)\n\nSet a step parameter from a number or its string form. Invalid values (unparsable, or rejected by the core preprocessing function when the preview recomputes) are reverted and rethrown, so the pipeline never sticks in a broken state.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_step_param!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.set_tool!-Tuple{ResultExplorer, Symbol}","page":"GUI (HammerheadGUI)","text":"set_tool!(ex::ResultExplorer, tool::Symbol)\n\nSelect the explorer's interaction tool: :inspect (click selects the nearest item — the default), :profile (two clicks define a line sampled with extract_profile), or :circulation (clicks accumulate a contour, alt_click! closes it and evaluates circulation). The analysis tools need a planar PIVResult at the current frame; switching tools (or frames) clears any in-progress gesture and outputs.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.set_tool!"},{"category":"function","location":"reference/gui/#HammerheadGUI.Controllers.shrink_mask!","page":"GUI (HammerheadGUI)","text":"Rasterize the editor state and shrink the excluded region by radius pixels.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.shrink_mask!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.start!-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"start!(bc::BatchRunner; async = true)\n\nValidate and start the batch. With async = true the run executes in a cooperative task (@async) so a GLMakie render loop keeps updating — run_piv's internal thread spawns provide the yield points; observables are still written from the primary thread. Progress lands in bc.progress/bc.status, results in bc.results, and the output file (when output_path is set) is written incrementally.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.start!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.start!-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"start!(sbc::StereoBatchRunner; async = true)\n\nValidate and start the stereo batch (same task semantics as the planar start!). Progress lands in progress/status, finished acquisitions stream into completed, results into results, and the output file is written incrementally.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.start!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.status_text-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"status_text(me::MaskEditor) -> String\n\nOne-line summary of the editing state (view status bar).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.status_text"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.stereo_pairs-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"stereo_pairs(sbc::StereoBatchRunner) -> (pairs1, pairs2)\n\nThe two cameras' synchronized correlation pair lists under the current pairing mode (Hammerhead.image_pairs per camera).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.stereo_pairs"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.tool_summary-Tuple{ResultExplorer}","page":"GUI (HammerheadGUI)","text":"tool_summary(ex::ResultExplorer) -> String\n\nOne-line status of the active analysis tool: gesture instructions while points are being placed, the profile legend once a line is set, or the computed circulation (line-integral and vorticity-area forms, with units).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.tool_summary"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.trajectory_gap_count-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"trajectory_gap_count(t) -> Int\n\nNumber of bridged frame gaps in a trajectory: steps where t.frames increases by more than one (nonconsecutive frames linked across a gap).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.trajectory_gap_count"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.trajectory_points-Tuple{Any}","page":"GUI (HammerheadGUI)","text":"trajectory_points(t) -> (xs, ys)\n\nFlat polyline coordinates for a trajectory with NaN breaks inserted at frame gaps, so a single lines! call renders one polyline per continuous run (the standard Makie NaN-separation approach).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.trajectory_points"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.undo_vertex!-Tuple{MaskEditor}","page":"GUI (HammerheadGUI)","text":"undo_vertex!(me::MaskEditor)\n\nRemove the last vertex of the active polygon (no-op when not drawing; removing the only vertex cancels the polygon).\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.undo_vertex!"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.validate-Tuple{BatchRunner}","page":"GUI (HammerheadGUI)","text":"validate(bc::BatchRunner) -> Union{Nothing,String}\n\nnothing when the batch can start, otherwise a human-readable reason. With a non-:custom effort the manual schedule is not consulted.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.validate"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.validate-Tuple{StereoBatchRunner}","page":"GUI (HammerheadGUI)","text":"validate(sbc::StereoBatchRunner) -> Union{Nothing,String}\n\nnothing when the stereo batch can start, otherwise a human-readable reason.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.validate"},{"category":"method","location":"reference/gui/#HammerheadGUI.Controllers.vector_data-Tuple{Union{PIVResult, StereoPIVResult}}","page":"GUI (HammerheadGUI)","text":"vector_data(result) -> NamedTuple\n\nDisplayable vectors of the result as flat, NaN-free arrays (; x, y, u, v, outlier) — the arrow-overlay input. Gridded results skip masked and out-of-view nodes; a PTVResult returns one entry per matched particle.\n\n\n\n\n\n","title":"HammerheadGUI.Controllers.vector_data"},{"category":"section","location":"explanation/gui/#The-graphical-user-interface-(GUI)-controller–view-split","page":"The GUI's controller–view split","text":"Every HammerheadGUI tool is two pieces with a strict boundary between them: a controller that owns all state and logic, and a view that renders it. Knowing the boundary is what turns the GUI from a set of windows into something you can script, test, and embed.","title":"The graphical user interface (GUI) controller–view split"},{"category":"section","location":"explanation/gui/#Controllers-own-the-state","page":"The GUI's controller–view split","text":"A controller — ResultExplorer, MaskEditor, BatchRunner, CalibrationReview — is a plain Julia object whose fields are Observables: the current frame, the displayed field, the polygons drawn so far, the batch progress. Everything the tool does is an ordinary function on the controller: set_field!, close_active!, start!. The controllers live in a HammerheadGUI.Controllers submodule that never imports Makie, so none of this needs a GL context — constructing a MaskEditor, feeding it click gestures, and exporting the mask works headless, on a server, or in a test suite.\n\nUser gestures are controller methods too. When you left-click in the mask editor, the view calls click!(me, x, y); the decision of whether that click adds a vertex, selects a polygon, or starts a new one lives in the controller, not in the widget code. (The gesture API is not re-exported at top level — using HammerheadGUI.Controllers: click!, alt_click! when you want it.)","title":"Controllers own the state"},{"category":"section","location":"explanation/gui/#Views-render-and-forward","page":"The GUI's controller–view split","text":"The view functions (result_explorer, mask_editor, …) build GLMakie figures whose widgets are wired to the controller's observables in both directions: moving the frame slider calls set_frame!, and calling set_frame! moves the slider. The view holds no state of its own — delete the window and the controller is intact; open two views on one controller and they stay in sync.\n\nThree practical consequences:\n\nAny open window can be driven from the REPL. Update an observable or call a controller function and the figure follows — the GUI tour is rendered exactly this way, and the same technique automates screenshots or demo recordings.\nGUI sessions are reproducible. A sequence of clicks is a sequence of controller calls, so an interactive session can be replayed as a script.\nViews compose. result_explorer! builds into a GridPosition of a larger figure; the self-calibration review embeds a full result explorer for its disparity maps the same way.","title":"Views render and forward"},{"category":"section","location":"explanation/gui/#The-boundary-to-the-core-package","page":"The GUI's controller–view split","text":"The controllers speak the core API and nothing else. The mask editor's export is polygon_mask — the same rasterization, the same true = excluded convention described in the masking model; its \"save\" writes the image load_mask reads. The batch runner is run_piv_sequence with its documented progress callback; its output file is an ordinary JLD2-format Julia data file written by save_results. The calibration review calls detect_calibration_grid and calibrate_camera verbatim. Nothing you produce in the GUI is in a GUI-only format, and any GUI workflow has a line-for-line scripted equivalent.\n\nThe dependency boundary mirrors the conceptual one: Hammerhead never depends on the GUI or on GLMakie — HammerheadGUI is a separate package that depends on Hammerhead, so headless installations (clusters, CI) never pay for it.","title":"The boundary to the core package"},{"category":"section","location":"explanation/stereo/#Stereo-geometry-and-self-calibration","page":"Stereo geometry and self-calibration","text":"Planar particle image velocimetry (PIV) measures two displacement components projected onto the image plane. With two cameras viewing the light sheet from different angles, the projections differ, and the difference encodes the out-of-plane component. This page explains Hammerhead's stereo chain: calibration → dewarping → reconstruction → self-calibration.","title":"Stereo geometry and self-calibration"},{"category":"section","location":"explanation/stereo/#Camera-calibration","page":"Stereo geometry and self-calibration","text":"A camera model maps world points (X, Y, Z) to pixel coordinates. Hammerhead provides two, both fit with calibrate_camera from (pixel, world) point pairs — typically produced by imaging a dot-grid calibration plate at several Z positions and running detect_calibration_grid:\n\nPinholeCamera — a projective model fitted by direct linear transformation (DLT). Physically grounded and exactly invertible, but it cannot represent lens distortion or refraction. Needs points on ≥ 2 Z planes.\nSoloffCamera — the 19-term polynomial of Soloff et al. (1997), cubic in X/Y and quadratic in Z. It absorbs distortion and refraction empirically, at the cost of a Newton-iterated inverse. Needs ≥ 3 Z planes. This is the default and the standard choice for real rigs.\n\nCheck any fit with calibration_quality; see the stereo-rig how-to for what residuals to expect from real plates.","title":"Camera calibration"},{"category":"section","location":"explanation/stereo/#Dewarping-to-a-common-plane","page":"Stereo geometry and self-calibration","text":"Rather than correlating raw images and reconciling geometry afterwards, Hammerhead resamples both cameras' images onto one regular grid of world coordinates in the measurement plane (a DewarpGrid shared by the rig, one ImageDewarper per camera). After dewarping, the same world point sits at the same pixel in both cameras' images, so the standard 2D engine can run per camera with identical parameters, and the two vector grids match point for point. Nodes outside a camera's view are recorded in a validity mask; the analysis is restricted to the stereo overlap (see the masking model).","title":"Dewarping to a common plane"},{"category":"section","location":"explanation/stereo/#Three-component-reconstruction","page":"Stereo geometry and self-calibration","text":"At each grid point, camera i's viewing ray drifts in-plane by (tXᵢ, tYᵢ) world units per unit Z (evaluated from the calibration by central differences). A world displacement (dx, dy, dz) therefore appears in camera i's dewarped two-component (2C) field as\n\nuᵢ = dx − dz·tXᵢ,    vᵢ = dy − dz·tYᵢ.\n\nTwo cameras give four equations for three unknowns; run_piv_stereo solves them by least squares per vector. The farther apart the viewing angles, the better conditioned dz is; with parallel rays (identical cameras) the system is degenerate and the vector comes out NaN. Per-camera uncertainties propagate through the same operator (see uncertainty quantification).\n\nThe result is a StereoPIVResult: world-coordinate grid, (u, v, w) in world units per frame interval, union outlier/mask flags, and both per-camera 2C results retained for diagnostics. To turn the displacements into velocities, attach a PhysicalScale with dt only and call physical — see the scaling how-to.","title":"Three-component reconstruction"},{"category":"section","location":"explanation/stereo/#Self-calibration","page":"Stereo geometry and self-calibration","text":"Calibration assumed the plate sat exactly in the light sheet. In practice it never does — millimeter offsets and small tilts are routine — and the error contaminates w directly. The fix is disparity self-calibration Wieneke (2005), implemented as self_calibrate:\n\nMeasure the disparity. Dewarp both cameras' images of the same instant and cross-correlate them (ensemble sum-of-correlation over several instants, one pass with large windows). If the sheet were exactly at the assumed plane, the two views would coincide; any systematic disparity field measures the misregistration.\nTriangulate. Each disparity vector, attributed symmetrically to the two viewing rays, is triangulated to a world point on the true sheet. Vectors with large triangulation residuals are rejected as false correlations.\nFit and correct. A plane is fitted through the triangulated points, and both camera models are rigidly transformed so the fitted plane becomes the measurement plane. A PinholeCamera absorbs the transform exactly into its projection matrix; other models are wrapped in a TransformedCamera.\nIterate. The measurement-correction loop repeats (the symmetric disparity attribution is second-order exact, and iteration absorbs the rest), then a final measurement verifies convergence.\n\nThe returned dewarpers are drop-in replacements for run_piv_stereo, and the SelfCalibrationReport records per-pass disparity statistics, fitted planes, and the cumulative world transform. Well-corrected setups converge to a residual disparity root-mean-square (RMS) below 0.1 px on real recordings (Wieneke, 2005); the synthetic test fixtures reach 0.01 px.\n\nOne caveat: self-calibration moves the world frame's Z origin onto the actual light sheet, anchored so that camera 1's view barely moves. If your downstream analysis depends on the original plate-defined frame, the cumulative transform in the report (R, t) maps corrected-frame coordinates back to it.","title":"Self-calibration"},{"category":"section","location":"howto/stereo_rig/#Calibrate-a-real-stereo-rig","page":"Calibrate a real stereo rig","text":"Goal: go from calibration-plate photographs to corrected camera models ready for run_piv_stereo. This guide describes the workflow on real data, using case E of the 4th International Particle Image Velocimetry (PIV) Challenge (Kähler et al., 2016) — a time-resolved stereo vortex ring recorded with a LaVision two-level dot-grid plate — as the running example. For executable end-to-end walkthroughs, see the stereo tutorial (synthetic, with ground truth) and stereo on a real recording (the 4E data itself); for the theory, see stereo geometry and self-calibration.","title":"Calibrate a real stereo rig"},{"category":"section","location":"howto/stereo_rig/#What-the-target-must-provide","page":"Calibrate a real stereo rig","text":"detect_calibration_grid handles rectilinear dot grids — LaVision-style plates, single- or two-level — and needs:\n\nKnown dot spacing in world units (the 4E plate: 15 mm per level).\nEnough Z information. A pinhole fit needs the plate imaged at ≥ 2 Z positions; the (default) Soloff model needs ≥ 3. A two-level plate contributes two planes per view, so a single photograph of a two-level plate suffices for a pinhole fit, and two positions for Soloff — but traversing the plate through more positions (4E used seven, −3 mm to +3 mm in 1 mm steps) improves conditioning and lets you check repeatability.\nA fiducial marker for multi-camera consistency. Both cameras must agree on which dot is the world origin. That requires the plate's filled square marker plus the origin_offset that locates the origin dot relative to it — for the 4E plate, (30.0, 7.5): the origin dot sits 30 mm right of and 7.5 mm above the square. Without a marker, each detection anchors to the dot nearest the image center, which is not consistent between cameras.\nRoughly upright cameras. World axes are anchored to the image orientation (+X nearest image-right, +Y nearest image-up). Standard stereo rigs qualify; heavily rolled cameras do not (currently unsupported).","title":"What the target must provide"},{"category":"section","location":"howto/stereo_rig/#Detect-and-calibrate","page":"Calibrate a real stereo rig","text":"Per camera, detect every plate position and fit one model:\n\nusing Hammerhead\n\nzs = -3.0:1.0:3.0                      # traverse positions, mm\ngrids = [detect_calibration_grid(load_image(path);\n             spacing = 15.0,\n             two_level = true, level_separation = 3.0,\n             origin_offset = (30.0, 7.5))\n         for path in cam1_plate_images]\ncam1 = calibrate_camera(grids, collect(zs))          # Soloff (default)\n\nUse invert = true for dark dots on a bright plate. Check each detection before fitting: length(grid.pixels) should cover the full grid, grid.square !== nothing confirms the marker was found, and on a two-level plate roughly half the dots should report level == 1.","title":"Detect and calibrate"},{"category":"section","location":"howto/stereo_rig/#What-residuals-to-expect","page":"Calibrate a real stereo rig","text":"Judge the fit with calibration_quality — but know what you're looking at. On the real 4E plates:\n\nPer-dot reprojection residuals of ~0.5–1 px root-mean-square (RMS) that are repeatable across plate positions reflect the plate's dot-position manufacturing tolerance, not detection error. Don't chase them with model changes; they are a property of the target.\nPlane-to-plane detection repeatability is 0.15–0.3 px — that is the level of the actual measurement noise.\n\nA pinhole fit that trails the Soloff fit badly indicates lens distortion or refraction along the optical path — expected through windows or liquid; stay with Soloff.","title":"What residuals to expect"},{"category":"section","location":"howto/stereo_rig/#Dewarp-and-self-calibrate-on-the-recordings","page":"Calibrate a real stereo rig","text":"Build the shared grid and per-camera dewarpers, then correct the plate-to-sheet misregistration with self_calibrate on the actual particle recordings — the plate never sits exactly in the light sheet:\n\ngrid = DewarpGrid(x = -40.0:0.1:40.0, y = -30.0:0.1:30.0)   # mm, finer than the vector spacing\ndw1 = ImageDewarper(cam1, grid, size_of_camera1_images)\ndw2 = ImageDewarper(cam2, grid, size_of_camera2_images)\n\n# 5–50 same-instant frame pairs give a well-shaped disparity correlation.\ndw1c, dw2c, report = self_calibrate(frames1, frames2, dw1, dw2)\n\nInspect the report before trusting it:\n\nDon't judge by report.converged alone: on real data the residual disparity RMS floors at the sheet-thickness decorrelation level (0.1 px on thin-sheet setups (Wieneke, 2005); ~0.5 px on the 4E recordings), which can sit above the default tol. Misalignment is systematic, so the signed median disparity components — computed from the maps with keep_disparity_maps = true — are the sharper test; the real-recording tutorial walks through this judgment.\nThe first pass's plane — its offset and tilt tell you how far the plate actually was from the sheet; millimeters are normal.\nA large triangulation_rms with a small final disparity suggests calibration errors rather than sheet misalignment — revisit the plate detections.\nPass keep_disparity_maps = true to inspect the raw disparity fields when convergence is poor (check the stereo overlap region, seeding density, and window size).\n\nThe corrected dewarpers then drop into stereo processing of the recording:\n\nstereo = run_piv_stereo(A1, B1, A2, B2, dw1c, dw2c, passes)\n\nFor large per-camera analyses, backend = :amdgpu or :cuda forwards GPU execution to both two-component (2C) PIV calls. Dewarping the four raw images and reconstructing the final three-component (3C) field remain CPU operations, so stereo does not yet form a fully device-resident pipeline. See Run PIV on a GPU for setup and the supported PIV option matrix.","title":"Dewarp and self-calibrate on the recordings"},{"category":"section","location":"explanation/precision/#Numeric-precision-policy","page":"Numeric precision policy","text":"Hammerhead follows one rule: precision follows the images. The element type of the analysis is\n\nT = float(promote_type(eltype(imgA), eltype(imgB)))\n\nand it flows through the correlators, image deformation, and every field of the returned PIVResult{T}. Feed Float64 matrices (the load_image default) and everything runs in double precision; feed Float32 matrices (load_image(Float32, path), or image_type = Float32 in the batch drivers) and the whole hot path — FFTs, window loads, resampling — runs in single precision, halving memory traffic on large recordings.","title":"Numeric precision policy"},{"category":"section","location":"explanation/precision/#Why-not-just-always-Float64?","page":"Numeric precision policy","text":"Particle image velocimetry (PIV) is bandwidth-bound: the dominant costs are fast Fourier transforms (FFTs) over interrogation windows and B-spline resampling over whole images. Single precision is plenty for image data quantized to 8–16 bits, and choosing precision at the input keeps the choice in the caller's hands with zero configuration surface — there is no precision parameter to document or misuse.\n\nOn graphics processing unit (GPU) backends, image deformation, FFTs, and correlation planes still follow the image type. Float32 therefore reduces the dominant device buffers. The uncertainty exception below remains Float64 on the GPU as well; see Run PIV on a GPU for the resulting memory and performance tradeoffs.","title":"Why not just always Float64?"},{"category":"section","location":"explanation/precision/#Deliberate-Float64-islands","page":"Numeric precision policy","text":"A few computations always run in double precision regardless of the image type, because they are far from the per-pixel hot path and benefit from the extra headroom. They convert back to T when stored:\n\nCamera calibration and stereo geometry — offline, once-per-experiment fits (calibrate_camera, detect_calibration_grid), the precomputed dewarp coordinate maps, per-vector stereo reconstruction, and self-calibration. These are O(points) or O(vector grid), not O(pixels).\nUncertainty statistics — the Wieneke (2015) sums accumulate in Float64 per window so that pooling across thousands of ensemble pairs cannot lose precision to cancellation.\nRobust statistics — replacement medians, temporal validation buffers, and field statistics accumulate in Float64.\nThe iterative :gauss2d subpixel fit (an LsqFit solve).\n\nThe practical consequence: a Float32 pipeline produces Float32 results whose accuracy is limited by the PIV method (≳ 0.03 px), not by the arithmetic — the islands make sure of it.","title":"Deliberate Float64 islands"},{"category":"section","location":"reference/backends/#KernelAbstractions-and-GPU-backends","page":"KernelAbstractions and GPU backends","text":"This page describes the internal execution-backend interface and the shared KernelAbstractions (KA) implementation. It is intended for contributors who are changing kernels, maintaining the CUDA/AMDGPU extensions, or adding a new device backend. The backend API is private and may change between releases. For installation and user-facing backend selection, see Run PIV on a GPU.","title":"KernelAbstractions and GPU backends"},{"category":"section","location":"reference/backends/#Architecture","page":"KernelAbstractions and GPU backends","text":"Backend selection is deliberately separate from PIVParameters and from result types. A public symbol is resolved once, at the driver boundary, to a private dispatch object:\n\nSelector Dispatch type Correlation implementation\n:cpu _CPUBackend FFTW correlators and CPU analysis\n:ka _KABackend Shared KA kernels on KernelAbstractions.CPU()\n:cuda _CUDABackend Shared KA kernels, CUDA arrays, and cuFFT\n:amdgpu _AMDGPUBackend Shared KA kernels, ROC arrays, and rocFFT\n\nThe core owns _AbstractHammerheadBackend, symbol resolution, pipeline orchestration, portable kernels, and the hardware-free :ka implementation. CUDA.jl and AMDGPU.jl remain weak dependencies. Loading either package activates its extension, whose _resolve_backend(::Val{...}) method registers the corresponding selector.\n\nThe three KA-family implementations share numerical kernels but not engine types. Each device extension owns the array allocation, transfer, FFT-plan, batching, and synchronization code appropriate to its device package. This division keeps device APIs out of the core while giving all three paths the same gather, correlation post-processing, peak analysis, deformation, and uncertainty mathematics.","title":"Architecture"},{"category":"section","location":"reference/backends/#Pipeline-dispatch-points","page":"KernelAbstractions and GPU backends","text":"The backend interface is a collection of private multiple-dispatch hooks, not an abstract type with a fixed list of methods. The principal hooks are:\n\nHook Responsibility\n_resolve_backend(::Val) Register a selector and construct its backend object\n_check_backend_params Reject unsupported options before expensive work begins\n_engine_nchunks Choose host-level fan-out; device backends use one logical chunk\npiv_correlation_engines Create or retrieve engines for a pass configuration\nprocess_windows! Correlate one pair and return packed per-window results\n_deform_context Stage interpolation coefficients and allocate resident warp buffers\napply_predictor Deform a pair and evaluate the predictor on the vector grid\n_plane_accumulator Allocate storage for ensemble sum-of-correlation planes\naccumulate_planes! Add one pair to the ensemble accumulator\nensemble_analyze! Analyze the completed accumulator and scatter the result grid\n\nCapability predicates such as _supports_fft, _supports_batched_fft, and _supports_fp64 default to false; a backend must opt in. The KA-family scope guard currently permits cross- or filtered phase correlation, :gauss3 or :gauss9 subpixel estimation, and optional Float64 uncertainty statistics. It rejects enlarged search_area_size, :gauss2d, and retained correlation planes. A new backend should fail unsupported configurations in _check_backend_params, rather than falling back to the CPU in the middle of a pass.\n\nPIVWorkspace owns a backend object and a private engine pool keyed by backend, precision, correlation method, window configuration, and peak count. An engine owns mutable scratch arrays and FFT plans. Consequently a workspace is backend-specific and must not be shared by concurrent run_piv calls.","title":"Pipeline dispatch points"},{"category":"section","location":"reference/backends/#Shared-KA-correlation-path","page":"KernelAbstractions and GPU backends","text":"For each device tile, process_windows! follows this sequence:\n\n_ka_window_means! computes masked window means.\n_ka_gather! copies mean-subtracted, apodized interrogation windows into the complex FFT batches.\nThe backend's batched forward FFT plans transform both batches.\n_ka_crosspower! forms the cross spectrum in place, or _ka_phasepower! forms an epsilon-guarded normalized spectrum with the same Gaussian frequency filter as the CPU PhaseCorrelator.\nThe backend's inverse FFT plan transforms the correlation batch.\n_ka_shiftgain! centers each plane and applies overlap-gain correction when padding is enabled.\n_ka_analyze! locates peaks and computes subpixel displacement, peak ratio, correlation moment, and alternative peaks.\nOnly the packed scalar output is copied to the host and scattered into the result arrays.\n\nCorrelation planes use the plane-major layout R[i, j, k]: adjacent threads within a workgroup traverse pixels of one plane. _ka_analyze! launches one workgroup per window and uses _KA_TPW threads cooperatively. Its workgroup size is part of the kernel contract: the launch size and Val{TPW} argument must agree.\n\nThe trailing window dimension of scratch and accumulator arrays is padded in the GPU extensions. Do not remove this apparently unused element: an exact power-of-two plane stride caused severe device-memory channel conflicts on validated hardware. The UQ scratch uses a similarly padded leading window dimension.","title":"Shared KA correlation path"},{"category":"section","location":"reference/backends/#Deformation-and-residency","page":"KernelAbstractions and GPU backends","text":"Cubic B-spline prefiltering stays on the CPU. After that one-time step, _deform_context copies the padded coefficient arrays to the selected KA device and allocates device-local warp outputs. Each iterative sweep transfers only the coarse predictor grid; _ka_deform! evaluates the predictor and the cubic B-spline interpolation on the device. The correlation engine recognizes those resident warp arrays and consumes them directly.\n\nA pass without deformation instead stages the source images in the engine. Masks are materialized as dense Array{Bool} before upload because a BitMatrix or arbitrary view cannot be transferred with the same direct copy path. Predictor-node interpolation and validation/replacement remain on the host because they operate on the much smaller vector grid.","title":"Deformation and residency"},{"category":"section","location":"reference/backends/#Ensemble-and-uncertainty-paths","page":"KernelAbstractions and GPU backends","text":"Ensemble correlation does not bring each pair's planes back to the host. _KAPlaneAccumulator holds one plane-major sum for the complete vector grid. For every pair, the engine runs through the inverse FFT and _ka_shiftgain_accum! adds the live tile into that sum. After the last pair, ensemble_analyze! analyzes accumulator views in device-sized tiles and returns packed scalars.\n\nCorrelation-statistics uncertainty has a distinct additive accumulator. _ka_uq_fill! materializes the smoothed correlation-difference field once, including its mean, and _ka_uq_stats! reduces it into Float64 statistics. Single-pair analysis copies those statistics back for finalization. Ensemble analysis keeps them device-resident across pairs and transfers them only when the accumulated field is finalized. A backend that cannot provide Float64 device accumulation must reject uncertainty in its scope check.","title":"Ensemble and uncertainty paths"},{"category":"section","location":"reference/backends/#CUDA-and-AMDGPU-extension-responsibilities","page":"KernelAbstractions and GPU backends","text":"The CUDA and AMDGPU extensions intentionally mirror one another. Each one:\n\nregisters its selector and capability methods;\ndefines a correlation engine containing device images, FFT batches, correlation and UQ scratch, packed-output buffers, origins, and FFT plans;\npools engines through pooled_engines using a backend-specific key;\nimplements device staging and a bounded sub-batch loop;\nlaunches the shared KA kernels with its native KA backend;\napplies in-place FFT plans with plan * array; and\nimplements resident deformation and ensemble hooks.\n\nDevice-specific differences should remain in allocation, array/view behavior, FFT integration, synchronization, and tuning constants. Numerical changes belong in src/ka_backend.jl so :ka, CUDA, and AMDGPU exercise the same implementation. When changing an extension, compare its corresponding method in the other extension; accidental drift between these nearly parallel files is a common source of backend-only bugs.\n\nGPU kernels must avoid operations that lower to host runtime calls. In particular, throwing conversions and round(Int, x) have caused allocation hostcalls on GPU compilers. Guard the range explicitly and use non-throwing conversion operations inside kernels. All cross-work-item state that survives a barrier must be in @localmem; ordinary locals do not provide that contract, including on the KA CPU backend.","title":"CUDA and AMDGPU extension responsibilities"},{"category":"section","location":"reference/backends/#Changing-or-adding-a-backend","page":"KernelAbstractions and GPU backends","text":"For a numerical-kernel change, make the change in src/ka_backend.jl and use backend = :ka as the first proving tier. Preserve the array layouts, precision policy, sign convention, and column-major tie ordering in peak selection. Then validate both single-pair and ensemble paths, with and without deformation and uncertainty.\n\nFor a new device backend, keep the device package in [weakdeps], register a package extension, and implement the dispatch points above. Start by adapting one existing GPU extension rather than copying the built-in :ka engine: the GPU engines contain the necessary staging, tiling, residency, and accumulator behavior that the CPU proving tier does not need to expose through a device API.\n\nThe hardware-free regression suite is test/test_ka.jl. Hardware validation and workload benchmarks are provided by bench/gpu_validate.jl and bench/gpu_benchmarks.jl; pass cuda or amdgpu as the script argument. Correctness comparisons should use scientific tolerances because device FFTs and reduction order need not be bitwise identical to FFTW.","title":"Changing or adding a backend"},{"category":"section","location":"reference/derived/#Derived-flow-analysis","page":"Derived flow analysis","text":"Spatial derivatives use only immediate valid neighbours and never cross a mask or outlier. The 2D swirling-strength and Q definitions describe the measured in-plane gradient tensor; they do not assume unmeasured 3D terms.\n\nPages = [\"derived.md\"]","title":"Derived flow analysis"},{"category":"method","location":"reference/derived/#Hammerhead.circulation-Tuple{PIVResult, AbstractVector{<:Tuple}}","page":"Derived flow analysis","text":"Compute circulation integral(u dx + v dy) around a supplied closed contour.\n\n\n\n\n\n","title":"Hammerhead.circulation"},{"category":"method","location":"reference/derived/#Hammerhead.circulation-Tuple{PIVResult}","page":"Derived flow analysis","text":"Area-form circulation over a rectangular or polygonal region, using the vorticity-area integral.\n\n\n\n\n\n","title":"Hammerhead.circulation"},{"category":"method","location":"reference/derived/#Hammerhead.extract_profile-Tuple{PIVResult, AbstractVector{<:Tuple}}","page":"Derived flow analysis","text":"Sample a result along a polyline, returning arc length, coordinates, and interpolated components.\n\n\n\n\n\n","title":"Hammerhead.extract_profile"},{"category":"method","location":"reference/derived/#Hammerhead.extract_region-Tuple{PIVResult, Any}","page":"Derived flow analysis","text":"Extract grid samples inside (xmin,xmax,ymin,ymax) or a polygon.\n\n\n\n\n\n","title":"Hammerhead.extract_region"},{"category":"method","location":"reference/derived/#Hammerhead.flow_derivatives-Tuple{AbstractVector, AbstractVector, AbstractMatrix, AbstractMatrix}","page":"Derived flow analysis","text":"flow_derivatives(result; include_invalid=false)\nflow_derivatives(x, y, u, v; valid=...)\n\nCompute the four entries of the planar velocity-gradient tensor on a regular grid. Central differences are used where both immediate neighbours are valid, one-sided differences at valid boundaries, and NaN where no valid local stencil exists. Masked and flagged vectors are excluded by default.\n\n\n\n\n\n","title":"Hammerhead.flow_derivatives"},{"category":"method","location":"reference/derived/#Hammerhead.q_criterion-Tuple{NamedTuple}","page":"Derived flow analysis","text":"q_criterion(...)\n\nThe explicitly two-dimensional Q criterion, Q = (||Omega||^2 - ||S||^2)/2 = -tr(grad(u)^2)/2. Positive values indicate rotation dominating strain in the measured plane; no unmeasured gradients are assumed.\n\n\n\n\n\n","title":"Hammerhead.q_criterion"},{"category":"method","location":"reference/derived/#Hammerhead.result_spectrum-Tuple{AbstractVector{<:PIVResult}, Int64, Int64}","page":"Derived flow analysis","text":"result_spectrum(results, i, j; component=:u, invalid=:error, dt=nothing, window=:hann)\n\nSpectrum of one grid point across a result sequence. dt defaults to attached scale metadata. Invalid samples can :error, be linearly :interpolated, or be replaced by the valid-sample :mean; invalid handling is always explicit.\n\n\n\n\n\n","title":"Hammerhead.result_spectrum"},{"category":"method","location":"reference/derived/#Hammerhead.strain_rate-Tuple{NamedTuple}","page":"Derived flow analysis","text":"Return planar strain components (xx, yy, xy, magnitude) with xy=(du/dy+dv/dx)/2.\n\n\n\n\n\n","title":"Hammerhead.strain_rate"},{"category":"method","location":"reference/derived/#Hammerhead.swirling_strength-Tuple{NamedTuple}","page":"Derived flow analysis","text":"swirling_strength(...)\n\nPlanar swirling strength: the magnitude of the imaginary part of the two eigenvalues of the local 2x2 velocity-gradient tensor. It is zero where the eigenvalues are real.\n\n\n\n\n\n","title":"Hammerhead.swirling_strength"},{"category":"section","location":"reference/internals/#Internals","page":"Internals","text":"Non-exported functions and types. These are implementation details and may change between releases.\n\nExecution backend plumbing is internal. Public driver/workspace keywords use selectors such as backend = :cpu; concrete backend implementation types are not exported and ordinary user code should rely on the default CPU behavior. The user-facing installation, feature, memory, and validation guide is Run PIV on a GPU. The core package provides backend = :cpu (FFTW) and the portable backend = :ka (the same KernelAbstractions kernels the GPU backends run, executed on the CPU — a hardware-free proving tier). GPU backends are provided by package extensions that activate when the device package is loaded: using AMDGPU enables backend = :amdgpu (ROCm) and using CUDA enables backend = :cuda (NVIDIA). An extension registers its selector by adding a _resolve_backend(::Val{:amdgpu})-style method, so unknown selectors report which package to load rather than silently failing. Device backends currently cover cross- and filtered phase correlation with :gauss3/:gauss9 subpixel refinement, for both run_piv and run_piv_ensemble (ensemble planes accumulate on the device; only the final vector grid returns to the host); :gauss2d and correlation-plane storage stay on :cpu and error with a clear message. Correlation-statistics uncertainty runs on all KA-family backends with Float64 device accumulation; single-pair runs return only the per-window statistics needed for finalization, while ensemble runs keep those additive statistics resident until the final vector grid is constructed. run_piv_stereo forwards the backend to its per-camera analyses (dewarping itself stays on the CPU).\n\nPages = [\"internals.md\"]","title":"Internals"},{"category":"section","location":"reference/internals/#Synthetic-Data","page":"Internals","text":"","title":"Synthetic Data"},{"category":"method","location":"reference/internals/#Base.size-Tuple{DewarpGrid}","page":"Internals","text":"size(grid::DewarpGrid) -> (ny, nx)\n\nSize of the dewarped image produced on grid: (length(grid.y), length(grid.x)).\n\n\n\n\n\n","title":"Base.size"},{"category":"method","location":"reference/internals/#Hammerhead.apply_world_transform-Tuple{CameraCalibration, AbstractMatrix{<:Real}, AbstractVector{<:Real}}","page":"Internals","text":"apply_world_transform(cam::CameraCalibration, R, t) -> CameraCalibration\n\nPre-compose cam with a rigid world transform: the returned camera maps a point w of the new world frame as cam maps R * w + t. A PinholeCamera absorbs the transform exactly into its projection matrix; other models are wrapped in a TransformedCamera (nested wrappers collapse into one).\n\n\n\n\n\n","title":"Hammerhead.apply_world_transform"},{"category":"method","location":"reference/internals/#Hammerhead.deform_images-Union{Tuple{T}, Tuple{Any, Any, Any, Any, Tuple{Int64, Int64}, Type{T}}} where T","page":"Internals","text":"deform_images(itpA, itpB, itp_u, itp_v, imgsize, ::Type{T}; threaded = false) -> (warpedA, warpedB)\n\nSymmetric (central-difference) image deformation: each imgsize output is resampled from its prebuilt cubic B-spline image interpolant (image_interpolant) — itpA shifted by −d/2 and itpB by +d/2, where d = (itp_u, itp_v) is the displacement field evaluated at each pixel. After deformation, content displaced by exactly d is aligned in both outputs, so correlating them measures the residual displacement. Out-of-image samples are zero-filled (a property of the passed interpolants).\n\nInterpolant evaluation is a pure read, so with threaded = true the output columns are filled concurrently.\n\nwarpA/warpB optionally supply the output buffers (each imgsize, element type T); when nothing they are freshly allocated. Multipass run_piv allocates them once and reuses them across every deforming pass, since each pass fully overwrites them before use.\n\n\n\n\n\n","title":"Hammerhead.deform_images"},{"category":"method","location":"reference/internals/#Hammerhead.effort_schedule-Tuple{Symbol}","page":"Internals","text":"effort_schedule(level::Symbol; ensemble = false, image_size = nothing, kwargs...) -> Vector{PIVParameters}\n\nBuild the schedule used by effort = :low, :medium, or :high. Keyword arguments whose names match PIVParameters fields override the preset; window_size rescales the pyramid so the final pass has that size, search_area_size likewise sets the final search size and rescales its pyramid, uncertainty, max_iterations, and keep_correlation_planes apply to the final pass only, and final = (;) is merged last.\n\n\n\n\n\n","title":"Hammerhead.effort_schedule"},{"category":"method","location":"reference/internals/#Hammerhead.image_interpolant-Union{Tuple{T}, Tuple{AbstractMatrix, Type{T}}} where T","page":"Internals","text":"image_interpolant(img, ::Type{T}) -> extrapolation\n\nBuild the cubic B-spline resampler (T-typed, zero-extrapolated) that deform_images samples. The prefilter is the expensive part, and it depends only on the source image, so multipass run_piv builds this once per image and reuses it across every deforming pass.\n\n\n\n\n\n","title":"Hammerhead.image_interpolant"},{"category":"method","location":"reference/internals/#Hammerhead.parse_validator-Tuple{PIVValidator}","page":"Internals","text":"parse_validator(spec) -> PIVValidator\n\nNormalize a validator specification: PIVValidator objects pass through, and Symbol => value pairs construct the corresponding validator (see the validator docstrings for the accepted pair forms).\n\n\n\n\n\n","title":"Hammerhead.parse_validator"},{"category":"method","location":"reference/internals/#Hammerhead.replace_vectors!-Tuple{AbstractMatrix, AbstractMatrix, AbstractMatrix{Bool}}","page":"Internals","text":"replace_vectors!(u, v, invalid::AbstractMatrix{Bool})\n\nReplace each vector flagged in invalid with the component-wise median of the valid vectors in a square neighborhood, growing the neighborhood until at least 3 valid neighbors are found. Replacements are computed from the original field, so the result does not depend on traversal order. Vectors with no valid neighbors anywhere are left unchanged.\n\n\n\n\n\n","title":"Hammerhead.replace_vectors!"},{"category":"method","location":"reference/internals/#Hammerhead.smooth_field-Tuple{AbstractMatrix{<:Real}}","page":"Internals","text":"smooth_field(f::AbstractMatrix) -> Matrix{float(eltype(f))}\n\n3×3 binomial (separable [1 2 1]/4) smoothing with replicated edges, preserving the field's floating-point precision. Used to condition the predictor field between interrogation passes.\n\n\n\n\n\n","title":"Hammerhead.smooth_field"},{"category":"method","location":"reference/internals/#Hammerhead.subpixel_gauss2d-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Tuple{Int64, Int64}}} where T<:AbstractFloat","page":"Internals","text":"subpixel_gauss2d(R, peakloc::Tuple{Int,Int}) -> (row, col)\n\nRefine an integer correlation peak by least-squares fitting a 2D Gaussian to the 3×3 neighborhood around it. Falls back to subpixel_gauss3 when the peak sits on the matrix edge or the fit fails to converge. The fit itself runs in Float64 (LsqFit on 9 points, CPU); the result is returned in the precision of R.\n\n\n\n\n\n","title":"Hammerhead.subpixel_gauss2d"},{"category":"method","location":"reference/internals/#Hammerhead.subpixel_gauss3-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Tuple{Int64, Int64}}} where T<:AbstractFloat","page":"Internals","text":"subpixel_gauss3(R, peakloc::Tuple{Int,Int}) -> (row, col)\n\nRefine an integer correlation peak to subpixel precision with independent 3-point Gaussian fits along rows and columns. Falls back to a zero offset along any axis where the fit is undefined (peak on the matrix edge, or non-positive neighbor values).\n\n\n\n\n\n","title":"Hammerhead.subpixel_gauss3"},{"category":"method","location":"reference/internals/#Hammerhead.subpixel_gauss9-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Tuple{Int64, Int64}}} where T<:AbstractFloat","page":"Internals","text":"subpixel_gauss9(R, peakloc::Tuple{Int,Int}) -> (row, col)\n\nRefine an integer correlation peak with a closed-form least-squares 2D Gaussian regression over the 3×3 neighborhood (log-paraboloid including the cross term, Nobach & Honkanen 2005). Unlike the two independent 1D fits of subpixel_gauss3, this is exact for an elliptical — including rotated — Gaussian peak and exhibits less peak locking, at the cost of nine logs and a hardcoded 2×2 solve. Falls back to subpixel_gauss3 at plane edges, on non-positive samples, or when the fitted surface has no interior maximum.\n\n\n\n\n\n","title":"Hammerhead.subpixel_gauss9"},{"category":"method","location":"reference/internals/#Hammerhead.substitute_alternatives!-Tuple{PIVResult, AbstractArray{<:Real, 3}, AbstractArray{<:Real, 3}, PIVParameters}","page":"Internals","text":"substitute_alternatives!(result, alt_u, alt_v, params) -> n_substituted\n\nPeak substitution: for each vector flagged in result.outliers (masked windows excluded), test its alternative peak displacements — alt_u/alt_v are (ny, nx, m) arrays ordered by peak strength, NaN where absent — against the valid neighbors using the UOD criterion (median ± threshold × (MAD + 0.1 px)). The first consistent alternative replaces the vector and clears its outlier flag: it is measured data, just not the tallest peak. Acceptance is judged against a snapshot of the field, so the result does not depend on traversal order.\n\n\n\n\n\n","title":"Hammerhead.substitute_alternatives!"},{"category":"type","location":"reference/internals/#Hammerhead.SelfCalPass","page":"Internals","text":"SelfCalPass\n\nDiagnostics of one measurement pass of self_calibrate (one entry per disparity-map computation, in report.passes):\n\ndisparity_rms, disparity_median: RMS and median magnitude of the disparity vectors (dewarped pixels) over valid, non-outlier grid points — measured before any correction of this pass. Wieneke (2005) reports well-corrected setups below 0.1 px.\nn_vectors: number of disparity vectors entering the statistics.\ntriangulation_rms: RMS of the per-vector 4-equation triangulation residual (dewarped pixels) over the accepted vectors; NaN when the pass applied no correction. Large values with a small plane-fit residual indicate calibration errors rather than sheet misalignment.\nplane: the fitted sheet plane z = a + b·X + c·Y (world units, in the frame current at that pass), or nothing when the pass only measured (converged, or the trailing verification pass).\n\n\n\n\n\n","title":"Hammerhead.SelfCalPass"},{"category":"method","location":"reference/internals/#Hammerhead.SyntheticData.laser_intensity-Tuple{Hammerhead.SyntheticData.GaussianLaserSheet, Real}","page":"Internals","text":"laser_intensity(laser::LaserSheet, z) -> Real\n\nLaser intensity multiplier at out-of-plane position z.\n\n\n\n\n\n","title":"Hammerhead.SyntheticData.laser_intensity"},{"category":"section","location":"reference/pipeline/#Core-pipeline-and-parameters","page":"Core pipeline and parameters","text":"The single-pair and multi-pass particle image velocimetry (PIV) engine: run_piv and its configuration (PIVParameters, multipass_parameters), the PIVResult container, analysis masks, correlators, and plotting. See the first tutorial for a guided walkthrough.\n\nAll public PIV drivers accept an execution-backend selector. See Run PIV on a GPU for optional package setup, the supported feature matrix, device-memory behavior, and validation commands.\n\nPages = [\"pipeline.md\"]","title":"Core pipeline and parameters"},{"category":"section","location":"reference/pipeline/#Running-an-analysis","page":"Core pipeline and parameters","text":"","title":"Running an analysis"},{"category":"section","location":"reference/pipeline/#Physical-units","page":"Core pipeline and parameters","text":"Results carry an optional PhysicalScale as metadata; the stored arrays stay in measured units (pixels, or world units for stereo) until physical converts them. See the scaling how-to and the conventions page.","title":"Physical units"},{"category":"section","location":"reference/pipeline/#Masks","page":"Core pipeline and parameters","text":"Analysis masks are image-sized Bool matrices, true marking excluded pixels — see the masking how-to and the masking model. Masks built from image files use load_mask.","title":"Masks"},{"category":"section","location":"reference/pipeline/#Correlators","page":"Core pipeline and parameters","text":"Lower-level access to the fast Fourier transform (FFT) correlation engine used by run_piv: correlator objects cache FFTW plans and buffers per window size.","title":"Correlators"},{"category":"section","location":"reference/pipeline/#Plotting","page":"Core pipeline and parameters","text":"Provided by a package extension — load a Makie backend (e.g. using GLMakie or using CairoMakie) to activate the methods.","title":"Plotting"},{"category":"type","location":"reference/pipeline/#Hammerhead.PIVParameters","page":"Core pipeline and parameters","text":"PIVParameters(; kwargs...)\n\nImmutable, validated configuration for a PIV analysis.\n\nKeyword arguments\n\nwindow_size = (32, 32): interrogation window size (rows, cols); an Int is expanded to a square window.\nsearch_area_size = window_size: centered search-area size (rows, cols). Each dimension must be at least the interrogation-window size and differ from it by an even number of pixels, so both areas share one pixel-grid center. A larger search area increases the measurable first-pass displacement without increasing the particle-sampling window.\noverlap = (16, 16): window overlap (rows, cols); must satisfy 0 ≤ overlap < window_size. An Int is expanded likewise.\ncorrelation_method = :cross: :cross (standard FFT cross-correlation) or :phase (phase correlation).\npadding = false: zero-pad the correlation FFT to twice the window size (true linear correlation; removes the wrap-around bias at ~4× FFT cost).\napodization = :none: :gauss applies a Gaussian window to each interrogation window before correlating.\nsubpixel_method = :gauss3: :gauss3 (two independent 3-point Gaussian fits), :gauss9 (closed-form 2D Gaussian regression on the 3×3 neighborhood — exact for rotated elliptical peaks, less peak locking, only marginally slower), :gauss2d (iterative least-squares 2D Gaussian fit), or :none.\nn_peaks = 3: number of correlation peaks located per window (primary + alternatives, ≥ 1). When > 1, vectors that fail validation are re-tested against their secondary/tertiary peak displacements and locally consistent alternatives are accepted before local-median replacement kicks in (\"peak substitution\"; accepted cells are unflagged since they hold measured data). The top two peaks are always located — the peak ratio needs both — so values up to 2 are free; each additional peak costs about one extra scan of the correlation plane, and alternatives are always refined with the cheap 3-point fit.\npeak_finder = :regionalmax: how integer correlation peaks are selected. The default first restricts candidates to 8-connected local maxima, so a real nearby secondary peak can still contribute to peak-ratio validation and peak substitution. :exclusion is the classic alternative: repeatedly take the largest remaining value outside a fixed exclusion box around stronger peaks.\nuncertainty = false: estimate a per-vector measurement uncertainty from correlation statistics (Wieneke 2015) into the uncertainty_u / uncertainty_v fields of the result. The estimator analyzes the residual asymmetry of the correlation peak between the two deformed windows and assumes the peak sits at ~zero residual displacement, so it runs on the final pass only and is meaningful only after multi-pass deformation has converged — iterate the final pass to convergence (max_iterations), or use the equivalent explicit schedule that repeats the final window size (e.g. multipass_parameters([32, 16, 16])). It estimates the random error only (systematic bias such as peak locking is invisible to it) and is accurate for uncertainties up to ~0.3 px.\nuod_enable = true: validate vectors with universal outlier detection.\nuod_threshold = 2.0: UOD sensitivity (higher is less sensitive).\nuod_neighborhood = 2: UOD neighborhood layers (1 → 3×3, 2 → 5×5, ...). The 5×5 default tolerates smooth velocity gradients at the field edges, where 3×3 neighborhoods falsely flag (and replacement then corrupts) the outermost rows of e.g. a sheared field.\nmin_peak_ratio = 1.0: vectors whose correlation peak ratio falls below this are flagged invalid; values ≤ 1 disable the check (the peak ratio is ≥ 1 by construction).\nvalidation = (): tuple of additional validators applied after the UOD and peak-ratio checks. Entries are validator objects or Symbol => value specs, e.g. (:peak_ratio => 1.3, :velocity_magnitude => (max = 50,)) — see validate_vectors!. Specs are parsed (and rejected if malformed) at construction.\nreplace_outliers = true: replace flagged vectors with the local median of valid neighbors. Intermediate passes of a multi-pass run always replace, regardless of this setting, to keep the predictor field well behaved.\nmax_iterations = 1: iteration budget for this pass. With the default 1 the pass correlates once (classic WIDIM). With more, the pass iterates: its own validated field becomes the deformation predictor, the images are re-deformed, and the windows re-correlated, until the field converges (see convergence_tol) or the budget is spent. Replacement artifacts then relax toward measured data within the pass instead of cascading into later (smaller-window) passes, and the final pass can be iterated to convergence — the predictor state the Wieneke (2015) uncertainty estimator assumes. Sweeps beyond the first always replace flagged vectors internally (the next predictor must be well behaved), but the returned field still honors replace_outliers.\nconvergence_tol = 0.05: convergence threshold (pixels) for the max_iterations loop — the pass stops early once 95% of the unmasked vectors change by less than this between successive sweeps (the 95th percentile rather than the maximum, because a few bistable low-signal windows flicker between correlation peaks indefinitely — they are validation's problem and would keep a max-norm from ever converging). 0 disables the early exit (the pass always runs max_iterations sweeps). Unused when max_iterations == 1.\nkeep_correlation_planes = false: retain each window's full correlation plane in the result's correlation_planes field for inspection. Opt-in and memory-heavy — a 32² window on a 100×100 grid in Float64 is ~800 MB — so intended for small regions or coarse grids. As run_piv returns the final pass, set it on the final pass only (pair it with the final keyword of multipass_parameters). See PIVResult.\n\nMulti-pass interrogation is configured with a vector of PIVParameters (one per pass) — see run_piv and multipass_parameters.\n\n\n\n\n\n","title":"Hammerhead.PIVParameters"},{"category":"type","location":"reference/pipeline/#Hammerhead.PIVResult","page":"Core pipeline and parameters","text":"PIVResult{T<:AbstractFloat}\n\nResult of run_piv. The numeric precision T follows the input images: float(promote_type(eltype(imgA), eltype(imgB))), e.g. Float32 images produce a PIVResult{Float32} and the whole pipeline runs in single precision.\n\nFields\n\nx, y: window-center coordinates of the interrogation grid (x along columns, y along rows, in pixels).\nu, v: displacement components on the (length(y), length(x)) grid; u is the column (x) displacement and v the row (y) displacement, in pixels. A particle at (row, col) in the first image is found at (row + v, col + u) in the second.\npeak_ratio: primary-to-secondary correlation peak ratio per window (higher is more reliable).\ncorrelation_moment: second moment of the correlation peak per window (an uncertainty proxy; lower is sharper).\nuncertainty_u, uncertainty_v: per-vector measurement uncertainty (one standard deviation, in pixels) of u and v, estimated from correlation statistics (Wieneke 2015) when the uncertainty parameter is enabled. NaN when disabled, for masked windows, and where the estimate is undefined (no usable correlation signal, or noise beyond the ~0.3 px validity of the method). The estimate describes the correlation measurement at the window; it is not updated when validation replaces or substitutes the vector.\noutliers: BitMatrix marking vectors that failed validation (UOD, peak-ratio check, and/or the validation pipeline). When outlier replacement is active, the u/v entries at these positions hold the local-median replacement rather than the measured displacement.\nmask: BitMatrix marking nodes dropped because their interrogation or search-area footprint overlaps the analysis mask beyond the threshold (see mask in run_piv). Masked windows hold NaN in u/v/peak_ratio/correlation_moment and are never counted as outliers. All-false when no mask was supplied.\nparameters: the PIVParameters of the (final) pass.\ncorrelation_planes: nothing unless the pass's keep_correlation_planes was set, in which case a Matrix{Union{Nothing,Matrix{T}}} indexed like the vector grid, holding a copy of each window's full correlation plane (nothing for masked/dropped windows). Opt-in and memory-heavy — see PIVParameters.\nscale: the PhysicalScale attached via the scale keyword of run_piv or with_scale; nothing when none was attached. Metadata only — the arrays above stay in pixels until physical converts them.\n\n\n\n\n\n","title":"Hammerhead.PIVResult"},{"category":"type","location":"reference/pipeline/#Hammerhead.PhysicalScale","page":"Core pipeline and parameters","text":"PhysicalScale(; pixel_size = 1.0, dt = 1.0, length_unit = \"px\", time_unit = \"frame\")\nPhysicalScale(pixel_size::Unitful.Length, dt::Unitful.Time)\n\nPhysical calibration attached to a result: pixel_size is the physical length of one pixel (in length_units per pixel; for stereo results, whose arrays are already in world units, it acts as an output-length-per-stored- world-unit factor — leave it at 1) and dt is the time between the two frames of a pair (in time_units per frame interval). The unit names are display-only labels (plot axes, show); the numbers carry the actual conversion, so results come out in whatever units go in — millimeters and seconds in, mm/s out.\n\nAttach a scale with the scale keyword of the drivers (run_piv, run_piv_sequence, run_piv_ensemble, run_piv_stereo, run_ptv, run_ptv_sequence, track_particles) or after the fact with with_scale. Attaching never changes the stored arrays — they stay in measured units (pixels, or world units for stereo) until physical converts them.\n\nThe Unitful-quantity constructor is provided by a package extension: load Unitful first (using Unitful), e.g. PhysicalScale(20.0u\"µm\", 0.5u\"ms\").\n\n\n\n\n\n","title":"Hammerhead.PhysicalScale"},{"category":"type","location":"reference/pipeline/#Hammerhead.PIVWorkspace","page":"Core pipeline and parameters","text":"PIVWorkspace\n\nReusable scratch for run_piv: the padded cubic-B-spline coefficient buffers of the two image interpolants, the two image-deformation output buffers, and a pool of window correlators (cached FFTW plans) — or, on the KA-family backends, of correlation engines with their batch buffers and FFT plans — keyed by window configuration. Build one with piv_workspace and pass it as the workspace keyword to reuse this scratch across many run_piv calls on equally sized image pairs — run_piv_sequence and run_piv_ensemble do this internally so a whole batch pays the allocations once. The buffers resize automatically if a later call presents a different image size or precision.\n\nA workspace is stateful scratch: it must not be shared across run_piv calls running concurrently. The drivers hold one workspace on their serial pair loop, so run_piv's own internal threading (which only reads the interpolant coefficients and writes disjoint buffer regions) stays race-free.\n\n\n\n\n\n","title":"Hammerhead.PIVWorkspace"},{"category":"function","location":"reference/pipeline/#Hammerhead.benchmark_piv_configurations","page":"Core pipeline and parameters","text":"benchmark_piv_configurations(imgA, imgB, params = nothing;\n    effort = nothing, backends = (:cpu,), samples = 3, warmup = true,\n    threaded = Threads.nthreads() > 1, kwargs...) -> Vector{NamedTuple}\n\nBenchmark CPU, device, and hybrid PIV execution on a representative in-memory image pair. backends is a collection of loaded backend selectors, for example (:cpu, :cuda) or (:cpu, :amdgpu). Every device backend is measured twice when uncertainty is enabled: fully on-device (configuration = :device) and with correlation/deformation on the device but uncertainty on the threaded CPU (configuration = :hybrid). The CPU reference is always included.\n\nPass an explicit PIVParameters or schedule as params, or omit it and use effort = :low/:medium/:high. Remaining keywords are handled exactly as by run_piv. Compilation is excluded when warmup = true; each reported time is the minimum of samples runs. A workspace is reused within each configuration, matching sequence-style production use.\n\nEach returned row contains configuration, backend, uncertainty_backend, seconds, speedup, max_vector_delta, p99_vector_delta, max_uncertainty_delta, and p99_uncertainty_delta. Deltas are measured against the CPU result over entries finite in both results; the p99_* 0.99-quantiles report bulk agreement, since the max_* field-wide extremes are dominated by the near-outlier windows the Wieneke estimator legitimately assigns huge σ. Benchmark the same image precision, size, schedule, mask, and preprocessing used in production; backend rankings are workload- and hardware-dependent.\n\n\n\n\n\n","title":"Hammerhead.benchmark_piv_configurations"},{"category":"method","location":"reference/pipeline/#Hammerhead.multipass_parameters-Tuple{AbstractVector}","page":"Core pipeline and parameters","text":"multipass_parameters(window_sizes; overlap_fraction = 0.5, final = (;), kwargs...) -> Vector{PIVParameters}\n\nBuild a multi-pass schedule with one PIVParameters per entry of window_sizes (integers or (rows, cols) tuples), each with overlap = floor(window_size * overlap_fraction). All remaining keyword arguments are forwarded to every pass.\n\nfinal is a NamedTuple of keyword overrides applied to the last entry only (its fields override the shared kwargs for that pass); it applies even to a length-1 schedule. Use it for settings you want only on the final pass — e.g. saving correlation planes for inspection, or turning off outlier replacement:\n\npasses = multipass_parameters([64, 32, 16]; padding = true,\n                              final = (n_peaks = 3, keep_correlation_planes = true))\n\nA schedule is just a Vector{PIVParameters}, so arbitrary per-pass control is always available by constructing the entries directly.\n\npasses = multipass_parameters([64, 32, 16]; padding = true, apodization = :gauss)\nresult = run_piv(imgA, imgB, passes)\n\n\n\n\n\n","title":"Hammerhead.multipass_parameters"},{"category":"method","location":"reference/pipeline/#Hammerhead.piv_workspace-Tuple{}","page":"Core pipeline and parameters","text":"piv_workspace(; backend = :cpu) -> PIVWorkspace\n\nConstruct an empty PIVWorkspace. Its buffers allocate lazily on the first run_piv call and are reused (and resized on demand) thereafter — pass one via the workspace keyword when running many equally sized pairs yourself, to amortize the per-pair interpolant/deformation/correlator allocations across the batch. backend = :cpu selects the execution backend; the core package provides the CPU backend, and package extensions add device selectors (see the internals reference).\n\n\n\n\n\n","title":"Hammerhead.piv_workspace"},{"category":"method","location":"reference/pipeline/#Hammerhead.run_piv-Tuple{AbstractMatrix{<:Real}, AbstractMatrix{<:Real}, AbstractVector{PIVParameters}}","page":"Core pipeline and parameters","text":"run_piv(imgA, imgB, passes::AbstractVector{PIVParameters};\n        threaded = Threads.nthreads() > 1,\n        predictor_smoothing = true,\n        backend = :cpu,\n        mask = nothing, mask_threshold = 0.5,\n        workspace = nothing, scale = nothing) -> PIVResult\nrun_piv(imgA, imgB, params::PIVParameters = PIVParameters(); kwargs...)\nrun_piv(imgA, imgB; effort = :low/:medium/:high, kwargs...)\n\nRun a (multi-pass) PIV analysis on an in-memory image pair. imgA and imgB must be equally sized real-valued matrices (load and convert image files with your preferred image package).\n\nEach pass tiles the images into interrogation windows (window_size, overlap). When search_area_size is larger, the centered frame-A interrogation window is correlated against that larger frame-B search area; grid stride remains window_size - overlap, while the outer grid centers move inward far enough to contain the search area. This raises the first-pass capture range without increasing the particle-sampling window. The pass then correlates each pair (correlation_method, optionally zero-padded and apodized), refines the peak to subpixel precision (subpixel_method), and validates the resulting field (universal outlier detection, peak-ratio threshold, and any additional validation pipeline — see the validation how-to for the full list of validators and their pair-spec syntax) with local-median replacement of invalid vectors.\n\nFrom the second pass on, the previous pass's validated field is used as a predictor: it is smoothed (predictor_smoothing), interpolated to pixel resolution, and both images are symmetrically deformed by ±half the predictor displacement (central-difference image deformation, cubic B-spline resampling). The pass then measures only the small residual displacement, so window sizes can shrink across passes — e.g. multipass_parameters([64, 32, 16]) — without violating the quarter-window displacement limit. Additional convergence sweeps are normally expressed with max_iterations on a pass; a schedule that repeats a window size is the equivalent explicit form.\n\nFor common tradeoffs, pass effort = :low, :medium, or :high instead of an explicit schedule. Typical single-threaded synthetic benchmarks on 256×256 pairs measure:\n\nEffort Schedule Options Typical result\n:low [32] PIVParameters() defaults 1× time; ≈0.10 px uniform-shift RMS, ≈0.43 px vortex RMS\n:medium [64, 32] defaults ≈4× time; ≈0.02 px uniform-shift RMS, ≈0.22 px vortex RMS\n:high [128, 64, 32] padding = true, apodization = :gauss, max_iterations = 2; final pass uncertainty = true ≈32× time; ≈0.02 px uniform-shift RMS, ≈0.09 px vortex RMS\n\nWith an effort level, PIVParameters keyword overrides are applied on top of the preset: most fields apply to every pass, window_size rescales the pyramid's final window size, search_area_size sets the final search size and rescales it with the window pyramid, and uncertainty, max_iterations, and keep_correlation_planes apply to the final pass only. final = (;) merges last and wins over both the preset and field keywords. The final window size is a seeding-density and physics decision, so smaller is not automatically better. Passing effort together with an explicit PIVParameters or pass vector is an error.\n\nA pass whose parameters set max_iterations > 1 additionally iterates in place: its own validated field is fed back as the deformation predictor and the windows are re-correlated until the bulk field stops changing (convergence_tol) or the budget runs out, so a bad vector caught by validation is re-measured within the stage instead of leaking its local-median replacement into the next pass's predictor. Iterating the final pass to convergence is also the predictor state the uncertainty estimator assumes — multipass_parameters([64, 32, 16]; final = (max_iterations = 3,)) is equivalent to repeating 16-px passes until converged, but stops as soon as the field settles.\n\nWith uncertainty = true in the final pass's parameters, a per-vector measurement uncertainty is estimated from correlation statistics (Wieneke\n\ninto uncertainty_u/uncertainty_v of the result — see\n\nPIVParameters for its convergence requirements.\n\nmask is an optional image-sized Bool matrix marking pixels to exclude (true = excluded), e.g. model geometry or reflection regions — build one with polygon_mask, load_mask, or any Bool array. The mask describes static lab-frame geometry and is not warped between passes. Windows whose masked-pixel fraction reaches mask_threshold produce no vector: they are flagged in result.mask, hold NaN, are never counted as outliers, and neither enter validation neighborhoods nor donate to replacement medians. For enlarged searches, the threshold is applied independently to the frame-A interrogation footprint and frame-B search footprint; reaching it in either drops the node. Footprints below the threshold are correlated over their valid pixels only.\n\nWith threaded = true (the default on multithreaded sessions) the window grid of each pass is split across tasks; results are identical to the serial path.\n\nbackend = :cpu selects the execution backend. The core package provides :cpu (FFTW) and :ka (the same portable KernelAbstractions kernels the GPU backends run, on the CPU); package extensions add the device selectors :amdgpu (ROCm, loaded with using AMDGPU) and :cuda (NVIDIA, loaded with using CUDA) — see Run PIV on a GPU for installation, supported features, memory sizing, and validation. Backend implementation types are internal.\n\nuncertainty_backend = :same evaluates correlation-statistics uncertainty on the selected backend. Set it to :cpu to keep correlation and deformation on that backend while transferring the final deformed pair once and evaluating uncertainty on the threaded CPU. This hybrid policy is backend-independent and is often preferable when the selected device has weak Float64 throughput. Use benchmark_piv_configurations on the production workload rather than selecting the policy from the device vendor or product name.\n\nworkspace optionally supplies a PIVWorkspace (from piv_workspace) whose interpolant, deformation, and correlator scratch is reused across calls — pass the same one to every run_piv in a hand-written loop over equally sized pairs to amortize those allocations. Results are bitwise identical to workspace = nothing. run_piv_sequence and run_piv_ensemble manage a workspace for you.\n\nscale optionally attaches a PhysicalScale (pixel size, frame interval, unit labels) to the result as metadata — the measured fields stay in pixels, identical to an unscaled run, until physical converts them.\n\nThe numeric precision of the analysis follows the images: T = float(promote_type(eltype(imgA), eltype(imgB))) is used for the correlators, deformation, and every field of the returned PIVResult{T}. Feed Float32 matrices (e.g. load_image(Float32, path)) to run the whole pipeline in single precision.\n\nReturns the PIVResult of the final pass.\n\n\n\n\n\n","title":"Hammerhead.run_piv"},{"category":"method","location":"reference/pipeline/#Hammerhead.physical-Tuple{Union{PIVResult, PTVResult, StereoPIVResult, TrackingResult}, PhysicalScale}","page":"Core pipeline and parameters","text":"physical(result) -> same-type result in physical units\nphysical(result, scale::PhysicalScale)\n\nConvert result to physical units using its attached PhysicalScale (the two-argument form attaches scale first, replacing any existing one). Positions (x, y, z, trajectory points) are multiplied by pixel_size and displacements together with their uncertainties (u, v, w, uncertainty_*, and a PTVResult's match_residual by pixel_size alone) by pixel_size / dt, turning displacements per frame interval into velocities. Returns result itself when there is nothing to convert (scale === nothing or an identity scale).\n\nThe returned result carries an identity scale with the same unit labels, so physical is idempotent and plot labels always match the arrays. Pixel-native diagnostics are never converted: peak_ratio, correlation_moment, correlation_planes, the embedded particles_a/particles_b, and a StereoPIVResult's per-camera cam1/cam2 results are shared untouched. Convert last — validators, peak_locking, and the other pixel-calibrated tools must run on the raw result.\n\nOne deviation from the identity rule: a TrackingResult stores only positions (velocities are derived by differencing), so its converted scale keeps dt — pass it to trajectory_velocities to get physical velocities from either the raw or the converted result.\n\n\n\n\n\n","title":"Hammerhead.physical"},{"category":"method","location":"reference/pipeline/#Hammerhead.with_scale-Union{Tuple{T}, Tuple{PIVResult{T}, Union{Nothing, PhysicalScale}}} where T","page":"Core pipeline and parameters","text":"with_scale(result, scale::Union{Nothing,PhysicalScale})\n\nReturn a result identical to result (all arrays shared, not copied) with its scale field replaced. Works on PIVResult, StereoPIVResult, PTVResult, and TrackingResult; nothing strips an attached scale (e.g. to overlay vectors on the source image in pixel coordinates). Attaching is pure metadata — the stored arrays keep their measured units until physical converts them.\n\n\n\n\n\n","title":"Hammerhead.with_scale"},{"category":"method","location":"reference/pipeline/#Hammerhead.automatic_mask-Tuple{AbstractMatrix{<:Real}}","page":"Core pipeline and parameters","text":"automatic_mask(image; method=:intensity, threshold=:auto, side=:high,\n               radius=5, grow=0) -> BitMatrix\nautomatic_mask(imageA, imageB; kwargs...) -> BitMatrix\n\nConstruct an image-derived exclusion mask. method is :intensity, :contrast (local standard deviation), or :edge (gradient magnitude). threshold=:auto selects a robust tail quantile; a numeric threshold uses the image's intensity units. The pair method returns the union of both frame masks, the normal convention for moving geometry.\n\n\n\n\n\n","title":"Hammerhead.automatic_mask"},{"category":"function","location":"reference/pipeline/#Hammerhead.grow_mask","page":"Core pipeline and parameters","text":"grow_mask(mask, radius=1) -> BitMatrix\nshrink_mask(mask, radius=1) -> BitMatrix\n\nGrow or shrink an exclusion mask by a circular pixel radius. These helpers preserve Hammerhead's true = excluded convention. A zero radius copies the input.\n\n\n\n\n\n","title":"Hammerhead.grow_mask"},{"category":"method","location":"reference/pipeline/#Hammerhead.polygon_mask-Tuple{Tuple{Int64, Int64}, AbstractVector}","page":"Core pipeline and parameters","text":"polygon_mask(size, vertices) -> BitMatrix\n\nRasterize a polygon into a (rows, cols) mask, true inside the polygon. vertices is a vector of (x, y) corners in pixel coordinates (x along columns, y along rows), in order, without repeating the first vertex. Combine multiple exclusion regions with .| and pass the result as mask to run_piv.\n\nmask = polygon_mask(size(img), [(120, 40), (480, 90), (450, 300), (100, 260)])\nresult = run_piv(imgA, imgB, passes; mask)\n\n\n\n\n\n","title":"Hammerhead.polygon_mask"},{"category":"type","location":"reference/pipeline/#Hammerhead.Correlator","page":"Core pipeline and parameters","text":"Correlator\n\nAbstract supertype for window correlators. Concrete subtypes: CrossCorrelator, PhaseCorrelator.\n\n\n\n\n\n","title":"Hammerhead.Correlator"},{"category":"type","location":"reference/pipeline/#Hammerhead.CrossCorrelator","page":"Core pipeline and parameters","text":"CrossCorrelator{T}(window_size::Dims{2}; search_area_size=window_size,\n                   padding=false, apodization=:none)\nCrossCorrelator(window_size; kwargs...)  # T = Float32\n\nFFT-based cross-correlation of a frame-A interrogation window against a centered frame-B search area, with preallocated buffers and cached in-place FFTW plans. The search area defaults to the interrogation size.\n\npadding = true zero-pads the FFT to twice the window size, replacing circular with true linear correlation; this removes the wrap-around noise that biases subpixel estimates toward zero, at roughly 4× the FFT cost.\napodization = :gauss applies a Gaussian window (σ = window/4) to the mean-subtracted inputs to reduce edge effects and spectral leakage.\n\n\n\n\n\n","title":"Hammerhead.CrossCorrelator"},{"category":"type","location":"reference/pipeline/#Hammerhead.PhaseCorrelator","page":"Core pipeline and parameters","text":"PhaseCorrelator{T}(window_size::Dims{2}; search_area_size=window_size,\n                   padding=false, apodization=:none,\n                   filter_sigma=min(fft_size...) / 8)\nPhaseCorrelator(window_size; kwargs...)  # T = Float32\n\nFiltered phase correlation (normalized cross-power spectrum) of two interrogation windows, with preallocated buffers and cached in-place FFTW plans. More robust to illumination differences than plain cross-correlation. padding and apodization behave as for CrossCorrelator.\n\nWhitening the spectrum gives noise-only high-frequency bins the same weight as signal-bearing ones, which destroys the peak for low-frequency content like particle images; the whitened spectrum is therefore weighted by a Gaussian low-pass with standard deviation filter_sigma (in FFT frequency bins of the possibly padded transform).\n\n\n\n\n\n","title":"Hammerhead.PhaseCorrelator"},{"category":"method","location":"reference/pipeline/#Hammerhead.correlate-Tuple{Correlator, AbstractMatrix, AbstractMatrix}","page":"Core pipeline and parameters","text":"correlate(c::Correlator, subA, subB; subpixel=:gauss3, mask=nothing)\n\nCorrelate a frame-A interrogation window against a frame-B search area and locate the displacement peak. For equal sizes, mask may be one window-sized Bool matrix. For an enlarged search area, pass (maskA, maskB) matching the two inputs; true pixels are excluded from the correlation (they enter at the valid-pixel mean, contributing zero after mean subtraction).\n\nReturns a named tuple (du, dv, peak, peakloc, refined_peakloc, correlation):\n\ndu, dv: subpixel displacement of subB relative to subA along columns (x) and rows (y) respectively;\npeak: correlation value at the integer peak;\npeakloc: integer (row, col) peak location in the correlation plane;\nrefined_peakloc: subpixel (row, col) peak location;\ncorrelation: the real correlation plane (zero lag at size .÷ 2 .+ 1).\n\nsubpixel is one of :gauss3, :gauss2d, or :none.\n\nwarning: Warning\ncorrelation aliases an internal buffer of c and is overwritten by the next correlate call. Copy it if you need to keep it.\n\n\n\n\n\n","title":"Hammerhead.correlate"},{"category":"method","location":"reference/pipeline/#Hammerhead.correlate_deformable-Tuple{Correlator, AbstractMatrix, AbstractMatrix}","page":"Core pipeline and parameters","text":"correlate_deformable(c::Correlator, subA, subB;\n                     iterations=3, subpixel=:gauss3, tol=1e-3)\n\nIteratively correlate subA against a back-warped subB: after each pass the accumulated displacement is used to translate subB into alignment with subA, and the residual displacement is re-estimated. Stops after iterations passes or when the residual falls below tol pixels.\n\nReturns the same named tuple as correlate, with du/dv holding the accumulated displacement and the remaining fields describing the final pass.\n\n\n\n\n\n","title":"Hammerhead.correlate_deformable"},{"category":"method","location":"reference/pipeline/#Hammerhead.find_peaks-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Int64}} where T<:AbstractFloat","page":"Core pipeline and parameters","text":"find_peaks(R, k; exclusion_radius = 2, peak_finder = :regionalmax) -> Vector of (value, location)\n\nLocate up to k correlation peaks in descending order. With the default peak_finder = :regionalmax, candidates are 8-connected local maxima, so nearby but distinct secondary peaks are not hidden by a fixed exclusion box. With peak_finder = :exclusion, this returns the global maximum, then repeatedly the largest value outside square exclusion zones of ±exclusion_radius pixels around every peak already found. Peaks after the first must be positive.\n\n\n\n\n\n","title":"Hammerhead.find_peaks"},{"category":"function","location":"reference/pipeline/#Hammerhead.plot_vector_field","page":"Core pipeline and parameters","text":"plot_vector_field(result::PIVResult; stride=1, lengthscale=:auto, show_replaced=true, replaced_color=:orangered, figure=(;), axis=(;), kwargs...)\nplot_vector_field(result::PTVResult; highlight_outliers=true, figure=(;), axis=(;), kwargs...)\nplot_vector_field(x, y, u, v; stride=1, lengthscale=:auto, figure=(;), axis=(;), kwargs...)\n\nPlot a PIV or PTV vector field as arrows and return the Makie.Figure. The y-axis is reversed to match image (row-down) coordinates. For a PIVResult the arrows sit on the interrogation grid; for a PTVResult they are scattered at the frame-A particle positions.\n\nFor grid fields, stride plots every stride-th vector in each direction (inspect a high-resolution field without a solid mat of arrows), and lengthscale = :auto scales the arrows so the 0.99-quantile of the valid vectors' magnitudes spans one plotted grid cell (stride × the grid spacing); pass a Real for a manual multiplier. Flagged/replaced vectors (result.outliers) are drawn in replaced_color when show_replaced is true and omitted otherwise; masked (NaN) vectors are always skipped; remaining kwargs are passed to arrows2d!.\n\nA result with an attached PhysicalScale is plotted in physical units (positions and velocities converted via physical, axis labels showing the length unit); to overlay arrows on the source image in pixel coordinates instead, plot with_scale(result, nothing). The bare x, y, u, v method always plots the arrays as given with pixel labels.\n\nProvided by a package extension: load a Makie backend first (e.g. using GLMakie or using CairoMakie).\n\n\n\n\n\n","title":"Hammerhead.plot_vector_field"},{"category":"function","location":"reference/pipeline/#Hammerhead.plot_vector_field!","page":"Core pipeline and parameters","text":"plot_vector_field!(ax, result::PIVResult; stride=1, lengthscale=:auto, show_replaced=true, replaced_color=:orangered, kwargs...)\nplot_vector_field!(ax, result::PTVResult; highlight_outliers=true, kwargs...)\nplot_vector_field!(ax, x, y, u, v; stride=1, lengthscale=:auto, kwargs...)\n\nLike plot_vector_field, but draws into an existing Makie.Axis. Scaled results are converted the same way, but the labels of an existing axis are left alone.\n\n\n\n\n\n","title":"Hammerhead.plot_vector_field!"},{"category":"section","location":"reference/ptv/#Particle-tracking-velocimetry-(PTV)","page":"PTV (particle tracking)","text":"Per-frame particle detection, hybrid particle image velocimetry (PIV)-guided two-frame matching, scattered validation, grid binning, and multi-frame trajectory linking. See the PTV tutorial for a worked example and the conventions explanation for when PTV beats PIV and how frame-A attribution works.\n\nPages = [\"ptv.md\"]","title":"Particle tracking velocimetry (PTV)"},{"category":"type","location":"reference/ptv/#Hammerhead.Particles","page":"PTV (particle tracking)","text":"Particles{T<:AbstractFloat}\n\nDetected particles from one frame (a struct-of-arrays), as produced by detect_particles. x is the subpixel column (x) position, y the subpixel row (y) position, intensity the background-subtracted peak amplitude, and diameter the 4σ estimate from the Gaussian fit (NaN when both axes fell back to the centroid, e.g. a saturated particle).\n\n\n\n\n\n","title":"Hammerhead.Particles"},{"category":"function","location":"reference/ptv/#Hammerhead.detect_particles","page":"PTV (particle tracking)","text":"detect_particles(img, params = PTVParameters(); mask = nothing) -> Particles\n\nDetect particles in a single image as subpixel local maxima with Gaussian peak fits. The numeric precision follows the image: T = float(eltype(img)).\n\nThe algorithm is:\n\nThreshold from a robust intensity floor. bg is the median over valid (unmasked) pixels and mad = 1.4826 · median(|img − bg|); with params.threshold === :auto the threshold is bg + params.threshold_k·mad, otherwise params.threshold is used as an absolute intensity.\nLocal maxima among interior pixels above threshold (deterministic single pick on flat plateaus; masked pixels are skipped).\nSubpixel refinement by an independent 3-point log-Gaussian fit per axis on background-subtracted intensities, falling back to an intensity-weighted 3×3 centroid on any axis where the fit is undefined.\nDiameter filtering to [params.min_diameter, params.max_diameter] (particles with a NaN diameter from the centroid fallback are kept).\nDeduplication: brighter particles win when two lie within params.min_separation px.\n\nSee PTVParameters for the detection parameters and run_ptv for the full tracking pipeline.\n\n\n\n\n\n","title":"Hammerhead.detect_particles"},{"category":"type","location":"reference/ptv/#Hammerhead.PTVParameters","page":"PTV (particle tracking)","text":"PTVParameters(; kwargs...)\n\nImmutable, validated configuration for particle detection, matching, and scattered-vector validation in detect_particles, run_ptv, and track_particles.\n\nKeyword arguments\n\nthreshold = :auto: detection intensity threshold — :auto for a robust floor bg + threshold_k·MAD, or an absolute intensity Real.\nthreshold_k = 4.0: MAD multiplier for the :auto threshold.\nmin_separation = 2.0: minimum spacing (px) between accepted peaks; the brighter of a closer pair is kept.\nmin_diameter = 1.0, max_diameter = 12.0: accepted particle diameter range (px, 4σ). Particles with a NaN diameter (centroid fallback) pass.\nsearch_radius = 3.0: match search radius (px) around the predicted frame-B position of each frame-A particle.\nintensity_weight = 0, diameter_weight = 0: optional nonnegative appearance penalties added to squared spatial match cost. The penalties use squared log-ratios, so they are scale-free; zero preserves distance-only matching exactly.\nuod_enable = true: flag outliers with the scattered normalized median test (Duncan et al. 2010).\nuod_threshold = 2.0: scattered-UOD sensitivity (higher is less sensitive).\nuod_neighbors = 8: number of nearest matched neighbors used per vector.\nuod_epsilon = 0.1: px noise floor for the scattered UOD (same rationale as the gridded UOD epsilon).\n\n\n\n\n\n","title":"Hammerhead.PTVParameters"},{"category":"type","location":"reference/ptv/#Hammerhead.PTVResult","page":"PTV (particle tracking)","text":"PTVResult{T<:AbstractFloat}\n\nResult of run_ptv. The numeric precision T follows the input images, like PIVResult.\n\nFields\n\nx, y: frame-A particle positions of the matched pairs (x along columns, y along rows, in pixels).\nu, v: displacement to frame B (px, package sign convention: a particle at (y, x) in frame A is found at (y + v, x + u) in frame B).\nmatch_residual: |found − predicted| distance (px) — a match-quality proxy.\noutliers: BitVector of scattered-UOD flags. Matched vectors are never replaced (a track is a measurement of one particle), only flagged.\nindex_a, index_b: the match i pairs particles_a[index_a[i]] with particles_b[index_b[i]].\nparticles_a, particles_b: all detections in each frame (unmatched ones are seeding dropout or new entries), kept for diagnostics.\nparameters: the PTVParameters used.\nscale: the PhysicalScale attached via the scale keyword of run_ptv or with_scale; nothing when none was attached. Metadata only — the arrays above stay in pixels until physical converts them.\n\n\n\n\n\n","title":"Hammerhead.PTVResult"},{"category":"method","location":"reference/ptv/#Hammerhead.ptv_to_grid-Union{Tuple{T}, Tuple{PTVResult{T}, Tuple{Int64, Int64}}} where T","page":"PTV (particle tracking)","text":"ptv_to_grid(result::PTVResult, image_size;\n            window_size = (32, 32), overlap = (16, 16),\n            min_count = 3, include_outliers = false) -> PIVResult\n\nBin the matched PTV vectors onto the regular interrogation grid that run_piv would use for image_size. Each vector contributes to the single bin whose center is nearest its frame-A position, and each bin value is the component-wise median of its vectors (robust to residual mismatches). Bins with fewer than min_count vectors are masked (mask = true, NaN fields), so the result plugs straight into the masked-result conventions (field_statistics, plotting). Outlier- flagged vectors are excluded unless include_outliers.\n\nThis is a binned PTV field, not a correlation measurement: peak_ratio, correlation_moment, and the uncertainty fields are NaN, and no vector is flagged an outlier in the returned grid. An attached PhysicalScale is carried over onto the grid; bin raw (pixel) results — the binning works in the frame-A image plane.\n\n\n\n\n\n","title":"Hammerhead.ptv_to_grid"},{"category":"function","location":"reference/ptv/#Hammerhead.run_ptv","page":"PTV (particle tracking)","text":"run_ptv(imgA, imgB, params = PTVParameters();\n        predictor = :piv, piv_passes = multipass_parameters([64, 32]),\n        mask = nothing, scale = nothing) -> PTVResult\n\nTwo-frame particle tracking velocimetry on an image pair. Particles are detected in both frames (detect_particles), each frame-A particle is matched to a frame-B particle by predictor-guided nearest neighbor with global cost-sorted greedy one-to-one assignment (Keane et al., 1995), and the matched displacements are validated with a scattered normalized median test (Duncan et al., 2010) that flags — but never replaces — outliers.\n\nThe predictor supplies the displacement field that centers each match search:\n\n:piv (default) runs a coarse run_piv on the pair internally (piv_passes), so run_ptv works out of the box at realistic densities and displacements — the hybrid PIV-guided default.\na PIVResult reuses an existing field (skips the redundant run).\na NamedTuple with x, y, u, v fields is used as-is.\nnothing gives pure nearest neighbor (zero displacement); set search_radius accordingly.\n\nmask is an optional image-sized Bool matrix marking excluded pixels (true = excluded); it is applied to detection in both frames and forwarded to the internal PIV run. scale attaches a PhysicalScale to the result as metadata (the fields stay in pixels until physical converts them; the internal PIV predictor always runs in pixels). Position attribution is frame-A: x/y are frame-A positions and u/v the displacement to frame B (see PTVResult). Empty frames or no matches yield a valid empty result rather than throwing.\n\n\n\n\n\n","title":"Hammerhead.run_ptv"},{"category":"type","location":"reference/ptv/#Hammerhead.TrackingResult","page":"PTV (particle tracking)","text":"TrackingResult{T<:AbstractFloat}\n\nResult of track_particles: the linked trajectories, the number of frames n_frames, the parameters used, and the optional PhysicalScale scale (nothing when none was attached — positions stay in pixels until physical converts them).\n\n\n\n\n\n","title":"Hammerhead.TrackingResult"},{"category":"type","location":"reference/ptv/#Hammerhead.Trajectory","page":"PTV (particle tracking)","text":"Trajectory{T<:AbstractFloat}\n\nA single particle track: start_frame (1-based) is the frame of the first point, and x/y hold one subpixel position per consecutive frame (no gaps in v1). length(t) is the number of points. See track_particles.\n\n\n\n\n\n","title":"Hammerhead.Trajectory"},{"category":"function","location":"reference/ptv/#Hammerhead.track_particles","page":"PTV (particle tracking)","text":"track_particles(frames, params = PTVParameters();\n                predictor = :piv, piv_passes = multipass_parameters([64, 32]),\n                min_track_length = 3, max_gap = 0, mask = nothing, scale = nothing,\n                image_type = Float64, progress = true) -> TrackingResult\n\nLink particles across a sequence of frames (≥ 2 file paths or real-valued matrices) into trajectories. Each frame is detected once; for every transition k → k+1, track heads are predicted — constant-velocity extrapolation for heads with ≥ 2 points, and a field predictor for fresh 1-point heads (the predictor option on the first transition, exactly as in run_ptv; the binned previous transition afterwards) — then matched with the greedy PTV matcher. Scattered-UOD-flagged links are rejected (never linked, so they cannot poison the constant-velocity predictor). max_gap keeps unmatched heads alive for a bounded number of missed frames and records reacquisition gaps explicitly in Trajectorysframes; its default zero terminates immediately as before. Unmatched frame-(k+1)` particles seed new tracks.\n\nOnly tracks with length ≥ min_track_length (which must be ≥ 2) are returned, sorted by start_frame then first position. scale attaches a PhysicalScale to the result (positions stay in pixels; see physical and trajectory_velocities). image_type is the element type frames load as (Float32 runs in single precision); progress is a Bool meter or an (i, n) callback ticked per transition, as in run_piv_sequence.\n\n\n\n\n\n","title":"Hammerhead.track_particles"},{"category":"method","location":"reference/ptv/#Hammerhead.trajectory_velocities-Union{Tuple{Trajectory{T}}, Tuple{T}, Tuple{Trajectory{T}, Union{Nothing, PhysicalScale}}} where T","page":"PTV (particle tracking)","text":"trajectory_velocities(t::Trajectory, scale = nothing) -> (u, v)\n\nPer-point displacement estimates along a trajectory (px per frame interval): central differences in the interior and one-sided differences at the ends. u is the column (x) component, v the row (y). Requires length(t) ≥ 2.\n\nWith a PhysicalScale the differences are multiplied by pixel_size / dt into physical velocities. Pass the owning result's scale field — this yields the same velocities whether t comes from a raw or a physical-converted TrackingResult, because the converted result's scale keeps dt (its positions are already lengths).\n\n\n\n\n\n","title":"Hammerhead.trajectory_velocities"},{"category":"section","location":"tutorials/gui_tour/#A-tour-of-the-graphical-user-interface-(GUI)","page":"A tour of the GUI","text":"This tutorial takes a pair of particle images to an explorable vector field in three form actions, then improves that first result the way a real analysis grows: attach a physical scale, mask what should not correlate, tune preprocessing against a live correlation probe, re-run, and analyze the flow. Calibration review, stereo, and particle tracking make a brief appearance at the end.\n\nThe GUI lives in a companion package, HammerheadGUI, so the core package never carries plotting dependencies. Install and launch it like any Julia package:\n\npkg> add HammerheadGUI        # `]` at the julia> prompt opens pkg>\n\njulia> using HammerheadGUI\njulia> batch_runner()         # the front door: an empty batch form\n\nOne reading note. Every window is a thin shell over a plain-Julia controller, and clicking a widget calls the same function the code below calls — the docs build cannot click, so where you would use the mouse this tutorial drives the controller instead. At your REPL every figure in this page opens as a live interactive window, and you can work with the mouse, the code, or both at once. (Why it is built this way, and what that lets you automate: the controller–view split.)","title":"A tour of the graphical user interface (GUI)"},{"category":"section","location":"tutorials/gui_tour/#First-vectors","page":"A tour of the GUI","text":"Your data is a folder of image files, added to the batch form with its \"add frames…\" file picker. The docs build fabricates a recording instead — a Lamb–Oseen vortex imaged as realistic particle images. Skip over this block; it merely stands in for your camera:\n\nusing Hammerhead\nusing Hammerhead.SyntheticData\nusing Random\n\ncenter, rc, Γ = (128.0, 128.0), 40.0, 1200.0\nfunction flow(x, y, z, t)\n    dx, dy = x - center[1], y - center[2]\n    r² = dx^2 + dy^2\n    k = r² < 1e-9 ? Γ / (2π * rc^2) : Γ / (2π * r²) * (1 - exp(-r² / rc^2))\n    return (-k * dy, k * dx, 0.0)\nend\n\nrng = MersenneTwister(42)\nimgA, imgB, _, _ = generate_synthetic_piv_pair(flow, (256, 256), 1.0;\n    particle_density = 0.05, background_noise = 0.03,\n    z_range = (-1.0, 1.0), rng)\n\nThe whole first analysis is three actions on the BatchRunner form, a front end to run_piv_sequence: add the frames, pick an effort preset from the \"effort\" menu (:low/:medium/:high — see Choose an effort level), press \"run\":\n\nusing HammerheadGUI\n\nbc = BatchRunner(files = Any[imgA, imgB])   # \"add frames…\"\nset_effort!(bc, :medium)                    # the \"effort\" menu\nstart!(bc; async = false)                   # the \"run\" button\nbc.status[]\n\nIn the GUI the run executes as a background task, so the window stays live: a progress bar counts pairs, \"cancel\" stops after the pair in flight (keeping the finished ones), and \"view results\" lights up as soon as the first pair completes, appending later pairs to the open explorer as they finish. The docs build runs synchronously and opens result_explorer on the output — exactly what \"view results\" does:\n\nex = ResultExplorer(bc.results[])\nfig = result_explorer(ex)\n\nThat is a working PIV measurement: the displacement-magnitude field under the vector arrows, a field menu of components and diagnostics, a frame slider for longer recordings, and click-to-inspect on every vector.\n\nIt can be better, though. The axes read pixels and the displacements pixels-per-frame; the frame edges correlate content that belongs to no flow; and the defaults know nothing about your imaging conditions. The rest of the walkthrough upgrades the same batch form one step at a time — which is how a real analysis usually iterates.","title":"First vectors"},{"category":"section","location":"tutorials/gui_tour/#Improve-it:-physical-units","page":"A tour of the GUI","text":"scale_tool turns a calibration image into a PhysicalScale: click the two endpoints of a feature of known physical size and type in the separation. Normally you would open a photographed target with scale_tool(\"plate.png\"); the docs build renders a synthetic plate — dots at exactly 15 mm spacing seen through a known camera (render_calibration_target). Skip this block too, it only fabricates that photo:\n\nθ = deg2rad(10.0)\nR = [cos(θ) 0.0 -sin(θ); 0.0 1.0 0.0; sin(θ) 0.0 cos(θ)]\ncamC = R' * [0.0, 0.0, -500.0]\nK = [3500.0 0.0 256.0; 0.0 -3500.0 256.0; 0.0 0.0 1.0]\ncam = PinholeCamera(K, R, -R * camC)\nplate = render_calibration_target(cam, (512, 512); spacing = 15.0)\n\nIn the window: click the centres of two neighbouring dots, enter the separation and units, and read the derived pixel size off the status line. Controllers.click! is literally the function a mouse click invokes:\n\nst = ScaleTool(plate)\ndot1 = world_to_pixel(cam, (0.0, 0.0, 0.0))     # two neighbouring dots,\ndot2 = world_to_pixel(cam, (15.0, 0.0, 0.0))    # 15 mm apart in the world\nHammerheadGUI.Controllers.click!(st, dot1[1], dot1[2])\nHammerheadGUI.Controllers.click!(st, dot2[1], dot2[2])\nset_separation!(st, 15.0)                        # mm between the clicks\nst.dt[] = 0.001                                  # 1 ms between frames\nst.time_unit[] = \"s\"\nscale_tool(st)\n\n\"Apply to batch\" copies the whole scale — pixel size, dt, and unit labels — into the batch form (apply_scale!); every result the batch produces from now on will carry it:\n\napply_scale!(bc, st)\nphysical_scale(st)","title":"Improve it: physical units"},{"category":"section","location":"tutorials/gui_tour/#Improve-it:-mask-what-should-not-correlate","page":"A tour of the GUI","text":"mask_editor draws exclusion polygons over a frame: left-click adds vertices (a click inside an existing polygon selects it instead), right-click closes the polygon, and buttons undo, delete, and grow or shrink the mask. In code those clicks are add_vertex! and close_active!:\n\nme = MaskEditor(imgA)\nfig = mask_editor(me)\nfor (x, y) in ((10, 10), (60, 10), (60, 60), (10, 60))\n    add_vertex!(me, x, y)\nend\nclose_active!(me)\nme.show_mask[] = true   # the \"show mask\" toggle\nfig\n\nThe editor exports the package's mask convention — image-sized Bool, true = excluded (the masking model) — and the batch form takes it directly (\"load mask…\" reads a saved mask image instead):\n\nbc.mask[] = polygon_mask(me)\ncount(bc.mask[])","title":"Improve it: mask what should not correlate"},{"category":"section","location":"tutorials/gui_tour/#Improve-it:-preprocessing,-tuned-with-a-correlation-probe","page":"A tour of the GUI","text":"preprocess_preview composes the core preprocessing set into an ordered, toggleable pipeline with a live raw/processed comparison. Give the PreprocessPreview controller the frame's pair partner and it also runs a single-window correlation probe: click a location on the processed image and that window's displacement and peak ratio update live as you toggle and tune steps — a direct readout of what each preprocessing choice does to the measurement itself, not just to how the image looks:\n\npp = PreprocessPreview(imgA; pair = imgB, enabled = [:highpass_filter])\nset_step_param!(pp, :highpass_filter, :sigma, 5)\nHammerheadGUI.Controllers.click!(pp, 190.0, 128.0)   # probe off the vortex core\npreprocess_preview(pp)\n\nprobe_summary(pp)\n\nToggle a step and the probe recomputes — here the displacement barely moves but the peak ratio shifts, which is exactly the comparison the probe exists for:\n\nenable_step!(pp, :percentile_stretch)\nprobe_summary(pp)\n\nenable_step!(pp, :percentile_stretch, false)\n\n\"Use in batch\" installs the pipeline; from code that is set_preprocess!, which snapshots the steps so later preview edits cannot affect a running batch:\n\nset_preprocess!(bc, pp)","title":"Improve it: preprocessing, tuned with a correlation probe"},{"category":"section","location":"tutorials/gui_tour/#Re-run-with-the-full-setup","page":"A tour of the GUI","text":"Back on the batch form: scale, mask, and preprocessing are now set, and the \"custom\" effort setting exposes the manual multi-pass window schedule and the accuracy options in their place — here the default 64/32/32 px schedule with the uncertainty estimator switched on:\n\nset_effort!(bc, :custom)     # back to the manual parameter form\nbc.uncertainty[] = true      # the \"uncertainty\" toggle\nfig = batch_runner(bc)\nstart!(bc; async = false)\nfig\n\nThe results are the plain Vector of PIVResults any script would produce, each carrying the physical scale:\n\nbc.results[]","title":"Re-run with the full setup"},{"category":"section","location":"tutorials/gui_tour/#Explore-and-analyze","page":"A tour of the GUI","text":"Open the explorer again — because the results now carry a scale, every axis, colorbar, and inspection panel reads in physical units:\n\nex = ResultExplorer(bc.results[])\nfig = result_explorer(ex)\n\nThe field menu holds the components and diagnostics plus the derived fields — vorticity, divergence, strain rate, swirling strength, and Q, computed via flow_derivatives and labelled 1/s here. The colorbar range defaults to a robust 2–98% percentile band over the valid vectors (color_limits) so outliers cannot wash it out, with manual overrides in the \"color range\" group. Switch to vorticity and inspect a vector near the core (the axes are in millimetres now):\n\nset_field!(ex, :vorticity)\nw = last(current_result(ex).x)      # field extent, mm\nselect_nearest!(ex, w / 2, w / 2)\ndescribe_selection(ex)\n\nfig\n\nThe tool menu adds interactive analysis on planar results. profile samples u, v, and |V| along a two-click line (extract_profile), drawn in a side panel; circulation accumulates a contour (right-click closes it) and evaluates circulation with both the line-integral and vorticity-area estimators:\n\nset_tool!(ex, :profile)\nHammerheadGUI.Controllers.click!(ex, 0.1w, 0.5w)\nHammerheadGUI.Controllers.click!(ex, 0.9w, 0.5w)\nfig\n\nset_tool!(ex, :circulation)\nfor (px, py) in ((0.35, 0.35), (0.75, 0.35), (0.75, 0.75), (0.35, 0.75))\n    HammerheadGUI.Controllers.click!(ex, px * w, py * w)   # clear of the mask\nend\nHammerheadGUI.Controllers.alt_click!(ex)\ntool_summary(ex)","title":"Explore and analyze"},{"category":"section","location":"tutorials/gui_tour/#Beyond-the-planar-workflow","page":"A tour of the GUI","text":"Calibration review. calibration_review runs the dot-grid detection on real plate images, fits a camera, and shows each plate with its dots colored by reprojection error. On the Particle Image Velocimetry (PIV) Challenge case-4E plates from the real stereo tutorial:\n\ndir = joinpath(pkgdir(Hammerhead), \"test\", \"reference_images\", \"E\")\nplates = [load_image(joinpath(dir, \"E_camera_1_z_$k.png\")) for k in (1, 4, 7)]\n\ncalibration_review(plates, [-3.0, 0.0, 3.0]; spacing = 15.0,\n    two_level = true, level_separation = 3.0, origin_offset = (30.0, 7.5))\n\nThe fiducial markers are outlined in cyan, the slider steps through the planes, and the camera-model menu refits on the spot; what residuals to expect from a physical plate (these ~0.5 px are the plate, not the detection) is covered in the stereo-rig how-to. Its sibling selfcal_review browses a SelfCalibrationReport with the per-pass disparity maps in an embedded explorer.\n\nStereo batch. stereo_calibration embeds two of these reviews side by side and builds the shared-grid ImageDewarper pair (build_dewarpers); stereo_batch_runner then drives run_piv_stereo_sequence over two synchronized frame lists — same live progress, live results, and incremental output as the planar form. See the GUI how-to for the workflow.\n\nScattered results. The explorer browses all four persisted result types, mixed sequences included. A PTVResult draws as a colored particle scatter with its scattered field menu:\n\nptv = run_ptv(imgA, imgB)\nresult_explorer(ptv)","title":"Beyond the planar workflow"},{"category":"section","location":"tutorials/gui_tour/#Where-to-go-next","page":"A tour of the GUI","text":"Task recipes — masks from files, batch output, live viewing, stereo: Work interactively with the GUI.\nWhy every tool is a controller + view pair, and what that lets you automate: The GUI's controller–view split.\nThe full API: GUI reference.\n\n\n\nThis page was generated using Literate.jl.","title":"Where to go next"},{"category":"section","location":"howto/effort/#Choose-an-effort-level","page":"Choose an effort level","text":"Goal: pick a particle image velocimetry (PIV) schedule quickly, then override only the knobs your data actually requires.\n\nFor routine runs, omit the explicit pass vector and use effort:\n\nquick = run_piv(imgA, imgB; effort = :low)\ndraft = run_piv(imgA, imgB; effort = :medium)\nfinal = run_piv(imgA, imgB; effort = :high)\n\nThe same keyword is accepted by run_piv_sequence, run_piv_ensemble, and run_piv_stereo. The current presets are:\n\nEffort Schedule Options Typical synthetic result\n:low [32] PIVParameters defaults 1× time; ~0.10 px uniform-shift root-mean-square (RMS) error, ~0.43 px vortex RMS error\n:medium [64, 32] defaults ~4× time; ~0.02 px uniform-shift RMS error, ~0.22 px vortex RMS error\n:high [128, 64, 32] padding = true, apodization = :gauss, max_iterations = 2; final pass uncertainty = true ~32× time; ~0.02 px uniform-shift RMS error, ~0.09 px vortex RMS error\n\nThose numbers are typical single-threaded timings on 256×256 synthetic pairs, not guarantees for every seeding density or flow.\n\nEffort and execution backend are independent choices. For example, run_piv(imgA, imgB; effort = :high, backend = :amdgpu) runs the same high-effort schedule through the AMD GPU extension, including final-pass Float64 uncertainty statistics. Small :low and :medium jobs often remain faster on the CPU because GPU setup and transfers do not amortize. See Run PIV on a GPU before selecting a device backend.","title":"Choose an effort level"},{"category":"section","location":"howto/effort/#Override-the-parts-that-matter","page":"Choose an effort level","text":"When effort is set, PIVParameters keyword arguments override the preset:\n\nresult = run_piv(imgA, imgB;\n    effort = :high,\n    window_size = 16,        # final pass is 16 px; pyramid rescales with it\n    uncertainty = false,     # final-pass override\n)\n\nMost field keywords apply to every pass. window_size sets the final window size and rescales the pyramid, while uncertainty, max_iterations, and keep_correlation_planes apply only to the final pass. Use final = (;) when you need an explicit last-pass override; it wins over both the preset and the field keywords.\n\nFinal window size is a physics and image-quality choice. Smaller windows give denser vectors, but they also contain fewer particles and can be noisier; the default :high preset stops at 32 px for that reason. Use window_size = 16 only when the seeding density and signal quality support it.\n\nUse an explicit multipass_parameters(...) schedule when you need per-pass control beyond those overrides. effort and an explicit PIVParameters or pass vector cannot be combined in one call.","title":"Override the parts that matter"},{"category":"section","location":"explanation/correlation/#Correlation-accuracy","page":"Correlation accuracy","text":"Particle image velocimetry (PIV) estimates a window's displacement from the location of its correlation peak. The defaults of PIVParameters favor speed; two switches — padding and apodization — trade roughly 4× the fast Fourier transform (FFT) cost for a large accuracy gain. This page explains what each one does and what accuracy to expect.","title":"Correlation accuracy"},{"category":"section","location":"explanation/correlation/#Enlarged-search-areas","page":"Correlation accuracy","text":"Set search_area_size larger than window_size when the first-pass displacement can exceed the interrogation window's useful capture range. The frame-A interrogation window remains small (preserving spatial resolution and particle sampling) and is embedded at the center of the larger frame-B search area. Each size difference must be even so the two footprints have exactly the same pixel-grid center. Grid stride remains window_size - overlap; only the outer centers move inward to keep the search area inside the images.\n\nThe correlation plane is search-area-sized (twice that size with padding), and only lags for which the interrogation footprint lies fully inside the search area are eligible peaks. Gaussian apodization is applied to the interrogation footprint; the larger search footprint is left untapered so candidates near its boundary are not suppressed. Existing equal-area runs use the original correlation path bit for bit. Enlarged search areas currently require backend = :cpu; KA-family backends reject them explicitly.","title":"Enlarged search areas"},{"category":"section","location":"explanation/correlation/#Plain-circular-correlation-biases-toward-zero","page":"Correlation accuracy","text":"FFT-based cross-correlation is circular: content shifted out of one side of the window wraps around to the other. With the default un-padded correlation, the wrap-around overlap decreases linearly as displacement grows, which systematically pulls the measured peak toward zero — about 0.15 px of bias on typical synthetic data. That is acceptable for exploratory work, but it is the dominant error source long before random noise matters.","title":"Plain circular correlation biases toward zero"},{"category":"section","location":"explanation/correlation/#Zero-padding-and-overlap-gain-normalization","page":"Correlation accuracy","text":"Setting padding = true zero-pads each window to twice its size before the FFT, turning circular correlation into true linear correlation — no wrap-around. Linear correlation has its own bias, though: a displacement of d leaves only N - d overlapping pixels, so raw peak heights fall off linearly with displacement, skewing the peak shape. Hammerhead therefore normalizes each correlation plane by the overlap gain (the number of contributing pixel pairs at each shift), which restores an unbiased peak. For an enlarged search area, eligible lags already keep the complete interrogation footprint inside the search footprint, so their overlap weight is constant and no additional gain is needed. This normalization is built in — padding = true always includes it.","title":"Zero padding and overlap-gain normalization"},{"category":"section","location":"explanation/correlation/#Gaussian-apodization","page":"Correlation accuracy","text":"Sharp window edges leak spectral energy across the correlation plane and distort the peak's shape at subpixel scale. apodization = :gauss applies a Gaussian taper to each interrogation window before correlating, suppressing edge effects.\n\nThe combination padding = true, apodization = :gauss is the accuracy configuration: on noise-free synthetic data with a converged multi-pass schedule it reaches about 0.03 px root-mean-square (RMS) error. All tutorials use it.","title":"Gaussian apodization"},{"category":"section","location":"explanation/correlation/#Subpixel-peak-fitting-and-peak-locking","page":"Correlation accuracy","text":"The integer peak location is refined by fitting a Gaussian to the peak's neighborhood (subpixel_method):\n\n:gauss3 (default) — two independent 3-point Gaussian fits, one per axis. Fast and accurate for round peaks.\n:gauss9 — closed-form 2D Gaussian regression on the 3×3 neighborhood (Nobach and Honkanen, 2005). Exact for rotated elliptical peaks and measurably less prone to peak locking (the tendency of subpixel estimates to cluster at integer displacements), at nearly the same cost.\n:gauss2d — iterative least-squares 2D Gaussian fit; the most flexible and by far the slowest.\n\nDiagnose peak locking in your own data with peak_locking, which histograms the fractional parts of the displacements and reports a locking index.","title":"Subpixel peak fitting and peak locking"},{"category":"section","location":"explanation/correlation/#Phase-correlation","page":"Correlation accuracy","text":"correlation_method = :phase whitens the cross-power spectrum before the inverse transform, sharpening the peak and suppressing broadband intensity variations at some cost in noise robustness. The standard :cross method with good preprocessing is usually the better starting point.","title":"Phase correlation"},{"category":"section","location":"howto/masking/#Mask-reflections-and-geometry","page":"Mask reflections and geometry","text":"Goal: exclude a region — model geometry, a wall, a persistent reflection — from the analysis so it produces no vectors and cannot contaminate its neighbors. Background on the semantics is in the masking model.","title":"Mask reflections and geometry"},{"category":"section","location":"howto/masking/#Draw-a-polygon-mask","page":"Mask reflections and geometry","text":"polygon_mask rasterizes a polygon given as (x, y) corners in pixel coordinates (x along columns):\n\nusing Hammerhead\n\nmask = polygon_mask(size(imgA), [(120, 40), (480, 90), (450, 300), (100, 260)])\nresult = run_piv(imgA, imgB, passes; mask)\n\nCombine several exclusion regions with element-wise OR:\n\nblade  = polygon_mask(size(imgA), blade_vertices)\nglare  = polygon_mask(size(imgA), glare_vertices)\nresult = run_piv(imgA, imgB, passes; mask = blade .| glare)","title":"Draw a polygon mask"},{"category":"section","location":"howto/masking/#Load-a-mask-image","page":"Mask reflections and geometry","text":"Many datasets ship a mask as an image file (e.g. the PIV Challenge case 1C impeller mask). load_mask thresholds it into a Bool matrix:\n\nmask = load_mask(\"mask.png\")                 # bright pixels = excluded\nmask = load_mask(\"mask.png\"; invert = true)  # dark pixels = excluded\n\nAny image-sized Bool/BitMatrix works as a mask — build one from raw pixel logic if that's easier, e.g. mask = background .> 0.8 to exclude persistently bright (reflective) pixels of a compute_background image.\n\nFor common bright reflections, dark bodies, low-texture regions, or sharp geometry edges, automatic_mask provides intensity-, local-contrast-, and edge-derived masks. Its two-frame method applies the detector to both frames and returns their union, the safe pair-mask convention for a moving boundary:\n\nmask = automatic_mask(imgA, imgB; method = :intensity,\n                      threshold = 0.9, side = :high, grow = 3)\nmask = grow_mask(mask, 2)       # add another safety margin\nmask = shrink_mask(mask, 1)     # trim an over-conservative mask","title":"Load a mask image"},{"category":"section","location":"howto/masking/#Control-when-a-window-is-dropped","page":"Mask reflections and geometry","text":"Windows whose masked-pixel fraction reaches mask_threshold (default 0.5) are dropped entirely; windows below it correlate over their valid pixels only. Lower the threshold to be more conservative near edges:\n\nresult = run_piv(imgA, imgB, passes; mask, mask_threshold = 0.25)","title":"Control when a window is dropped"},{"category":"section","location":"howto/masking/#Read-the-results-correctly","page":"Mask reflections and geometry","text":"result.mask       # true = window dropped (NaN fields, no measurement)\nresult.outliers   # true = measured but failed validation (replaced if enabled)\nvalid = .!(result.mask .| result.outliers)\n\nMasked cells are never counted as outliers, and NaNs in u/v at masked cells are intentional — filter with valid before computing your own statistics (the built-in field_statistics and error_statistics already do).","title":"Read the results correctly"},{"category":"section","location":"howto/masking/#Batch-and-stereo","page":"Mask reflections and geometry","text":"The same mask keyword flows through run_piv_sequence and run_ptv_sequence. It may be one static mask, a per-pair sequence, a per-frame sequence (the two exposure masks are unioned), or an (i, frameA, frameB) -> mask callback. Ensemble correlation uses one static mask for all pairs. For stereo, pass a grid-sized mask to run_piv_stereo / self_calibrate; it is combined with the dewarpers' out-of-view masks automatically.","title":"Batch and stereo"},{"category":"section","location":"reference/validation/#Validation-and-quality","page":"Validation and quality","text":"Vector validation (universal outlier detection, peak-ratio and other criteria), outlier replacement, and field smoothing. Validation runs automatically inside run_piv — see PIVParameters for the knobs and the validation how-to for tuning guidance; the functions below are the building blocks, usable standalone.\n\nPages = [\"validation.md\"]","title":"Validation and quality"},{"category":"type","location":"reference/validation/#Hammerhead.CorrelationMomentValidator","page":"Validation and quality","text":"CorrelationMomentValidator(threshold)\n\nFlag vectors whose correlation moment exceeds threshold, i.e. whose correlation peak is too broad (NaN moments are flagged too). Pair spec: :correlation_moment => threshold. See the validation how-to for the full list of validators.\n\n\n\n\n\n","title":"Hammerhead.CorrelationMomentValidator"},{"category":"type","location":"reference/validation/#Hammerhead.LocalValidator","page":"Validation and quality","text":"LocalValidator <: PIVValidator\n\nValidators that judge each vector from its own properties alone.\n\n\n\n\n\n","title":"Hammerhead.LocalValidator"},{"category":"type","location":"reference/validation/#Hammerhead.NeighborhoodValidator","page":"Validation and quality","text":"NeighborhoodValidator <: PIVValidator\n\nValidators that judge each vector relative to its spatial neighbors.\n\n\n\n\n\n","title":"Hammerhead.NeighborhoodValidator"},{"category":"type","location":"reference/validation/#Hammerhead.PIVValidator","page":"Validation and quality","text":"PIVValidator\n\nAbstract supertype for vector validation criteria. A validator is applied with apply_validator!, which ORs the vectors it rejects into result.outliers; validate_vectors! applies a whole pipeline.\n\n\n\n\n\n","title":"Hammerhead.PIVValidator"},{"category":"type","location":"reference/validation/#Hammerhead.PeakRatioValidator","page":"Validation and quality","text":"PeakRatioValidator(threshold)\n\nFlag vectors whose correlation peak ratio falls below threshold (NaN ratios are flagged too). Pair spec: :peak_ratio => threshold. See the validation how-to for the full list of validators.\n\n\n\n\n\n","title":"Hammerhead.PeakRatioValidator"},{"category":"type","location":"reference/validation/#Hammerhead.UniversalOutlierValidator","page":"Validation and quality","text":"UniversalOutlierValidator(threshold; neighborhood_size = 2, epsilon = 0.1)\n\nFlag vectors that fail the normalized median test — see universal_outlier_detection for the parameters. Pair spec: :uod => (threshold = 2.0, neighborhood_size = 2, epsilon = 0.1) (only threshold is required; :universal_outlier is an alias). See the validation how-to for the full list of validators.\n\n\n\n\n\n","title":"Hammerhead.UniversalOutlierValidator"},{"category":"type","location":"reference/validation/#Hammerhead.VelocityMagnitudeValidator","page":"Validation and quality","text":"VelocityMagnitudeValidator(min, max)\n\nFlag vectors whose displacement magnitude lies outside [min, max] pixels (NaN magnitudes are flagged too). Pair spec: :velocity_magnitude => (min = 0, max = 50); min defaults to 0. See the validation how-to for the full list of validators.\n\n\n\n\n\n","title":"Hammerhead.VelocityMagnitudeValidator"},{"category":"method","location":"reference/validation/#Hammerhead.apply_validator!-Tuple{PIVResult, PeakRatioValidator}","page":"Validation and quality","text":"apply_validator!(result::PIVResult, v::PIVValidator) -> PIVResult\n\nApply a single validator, ORing the vectors it rejects into result.outliers.\n\n\n\n\n\n","title":"Hammerhead.apply_validator!"},{"category":"method","location":"reference/validation/#Hammerhead.calculate_correlation_moment-Tuple{AbstractMatrix{<:Real}, Tuple{Real, Real}}","page":"Validation and quality","text":"calculate_correlation_moment(R, peakloc::Tuple{Real,Real}; neighborhood_size=3)\n\nSquare root of the second moment of the correlation values in an odd-sized square neighborhood around the (subpixel) peak location (row, col) — a weighted standard deviation of the peak, usable as an uncertainty proxy. Lower values indicate a sharper peak. Returns NaN when the neighborhood is empty or its correlation sum is non-positive.\n\n\n\n\n\n","title":"Hammerhead.calculate_correlation_moment"},{"category":"method","location":"reference/validation/#Hammerhead.calculate_peak_ratio-Union{Tuple{T}, Tuple{AbstractMatrix{T}, Tuple{Int64, Int64}}} where T<:AbstractFloat","page":"Validation and quality","text":"calculate_peak_ratio(R, peakloc::Tuple{Int,Int}; exclusion_radius=2,\n                     peak_finder=:regionalmax)\n\nRatio of the primary correlation peak at peakloc to the largest other regional maximum (peak_finder = :regionalmax, the default), or to the largest value outside a square exclusion zone of ±exclusion_radius pixels around it (peak_finder = :exclusion). Returns Inf when no positive secondary peak exists. Higher values indicate a more reliable displacement estimate.\n\n\n\n\n\n","title":"Hammerhead.calculate_peak_ratio"},{"category":"method","location":"reference/validation/#Hammerhead.smoothn-Tuple{AbstractMatrix{<:Real}}","page":"Validation and quality","text":"smoothn(y; s = nothing, weights = nothing, robust = false) -> (; z, s)\n\nPenalized least-squares smoothing of a gridded field (Garcia, CSDA 2010), solved in the DCT domain. The smoothing parameter s is chosen by generalized cross-validation when not given; larger values smooth more. weights (same size, ≥ 0) express per-point confidence — e.g. .!(result.outliers .| result.mask) — and non-finite entries of y automatically get weight 0 and are filled from the smooth surface. robust = true adds bisquare reweighting passes that resist outliers not captured by the weights. Returns the smoothed field z and the s used.\n\n\n\n\n\n","title":"Hammerhead.smoothn"},{"category":"method","location":"reference/validation/#Hammerhead.universal_outlier_detection-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractMatrix{T}, Real}} where T<:Real","page":"Validation and quality","text":"universal_outlier_detection(u, v, threshold;\n                            neighborhood_size=1, epsilon=0.1,\n                            exclude=nothing) -> BitMatrix\n\nUniversal outlier detection (normalized median test, Westerweel & Scarano 2005) on a 2D vector field. For each vector, the residual relative to the median of its neighbors is normalized by the median absolute neighbor residual plus epsilon; the vector is flagged when either component's normalized residual exceeds threshold.\n\nneighborhood_size is the number of neighbor layers (1 → 3×3, 2 → 5×5). epsilon is the assumed measurement noise level in the same units as u and v (typically ≈ 0.1 px for PIV); it keeps ordinary subpixel noise in smooth regions from being flagged.\n\nCells marked true in exclude (e.g. masked windows) are never flagged and never enter a neighbor median, so NaN entries there cannot poison the test.\n\nReturns a BitMatrix where true marks an outlier.\n\n\n\n\n\n","title":"Hammerhead.universal_outlier_detection"},{"category":"method","location":"reference/validation/#Hammerhead.validate_vectors!-Tuple{PIVResult, Any}","page":"Validation and quality","text":"validate_vectors!(result::PIVResult, pipeline) -> PIVResult\n\nApply a validation pipeline — a single validator or specification, or a tuple of them (see parse_validator) — accumulating rejected vectors into result.outliers.\n\n\n\n\n\n","title":"Hammerhead.validate_vectors!"},{"category":"section","location":"#Hammerhead","page":"Home","text":"Documentation for Hammerhead, a Julia package for particle image velocimetry (PIV). Hammerhead covers planar two-dimensional, two-component (2D2C) measurements, stereoscopic two-dimensional, three-component (2D3C) measurements, and two-frame particle tracking velocimetry (PTV) with multi-frame trajectory linking. The analysis core provides multi-pass image deformation, vector validation, per-vector uncertainty quantification, ensemble correlation, camera calibration with disparity self-calibration, physical-unit scaling, batch processing with incremental result files, and optional GPU execution. A companion desktop package, HammerheadGUI, wraps the same API in interactive tools — result explorer, mask editor, preprocessing preview with a correlation probe, and batch forms (take the tour).\n\nPIV starts from two images of tracer particles separated by a known time interval. By finding how the particle pattern moves from the first image to the second, it estimates a displacement vector at each location in the image. Supplying the physical pixel size and time interval turns those displacements into velocities.","title":"Hammerhead"},{"category":"section","location":"#Installation","page":"Home","text":"pkg> add Hammerhead        # `]` at the julia> prompt opens pkg>\npkg> add HammerheadGUI     # optional: the desktop GUI tools","title":"Installation"},{"category":"section","location":"#Quick-example","page":"Home","text":"The simplest complete analysis is one call with an effort preset. run_piv operates on in-memory image pairs (any equally sized real-valued matrices); load_image loads image files as grayscale Matrix{Float64}:\n\nusing Hammerhead\n\nimgA = load_image(\"frame_0001.tif\")\nimgB = load_image(\"frame_0002.tif\")\n\nresult = run_piv(imgA, imgB; effort = :high)   # or :low / :medium\n\neffort picks a full multi-pass schedule sized to the images (see Choose an effort level). When your data needs specific knobs, pass an explicit schedule instead:\n\n# Multi-pass with symmetric image deformation: each pass uses the previous\n# validated field as a predictor and shrinks the window.\npasses = multipass_parameters([64, 32, 16, 16];\n    padding = true,         # zero-padded (linear) correlation\n    apodization = :gauss,   # Gaussian window on each interrogation window\n    uncertainty = true,     # per-vector uncertainty on the final pass\n)\nresult = run_piv(imgA, imgB, passes)\n\nresult.u, result.v    # displacement field (px), u along x/columns\nresult.x, result.y    # interrogation grid centers (px)\nresult.outliers       # validation flags\n\nThe returned u and v values are displacements, not yet physical velocities. See Scale results to physical units when your pixel size and frame interval are known.\n\nWhole recordings are processed with run_piv_sequence, which loads frame pairs (see image_pairs), applies optional preprocessing, and persists results incrementally in the JLD2 Julia data format (save_results / load_results).","title":"Quick example"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"The documentation follows the Diátaxis structure:\n\nTutorials — guided, executable walkthroughs. Start with Your first vector field or the point-and-click path, a tour of the GUI; continue with a real wind-tunnel recording, then stereo PIV end to end, stereo on a real recording, and particle tracking velocimetry.\nHow-to guides — recipes for specific jobs: masking, preprocessing, effort selection, physical-unit scaling, GPU execution, validation tuning, ensemble correlation, batch processing, working with the GUI, and calibrating a real stereo rig.\nExplanation — how and why Hammerhead works the way it does: coordinate conventions, correlation accuracy, multi-pass deformation, the masking model, uncertainty quantification, stereo geometry and self-calibration, the GUI's controller–view split, the numeric precision policy, and the compatibility policy.\nReference — the API, one page per topic, starting at Core pipeline and parameters; the GUI's API is under GUI (HammerheadGUI).","title":"Where to go next"},{"category":"section","location":"explanation/multipass/#Multi-pass-interrogation-and-image-deformation","page":"Multi-pass interrogation and image deformation","text":"In particle image velocimetry (PIV), a single correlation pass faces a hard trade-off: small windows resolve fine flow structure but can only measure small displacements reliably (the quarter-window rule), while large windows measure large displacements but average over structure. Multi-pass interrogation with window refinement gets both, and image deformation extends it to strongly sheared flows (Scarano, 2002).","title":"Multi-pass interrogation and image deformation"},{"category":"section","location":"explanation/multipass/#The-predictor–corrector-loop","page":"Multi-pass interrogation and image deformation","text":"run_piv(imgA, imgB, passes) with a vector of PIVParameters — conveniently built with multipass_parameters — runs one correlation pass per entry:\n\nThe first pass measures the field at the coarsest window size.\nEach later pass takes the previous pass's validated field as a predictor: the field is smoothed (a 3×3 binomial kernel, controlled by predictor_smoothing), interpolated to pixel resolution, and used to deform the images.\nThe pass then correlates the deformed images, measuring only the small residual displacement, and adds the predictor back.\n\nBecause each pass only needs to measure the residual, window sizes can shrink across passes — multipass_parameters([64, 32, 16]) — without violating the quarter-window limit, even when the total displacement is large.","title":"The predictor–corrector loop"},{"category":"section","location":"explanation/multipass/#Symmetric-(central-difference)-deformation","page":"Multi-pass interrogation and image deformation","text":"Hammerhead deforms both images symmetrically: image A is resampled shifted by −d/2 and image B by +d/2, where d is the predictor displacement at each pixel (cubic B-spline resampling). Content displaced by exactly d is then aligned in both outputs. Compared to deforming only one image, the symmetric scheme is second-order accurate: the measurement is centered at the midpoint of the particle trajectory, which cancels the leading-order bias in curved or sheared flow.","title":"Symmetric (central-difference) deformation"},{"category":"section","location":"explanation/multipass/#Convergence-sweeps-and-iterative-passes","page":"Multi-pass interrogation and image deformation","text":"A pass with max_iterations > 1 adds convergence sweeps at that window size: it feeds its own validated field back as the deformation predictor and re-correlates, sweep after sweep, until the field converges or the budget is spent. Residual displacements shrink toward zero and the measurement approaches the deformation-limited accuracy —\n\nmultipass_parameters([64, 32, 16]; final = (max_iterations = 3,))\n\nThis iterates the 16-px stage and stops as soon as continuing would not change the answer. Repeating the final window size explicitly — multipass_parameters([64, 32, 16, 16, 16]) — is the older equivalent form when the early exit is disabled. Convergence means the 95th percentile of the per-vector displacement change between successive sweeps drops below convergence_tol (0.05 px by default). The criterion is a percentile rather than a maximum deliberately: a few bistable low-signal windows flicker between correlation peaks for arbitrarily many sweeps (on synthetic test scenes the maximum change stays near a pixel while the median falls below 10⁻³ px), and those windows are validation's problem — a max-norm would never converge and the early exit would be dead. Setting convergence_tol = 0 disables the early exit, making the pass run exactly max_iterations sweeps — exactly equivalent to repeating the pass that many times in the schedule.\n\nIteration also stops validation failures from cascading: within an iterating pass a flagged vector's local-median replacement seeds the next sweep's deformation, and the window is then re-measured — so replacement artifacts relax toward measured data within the stage instead of leaking into the next (smaller-window) pass's predictor, where a corrupted predictor would spoil every window it touches.\n\nEach extra sweep costs one full-image re-deformation plus one re-correlation of every window (roughly the cost of an extra pass; the deformation is O(image) no matter how few windows still change, which is why Hammerhead re-correlates the whole field rather than tracking per-vector convergence).\n\nTwo features require a converged schedule:\n\nPer-vector uncertainty quantification assumes the correlation peak of the deformed windows sits at nearly zero residual; it runs on the final pass only.\nThe ~0.03 px root-mean-square (RMS) accuracy figure of the padded + apodized configuration is only reached once the residual is small.","title":"Convergence sweeps and iterative passes"},{"category":"section","location":"explanation/multipass/#Validation-between-passes","page":"Multi-pass interrogation and image deformation","text":"Every pass validates its field (see PIVParameters) and intermediate passes always replace invalid vectors regardless of the replace_outliers setting — a spike in the predictor would otherwise corrupt the deformation for every window it touches. The sweeps of an iterating pass replace internally for the same reason, but the returned field still honors replace_outliers: with replacement off, cells that are still flagged after the last sweep hold their measured displacement. Masked regions are filled from valid neighbors before smoothing for the same reason (see the masking model).","title":"Validation between passes"},{"category":"section","location":"howto/ensemble/#Ensemble-correlation-for-low-signal-to-noise-ratio","page":"Ensemble correlation for low SNR","text":"Goal: extract a mean displacement field from recordings whose individual pairs are too noisy for reliable peaks — micro-scale particle image velocimetry (micro-PIV), weak seeding, or low laser power (for example, PIV Challenge case 4A). This is the low signal-to-noise ratio (SNR) regime.","title":"Ensemble correlation for low signal-to-noise ratio"},{"category":"section","location":"howto/ensemble/#When-it-applies","page":"Ensemble correlation for low SNR","text":"Ensemble (sum-of-correlation) PIV (Meinhart et al., 2000) averages each interrogation window's correlation planes across many pairs before locating the peak once. Random noise peaks average out; the displacement peak reinforces. The catch: it assumes statistically stationary flow — the result is the ensemble-mean field, and pair-to-pair fluctuation is averaged away, not measured.","title":"When it applies"},{"category":"section","location":"howto/ensemble/#Basic-use","page":"Ensemble correlation for low SNR","text":"run_piv_ensemble takes the same pair list as run_piv_sequence:\n\nusing Hammerhead\n\nfiles = readdir(\"micropiv\"; join = true)\npairs = image_pairs(files)                 # (1,2), (3,4), ... double-frame\npasses = multipass_parameters([64, 32, 16]; padding = true, apodization = :gauss)\n\nresult = run_piv_ensemble(pairs, passes; preprocess = img -> highpass_filter!(img, sigma = 6))\n\nMulti-pass works as in the single-pair engine, with one difference: each pass's ensemble field acts as a shared deformation predictor for every pair of the next pass.","title":"Basic use"},{"category":"section","location":"howto/ensemble/#Keep-large-ensembles-on-a-GPU","page":"Ensemble correlation for low SNR","text":"Pass backend = :amdgpu or :cuda to accumulate the summed planes on the device across every pair:\n\nusing AMDGPU\nresult = run_piv_ensemble(pairs, passes;\n    backend = :amdgpu,\n    preprocess = img -> highpass_filter!(img, sigma = 6),\n)\n\nLoading and preprocess remain CPU work. The correlation accumulator and, when enabled, Float64 uncertainty statistics remain device-resident until the final field is built. Accumulator memory scales with vector-grid density and correlation-plane area, not pair count; padding quadruples the plane footprint. Use the sizing formula and validation workflow in Run PIV on a GPU before a large production ensemble.","title":"Keep large ensembles on a GPU"},{"category":"section","location":"howto/ensemble/#Stereo-ensembles","page":"Ensemble correlation for low SNR","text":"For a statistically stationary low-SNR stereo recording, compose one ensemble field per camera followed by calibrated 3C reconstruction:\n\nmean3c = run_piv_stereo_ensemble(cam1_pairs, cam2_pairs, dw1, dw2, passes;\n    preprocess = (preprocess_cam1, preprocess_cam2),\n)\n\nRaw frames are loaded and dewarped lazily on each pass, so the driver does not retain the entire dewarped recording. As in planar ensemble PIV, the result is one stationary mean field. Use run_piv_stereo_sequence followed by field_statistics when physical fluctuations and all six Reynolds-stress terms are required.","title":"Stereo ensembles"},{"category":"section","location":"howto/ensemble/#Practical-notes","page":"Ensemble correlation for low SNR","text":"More pairs beat bigger windows. The whole point is that window size no longer has to compensate for noise; keep windows sized to the flow structure and add pairs until the field is clean.\npeak_ratio describes the ensemble planes, so it improves with pair count — a rising ensemble peak ratio is your convergence indicator.\nFile paths are reloaded once per pass. For many passes over slow storage, load frames into memory first and pass matrices.\nPreprocessing (preprocess, image_type) and masking (mask, one static mask for all pairs) work exactly as in the batch driver.","title":"Practical notes"},{"category":"section","location":"howto/ensemble/#Uncertainty-of-the-ensemble-mean","page":"Ensemble correlation for low SNR","text":"With uncertainty = true (final pass repeated for convergence — see uncertainty quantification), the correlation-statistics estimator pools its sums across all pairs, so uncertainty_u/uncertainty_v describe the noise-driven uncertainty of the ensemble-mean vector and shrink as pairs are added:\n\npasses = multipass_parameters([64, 32, 16, 16];\n    padding = true, apodization = :gauss, uncertainty = true)\nresult = run_piv_ensemble(pairs, passes)\n\nThis does not include genuine flow fluctuation. If the flow is not perfectly stationary, quantify the fluctuation separately with field_statistics over single-pair results — when the per-pair SNR allows it — and treat the ensemble uncertainty as a lower bound.","title":"Uncertainty of the ensemble mean"}]
}
